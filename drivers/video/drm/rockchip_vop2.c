// SPDX-License-Identifier: GPL-2.0-only
/*
 * (C) Copyright 2008-2017 Fuzhou Rockchip Electronics Co., Ltd
 *
 */

#include <config.h>
#include <common.h>
#include <errno.h>
#include <malloc.h>
#include <fdtdec.h>
#include <fdt_support.h>
#include <regmap.h>
#include <asm/arch/cpu.h>
#include <asm/unaligned.h>
#include <asm/io.h>
#include <linux/list.h>
#include <linux/log2.h>
#include <linux/media-bus-format.h>
#include <asm/arch/clock.h>
#include <asm/gpio.h>
#include <linux/err.h>
#include <linux/ioport.h>
#include <dm/device.h>
#include <dm/read.h>
#include <dm/ofnode.h>
#include <fixp-arith.h>
#include <syscon.h>
#include <linux/iopoll.h>
#include <dm/uclass-internal.h>
#include <stdlib.h>
#include <dm/of_access.h>

#include "rockchip_display.h"
#include "rockchip_crtc.h"
#include "rockchip_connector.h"
#include "rockchip_phy.h"
#include "rockchip_post_csc.h"

// PRQA S 3101 ++
// PRQA S 5118 ++
// PRQA S 5124 ++
// PRQA S 5125 ++
// PRQA S 5129 ++

/* System registers definition */
#define RK3568_REG_CFG_DONE			0x000U
#define	CFG_DONE_EN				BIT(15)

#define RK3568_VERSION_INFO			0x004U
#define EN_MASK					1U

#define RK3568_AUTO_GATING_CTRL			0x008U
#define AUTO_GATING_EN_SHIFT			31
#define PORT_DCLK_AUTO_GATING_EN_SHIFT		14
#define ACLK_PRE_AUTO_GATING_EN_SHIFT		7

#define RK3576_SYS_AXI_HURRY_CTRL0_IMD		0x014U
#define AXI0_PORT_URGENCY_EN_SHIFT		24

#define RK3576_SYS_AXI_HURRY_CTRL1_IMD		0x018U
#define AXI1_PORT_URGENCY_EN_SHIFT		24

#define RK3576_SYS_MMU_CTRL			0x020U
#define RKMMU_V2_EN_SHIFT			0
#define RKMMU_V2_SEL_AXI_SHIFT			1

#define RK3568_SYS_AXI_LUT_CTRL			0x024U
#define LUT_DMA_EN_SHIFT			0
#define DSP_VS_T_SEL_SHIFT			16

#define RK3568_DSP_IF_EN			0x028U
#define RGB_EN_SHIFT				0
#define RK3588_DP0_EN_SHIFT			0
#define RK3588_DP1_EN_SHIFT			1
#define RK3588_RGB_EN_SHIFT			8
#define HDMI0_EN_SHIFT				1
#define EDP0_EN_SHIFT				3
#define RK3588_EDP0_EN_SHIFT			2
#define RK3588_HDMI0_EN_SHIFT			3
#define MIPI0_EN_SHIFT				4
#define RK3588_EDP1_EN_SHIFT			4
#define RK3588_HDMI1_EN_SHIFT			5
#define RK3588_MIPI0_EN_SHIFT			6
#define MIPI1_EN_SHIFT				20
#define RK3588_MIPI1_EN_SHIFT			7
#define LVDS0_EN_SHIFT				5
#define LVDS1_EN_SHIFT				24
#define BT1120_EN_SHIFT				6
#define BT656_EN_SHIFT				7
#define IF_MUX_MASK				3
#define RGB_MUX_SHIFT				8
#define HDMI0_MUX_SHIFT				10
#define RK3588_DP0_MUX_SHIFT			12
#define RK3588_DP1_MUX_SHIFT			14
#define EDP0_MUX_SHIFT				14
#define RK3588_HDMI_EDP0_MUX_SHIFT		16
#define RK3588_HDMI_EDP1_MUX_SHIFT		18
#define MIPI0_MUX_SHIFT				16
#define RK3588_MIPI0_MUX_SHIFT			20
#define MIPI1_MUX_SHIFT				21
#define LVDS0_MUX_SHIFT				18
#define LVDS1_MUX_SHIFT				25

#define RK3576_SYS_PORT_CTRL			0x028U
#define VP_INTR_MERGE_EN_SHIFT			14
#define RK3576_DSP_VS_T_SEL_SHIFT		4
#define INTERLACE_FRM_REG_DONE_MASK		0x7
#define INTERLACE_FRM_REG_DONE_SHIFT		0

#define RK3568_DSP_IF_CTRL			0x02cU
#define LVDS_DUAL_EN_SHIFT			0
#define RK3588_BT656_UV_SWAP_SHIFT		0
#define LVDS_DUAL_LEFT_RIGHT_EN_SHIFT		1
#define RK3588_BT656_YC_SWAP_SHIFT		1
#define LVDS_DUAL_SWAP_EN_SHIFT			2
#define BT656_UV_SWAP				4
#define RK3588_BT1120_UV_SWAP_SHIFT		4
#define BT656_YC_SWAP				5
#define RK3588_BT1120_YC_SWAP_SHIFT		5
#define BT656_DCLK_POL				6
#define RK3588_HDMI_DUAL_EN_SHIFT		8
#define RK3588_EDP_DUAL_EN_SHIFT		8
#define RK3588_DP_DUAL_EN_SHIFT			9
#define RK3568_MIPI_DUAL_EN_SHIFT		10
#define RK3588_MIPI_DSI0_MODE_SEL_SHIFT		11
#define RK3588_MIPI_DSI1_MODE_SEL_SHIFT		12

#define RK3568_DSP_IF_POL			0x030U
#define IF_CTRL_REG_DONE_IMD_SHIFT		28
#define IF_CRTL_MIPI_DCLK_POL_SHIT		19
#define IF_CTRL_MIPI_PIN_POL_MASK		0x7
#define IF_CTRL_MIPI_PIN_POL_SHIFT		16
#define IF_CRTL_EDP_DCLK_POL_SHIT		15
#define IF_CTRL_EDP_PIN_POL_MASK		0x7
#define IF_CTRL_EDP_PIN_POL_SHIFT		12
#define IF_CRTL_HDMI_DCLK_POL_SHIT		7
#define IF_CRTL_HDMI_PIN_POL_MASK		0x7
#define IF_CRTL_HDMI_PIN_POL_SHIT		4
#define IF_CTRL_RGB_LVDS_DCLK_POL_SHIFT		3
#define IF_CTRL_RGB_LVDS_PIN_POL_MASK		0x7
#define IF_CTRL_RGB_LVDS_PIN_POL_SHIFT		0

#define RK3562_MIPI_DCLK_POL_SHIFT		15
#define RK3562_MIPI_PIN_POL_SHIFT		12
#define RK3562_IF_PIN_POL_MASK			0x7

#define RK3588_DP0_PIN_POL_SHIFT		8
#define RK3588_DP1_PIN_POL_SHIFT		12
#define RK3588_IF_PIN_POL_MASK			0x7

#define HDMI_EDP0_DCLK_DIV_SHIFT		16
#define HDMI_EDP0_PIXCLK_DIV_SHIFT		18
#define HDMI_EDP1_DCLK_DIV_SHIFT		20
#define HDMI_EDP1_PIXCLK_DIV_SHIFT		22
#define MIPI0_PIXCLK_DIV_SHIFT			24
#define MIPI1_PIXCLK_DIV_SHIFT			26

#define RK3576_SYS_CLUSTER_PD_CTRL		0x030U
#define RK3576_CLUSTER_PD_EN_SHIFT		0

#define RK3588_SYS_PD_CTRL			0x034U
#define RK3588_CLUSTER0_PD_EN_SHIFT		0U
#define RK3588_CLUSTER1_PD_EN_SHIFT		1U
#define RK3588_CLUSTER2_PD_EN_SHIFT		2U
#define RK3588_CLUSTER3_PD_EN_SHIFT		3U
#define RK3588_DSC_8K_PD_EN_SHIFT		5U
#define RK3588_DSC_4K_PD_EN_SHIFT		6U
#define RK3588_ESMART_PD_EN_SHIFT		7U

#define RK3576_SYS_ESMART_PD_CTRL		0x034U
#define RK3576_ESMART_PD_EN_SHIFT		0
#define RK3576_ESMART_LB_MODE_SEL_SHIFT		6
#define RK3576_ESMART_LB_MODE_SEL_MASK		0x3

#define RK3568_SYS_OTP_WIN_EN			0x50U
#define OTP_WIN_EN_SHIFT			0
#define RK3568_SYS_LUT_PORT_SEL			0x58U
#define GAMMA_PORT_SEL_MASK			0x3
#define GAMMA_PORT_SEL_SHIFT			0
#define GAMMA_AHB_WRITE_SEL_MASK		0x3
#define GAMMA_AHB_WRITE_SEL_SHIFT		12
#define PORT_MERGE_EN_SHIFT			16
#define ESMART_LB_MODE_SEL_MASK			0x3
#define ESMART_LB_MODE_SEL_SHIFT		26

#define RK3568_VP0_LINE_FLAG			0x70U
#define RK3568_VP1_LINE_FLAG			0x74U
#define RK3568_VP2_LINE_FLAG			0x78U
#define RK3568_SYS0_INT_EN			0x80U
#define RK3568_SYS0_INT_CLR			0x84U
#define RK3568_SYS0_INT_STATUS			0x88U
#define RK3568_SYS1_INT_EN			0x90U
#define RK3568_SYS1_INT_CLR			0x94U
#define RK3568_SYS1_INT_STATUS			0x98U
#define RK3568_VP0_INT_EN			0xA0U
#define RK3568_VP0_INT_CLR			0xA4U
#define RK3568_VP0_INT_STATUS			0xA8U
#define RK3568_VP1_INT_EN			0xB0U
#define RK3568_VP1_INT_CLR			0xB4U
#define RK3568_VP1_INT_STATUS			0xB8U
#define RK3568_VP2_INT_EN			0xC0U
#define RK3568_VP2_INT_CLR			0xC4U
#define RK3568_VP2_INT_STATUS			0xC8U
#define RK3568_VP2_INT_RAW_STATUS		0xCCU
#define RK3588_VP3_INT_EN			0xD0U
#define RK3588_VP3_INT_CLR			0xD4U
#define RK3588_VP3_INT_STATUS			0xD8U
#define RK3576_WB_CTRL				0x100U
#define RK3576_WB_XSCAL_FACTOR			0x104U
#define RK3576_WB_YRGB_MST			0x108U
#define RK3576_WB_CBR_MST			0x10CU
#define RK3576_WB_VIR_STRIDE			0x110U
#define RK3576_WB_TIMEOUT_CTRL			0x114U
#define RK3576_MIPI0_IF_CTRL			0x180U
#define RK3576_IF_OUT_EN_SHIFT			0
#define RK3576_IF_CLK_OUT_EN_SHIFT		1
#define RK3576_IF_PORT_SEL_SHIFT		2
#define RK3576_IF_PORT_SEL_MASK			0x3
#define RK3576_IF_PIN_POL_SHIFT			4
#define RK3576_IF_PIN_POL_MASK			0x7
#define RK3576_IF_SPLIT_EN_SHIFT		8
#define RK3576_IF_DATA1_SEL_SHIFT		9
#define RK3576_MIPI_CMD_MODE_SHIFT		11
#define RK3576_IF_DCLK_SEL_SHIFT		21
#define RK3576_IF_DCLK_SEL_MASK			0x1
#define RK3576_IF_PIX_CLK_SEL_SHIFT		20
#define RK3576_IF_PIX_CLK_SEL_MASK		0x1
#define RK3576_IF_REGDONE_IMD_EN_SHIFT		31
#define RK3576_HDMI0_IF_CTRL			0x184U
#define RK3576_EDP0_IF_CTRL			0x188U
#define RK3576_DP0_IF_CTRL			0x18CU
#define RK3576_RGB_IF_CTRL			0x194U
#define RK3576_BT656_OUT_EN_SHIFT		12
#define RK3576_BT656_UV_SWAP_SHIFT		13
#define RK3576_BT656_YC_SWAP_SHIFT		14
#define RK3576_BT1120_OUT_EN_SHIFT		16
#define RK3576_BT1120_UV_SWAP_SHIFT		17
#define RK3576_BT1120_YC_SWAP_SHIFT		18
#define RK3576_DP1_IF_CTRL			0x1A4U
#define RK3576_DP2_IF_CTRL			0x1B0U

#define RK3588_SYS_VAR_FREQ_CTRL		0x038U
#define RK3588_VP0_LINE_FLAG_OR_EN_SHIFT	20
#define RK3588_VP0_DSP_HOLD_OR_EN_SHIFT		24
#define RK3588_VP0_ALMOST_FULL_OR_EN_SHIFT	28

#define RK3568_SYS_STATUS0			0x60U
#define RK3588_CLUSTER0_PD_STATUS_SHIFT		8U
#define RK3588_CLUSTER1_PD_STATUS_SHIFT		9U
#define RK3588_CLUSTER2_PD_STATUS_SHIFT		10U
#define RK3588_CLUSTER3_PD_STATUS_SHIFT		11U
#define RK3588_DSC_8K_PD_STATUS_SHIFT		13U
#define RK3588_DSC_4K_PD_STATUS_SHIFT		14U
#define RK3588_ESMART_PD_STATUS_SHIFT		15U

#define RK3568_SYS_CTRL_LINE_FLAG0		0x70U
#define LINE_FLAG_NUM_MASK			0x1fff
#define RK3568_DSP_LINE_FLAG_NUM0_SHIFT		0
#define RK3568_DSP_LINE_FLAG_NUM1_SHIFT		16

/* DSC CTRL registers definition */
#define RK3588_DSC_8K_SYS_CTRL			0x200U
#define DSC_PORT_SEL_MASK			0x3
#define DSC_PORT_SEL_SHIFT			0
#define DSC_MAN_MODE_MASK			0x1
#define DSC_MAN_MODE_SHIFT			2
#define DSC_INTERFACE_MODE_MASK			0x3
#define DSC_INTERFACE_MODE_SHIFT		4
#define DSC_PIXEL_NUM_MASK			0x3
#define DSC_PIXEL_NUM_SHIFT			6
#define DSC_PXL_CLK_DIV_MASK			0x1
#define DSC_PXL_CLK_DIV_SHIFT			8
#define DSC_CDS_CLK_DIV_MASK			0x3
#define DSC_CDS_CLK_DIV_SHIFT			12
#define DSC_TXP_CLK_DIV_MASK			0x3
#define DSC_TXP_CLK_DIV_SHIFT			14
#define DSC_INIT_DLY_MODE_MASK			0x1
#define DSC_INIT_DLY_MODE_SHIFT			16
#define DSC_SCAN_EN_SHIFT			17
#define DSC_HALT_EN_SHIFT			18

#define RK3588_DSC_8K_RST			0x204U
#define RST_DEASSERT_MASK			0x1
#define RST_DEASSERT_SHIFT			0

#define RK3588_DSC_8K_CFG_DONE			0x208U
#define DSC_CFG_DONE_SHIFT			0

#define RK3588_DSC_8K_INIT_DLY			0x20CU
#define DSC_INIT_DLY_NUM_MASK			0xffff
#define DSC_INIT_DLY_NUM_SHIFT			0
#define SCAN_TIMING_PARA_IMD_EN_SHIFT		16

#define RK3588_DSC_8K_HTOTAL_HS_END		0x210U
#define DSC_HTOTAL_PW_MASK			0xffffffffU
#define DSC_HTOTAL_PW_SHIFT			0

#define RK3588_DSC_8K_HACT_ST_END		0x214U
#define DSC_HACT_ST_END_MASK			0xffffffffU
#define DSC_HACT_ST_END_SHIFT			0

#define RK3588_DSC_8K_VTOTAL_VS_END		0x218U
#define DSC_VTOTAL_PW_MASK			0xffffffffU
#define DSC_VTOTAL_PW_SHIFT			0

#define RK3588_DSC_8K_VACT_ST_END		0x21CU
#define DSC_VACT_ST_END_MASK			0xffffffffU
#define DSC_VACT_ST_END_SHIFT			0

#define RK3588_DSC_8K_STATUS			0x220U

/* Overlay registers definition    */
#define RK3528_OVL_SYS				0x500U
#define RK3528_OVL_SYS_PORT_SEL			0x504U
#define RK3528_OVL_SYS_GATING_EN		0x508U
#define RK3528_OVL_SYS_CLUSTER0_CTRL		0x510U
#define RK3528_OVL_SYS_ESMART0_CTRL		0x520U
#define ESMART_DLY_NUM_MASK			0xff
#define ESMART_DLY_NUM_SHIFT			0
#define RK3528_OVL_SYS_ESMART1_CTRL		0x524U
#define RK3528_OVL_SYS_ESMART2_CTRL		0x528U
#define RK3528_OVL_SYS_ESMART3_CTRL		0x52CU
#define RK3528_CLUSTER0_MIX_SRC_COLOR_CTRL	0x530U
#define RK3528_CLUSTER0_MIX_DST_COLOR_CTRL	0x534U
#define RK3528_CLUSTER0_MIX_SRC_ALPHA_CTRL	0x538U
#define RK3528_CLUSTER0_MIX_DST_ALPHA_CTRL	0x53cU
#define RK3576_CLUSTER1_MIX_SRC_COLOR_CTRL	0x540U
#define RK3576_CLUSTER1_MIX_DST_COLOR_CTRL	0x544U
#define RK3576_CLUSTER1_MIX_SRC_ALPHA_CTRL	0x548U
#define RK3576_CLUSTER1_MIX_DST_ALPHA_CTRL	0x54cU

#define RK3528_OVL_PORT0_CTRL			0x600U
#define RK3568_OVL_CTRL				0x600U
#define OVL_MODE_SEL_MASK			0x1
#define OVL_MODE_SEL_SHIFT			0
#define OVL_PORT_MUX_REG_DONE_IMD_SHIFT		28
#define RK3528_OVL_PORT0_LAYER_SEL		0x604U
#define RK3568_OVL_LAYER_SEL			0x604U
#define LAYER_SEL_MASK				0xf

#define RK3568_OVL_PORT_SEL			0x608U
#define PORT_MUX_MASK				0xf
#define PORT_MUX_SHIFT				0U
#define LAYER_SEL_PORT_MASK			0x3
#define LAYER_SEL_PORT_SHIFT			16U

#define RK3568_CLUSTER0_MIX_SRC_COLOR_CTRL	0x610U
#define RK3568_CLUSTER0_MIX_DST_COLOR_CTRL	0x614U
#define RK3568_CLUSTER0_MIX_SRC_ALPHA_CTRL	0x618U
#define RK3568_CLUSTER0_MIX_DST_ALPHA_CTRL	0x61CU
#define RK3528_OVL_PORT0_MIX0_SRC_COLOR_CTRL	0x620U
#define RK3528_OVL_PORT0_MIX0_DST_COLOR_CTRL	0x624U
#define RK3528_OVL_PORT0_MIX0_SRC_ALPHA_CTRL	0x628U
#define RK3528_OVL_PORT0_MIX0_DST_ALPHA_CTRL	0x62CU
#define RK3528_OVL_PORT0_MIX1_SRC_COLOR_CTRL	0x630U
#define RK3528_OVL_PORT0_MIX1_DST_COLOR_CTRL	0x634U
#define RK3528_OVL_PORT0_MIX1_SRC_ALPHA_CTRL	0x638U
#define RK3528_OVL_PORT0_MIX1_DST_ALPHA_CTRL	0x63CU
#define RK3528_OVL_PORT0_MIX2_SRC_COLOR_CTRL	0x640U
#define RK3528_OVL_PORT0_MIX2_DST_COLOR_CTRL	0x644U
#define RK3528_OVL_PORT0_MIX2_SRC_ALPHA_CTRL	0x648U
#define RK3528_OVL_PORT0_MIX2_DST_ALPHA_CTRL	0x64CU
#define RK3568_MIX0_SRC_COLOR_CTRL		0x650U
#define RK3568_MIX0_DST_COLOR_CTRL		0x654U
#define RK3568_MIX0_SRC_ALPHA_CTRL		0x658U
#define RK3568_MIX0_DST_ALPHA_CTRL		0x65CU
#define RK3576_EXTRA_SRC_COLOR_CTRL		0x650U
#define RK3576_EXTRA_DST_COLOR_CTRL		0x654U
#define RK3576_EXTRA_SRC_ALPHA_CTRL		0x658U
#define RK3576_EXTRA_DST_ALPHA_CTRL		0x65CU
#define RK3528_HDR_SRC_COLOR_CTRL		0x660U
#define RK3528_HDR_DST_COLOR_CTRL		0x664U
#define RK3528_HDR_SRC_ALPHA_CTRL		0x668U
#define RK3528_HDR_DST_ALPHA_CTRL		0x66CU
#define RK3528_OVL_PORT0_BG_MIX_CTRL		0x670U
#define RK3568_HDR0_SRC_COLOR_CTRL		0x6C0U
#define RK3568_HDR0_DST_COLOR_CTRL		0x6C4U
#define RK3568_HDR0_SRC_ALPHA_CTRL		0x6C8U
#define RK3568_HDR0_DST_ALPHA_CTRL		0x6CCU
#define RK3568_VP0_BG_MIX_CTRL			0x6E0U
#define BG_MIX_CTRL_MASK			0xff
#define BG_MIX_CTRL_SHIFT			24
#define RK3568_VP1_BG_MIX_CTRL			0x6E4U
#define RK3568_VP2_BG_MIX_CTRL			0x6E8U
#define RK3568_CLUSTER_DLY_NUM			0x6F0U
#define RK3568_SMART_DLY_NUM			0x6F8U

#define RK3528_OVL_PORT1_CTRL			0x700U
#define RK3528_OVL_PORT1_LAYER_SEL		0x704U
#define RK3528_OVL_PORT1_MIX0_SRC_COLOR_CTRL	0x720U
#define RK3528_OVL_PORT1_MIX0_DST_COLOR_CTRL	0x724U
#define RK3528_OVL_PORT1_MIX0_SRC_ALPHA_CTRL	0x728U
#define RK3528_OVL_PORT1_MIX0_DST_ALPHA_CTRL	0x72CU
#define RK3528_OVL_PORT1_MIX1_SRC_COLOR_CTRL	0x730U
#define RK3528_OVL_PORT1_MIX1_DST_COLOR_CTRL	0x734U
#define RK3528_OVL_PORT1_MIX1_SRC_ALPHA_CTRL	0x738U
#define RK3528_OVL_PORT1_MIX1_DST_ALPHA_CTRL	0x73CU
#define RK3528_OVL_PORT1_MIX2_SRC_COLOR_CTRL	0x740U
#define RK3528_OVL_PORT1_MIX2_DST_COLOR_CTRL	0x744U
#define RK3528_OVL_PORT1_MIX2_SRC_ALPHA_CTRL	0x748U
#define RK3528_OVL_PORT1_MIX2_DST_ALPHA_CTRL	0x74CU
#define RK3528_OVL_PORT1_BG_MIX_CTRL		0x770U
#define RK3576_OVL_PORT2_CTRL			0x800U
#define RK3576_OVL_PORT2_LAYER_SEL		0x804U
#define RK3576_OVL_PORT2_MIX0_SRC_COLOR_CTRL	0x820U
#define RK3576_OVL_PORT2_MIX0_DST_COLOR_CTRL	0x824U
#define RK3576_OVL_PORT2_MIX0_SRC_ALPHA_CTRL	0x828U
#define RK3576_OVL_PORT2_MIX0_DST_ALPHA_CTRL	0x82CU
#define RK3576_OVL_PORT2_BG_MIX_CTRL		0x870U

/* Video Port registers definition */
#define RK3568_VP0_DSP_CTRL			0xC00U
#define OUT_MODE_MASK				0xf
#define OUT_MODE_SHIFT				0
#define DATA_SWAP_MASK				0x1f
#define DATA_SWAP_SHIFT				8
#define DSP_BG_SWAP				0x1
#define DSP_RB_SWAP				0x2
#define DSP_RG_SWAP				0x4
#define DSP_DELTA_SWAP				0x8
#define CORE_DCLK_DIV_EN_SHIFT			4
#define P2I_EN_SHIFT				5
#define DSP_FILED_POL				6
#define INTERLACE_EN_SHIFT			7
#define DSP_X_MIR_EN_SHIFT			13
#define POST_DSP_OUT_R2Y_SHIFT			15
#define PRE_DITHER_DOWN_EN_SHIFT		16
#define DITHER_DOWN_EN_SHIFT			17
#define DITHER_DOWN_SEL_SHIFT			18
#define DITHER_DOWN_SEL_MASK			0x3
#define DITHER_DOWN_MODE_SHIFT			20
#define GAMMA_UPDATE_EN_SHIFT			22
#define DSP_LUT_EN_SHIFT			28

#define STANDBY_EN_SHIFT			31

#define RK3568_VP0_MIPI_CTRL			0xC04U
#define DCLK_DIV2_SHIFT				4
#define DCLK_DIV2_MASK				0x3
#define MIPI_DUAL_EN_SHIFT			20
#define MIPI_DUAL_SWAP_EN_SHIFT			21
#define EDPI_TE_EN				28
#define EDPI_WMS_HOLD_EN			30
#define EDPI_WMS_FS				31


#define RK3568_VP0_COLOR_BAR_CTRL		0xC08U
#define POST_URGENCY_EN_SHIFT			8
#define POST_URGENCY_THL_SHIFT			16
#define POST_URGENCY_THL_MASK			0xf
#define POST_URGENCY_THH_SHIFT			20
#define POST_URGENCY_THH_MASK			0xf

#define RK3568_VP0_DCLK_SEL			0xC0CU
#define RK3576_DCLK_CORE_SEL_SHIFT		0
#define RK3576_DCLK_OUT_SEL_SHIFT		2

#define RK3568_VP0_3D_LUT_CTRL			0xC10U
#define VP0_3D_LUT_EN_SHIFT				0
#define VP0_3D_LUT_UPDATE_SHIFT			2

#define RK3588_VP0_CLK_CTRL			0xC0CU
#define DCLK_CORE_DIV_SHIFT			0
#define DCLK_OUT_DIV_SHIFT			2

#define RK3568_VP0_3D_LUT_MST			0xC20U

#define RK3568_VP0_DSP_BG			0xC2CU
#define RK3568_VP0_PRE_SCAN_HTIMING		0xC30U
#define RK3568_VP0_POST_DSP_HACT_INFO		0xC34U
#define RK3568_VP0_POST_DSP_VACT_INFO		0xC38U
#define RK3568_VP0_POST_SCL_FACTOR_YRGB		0xC3CU
#define RK3568_VP0_POST_SCL_CTRL		0xC40U
#define RK3568_VP0_POST_SCALE_MASK		0x3
#define RK3568_VP0_POST_SCALE_SHIFT		0
#define RK3568_VP0_POST_DSP_VACT_INFO_F1	0xC44U
#define RK3568_VP0_DSP_HTOTAL_HS_END		0xC48U
#define RK3568_VP0_DSP_HACT_ST_END		0xC4CU
#define RK3568_VP0_DSP_VTOTAL_VS_END		0xC50U
#define RK3568_VP0_DSP_VACT_ST_END		0xC54U
#define RK3568_VP0_DSP_VS_ST_END_F1		0xC58U
#define RK3568_VP0_DSP_VACT_ST_END_F1		0xC5CU

#define RK3568_VP0_BCSH_CTRL			0xC60U
#define BCSH_CTRL_Y2R_SHIFT			0
#define BCSH_CTRL_Y2R_MASK			0x1
#define BCSH_CTRL_Y2R_CSC_MODE_SHIFT		2
#define BCSH_CTRL_Y2R_CSC_MODE_MASK		0x3
#define BCSH_CTRL_R2Y_SHIFT			4
#define BCSH_CTRL_R2Y_MASK			0x1
#define BCSH_CTRL_R2Y_CSC_MODE_SHIFT		6
#define BCSH_CTRL_R2Y_CSC_MODE_MASK		0x3

#define RK3568_VP0_BCSH_BCS			0xC64U
#define BCSH_BRIGHTNESS_SHIFT			0
#define BCSH_BRIGHTNESS_MASK			0xFF
#define BCSH_CONTRAST_SHIFT			8
#define BCSH_CONTRAST_MASK			0x1FF
#define BCSH_SATURATION_SHIFT			20
#define BCSH_SATURATION_MASK			0x3FF
#define BCSH_OUT_MODE_SHIFT			30
#define BCSH_OUT_MODE_MASK			0x3

#define RK3568_VP0_BCSH_H			0xC68U
#define BCSH_SIN_HUE_SHIFT			0
#define BCSH_SIN_HUE_MASK			0x1FF
#define BCSH_COS_HUE_SHIFT			16
#define BCSH_COS_HUE_MASK			0x1FF

#define RK3568_VP0_BCSH_COLOR			0xC6CU
#define BCSH_EN_SHIFT				31
#define BCSH_EN_MASK				1

#define RK3576_VP0_POST_DITHER_FRC_0		0xCA0U
#define RK3576_VP0_POST_DITHER_FRC_1		0xCA4U
#define RK3576_VP0_POST_DITHER_FRC_2		0xCA8U

#define RK3528_VP0_ACM_CTRL			0xCD0U
#define POST_CSC_COE00_MASK			0xFFFF
#define POST_CSC_COE00_SHIFT			16
#define POST_R2Y_MODE_MASK			0x7
#define POST_R2Y_MODE_SHIFT			8
#define POST_CSC_MODE_MASK			0x7
#define POST_CSC_MODE_SHIFT			3
#define POST_R2Y_EN_MASK			0x1
#define POST_R2Y_EN_SHIFT			2
#define POST_CSC_EN_MASK			0x1
#define POST_CSC_EN_SHIFT			1
#define POST_ACM_BYPASS_EN_MASK			0x1
#define POST_ACM_BYPASS_EN_SHIFT		0
#define RK3528_VP0_CSC_COE01_02			0xCD4U
#define RK3528_VP0_CSC_COE10_11			0xCD8U
#define RK3528_VP0_CSC_COE12_20			0xCDCU
#define RK3528_VP0_CSC_COE21_22			0xCE0U
#define RK3528_VP0_CSC_OFFSET0			0xCE4U
#define RK3528_VP0_CSC_OFFSET1			0xCE8U
#define RK3528_VP0_CSC_OFFSET2			0xCECU

#define RK3562_VP0_MCU_CTRL			0xCF8U
#define MCU_TYPE_SHIFT				31
#define MCU_BYPASS_SHIFT			30
#define MCU_RS_SHIFT				29
#define MCU_FRAME_ST_SHIFT			28
#define MCU_HOLD_MODE_SHIFT			27
#define MCU_CLK_SEL_SHIFT			26
#define MCU_CLK_SEL_MASK			0x1
#define MCU_RW_PEND_SHIFT			20
#define MCU_RW_PEND_MASK			0x3F
#define MCU_RW_PST_SHIFT			16
#define MCU_RW_PST_MASK				0xF
#define MCU_CS_PEND_SHIFT			10
#define MCU_CS_PEND_MASK			0x3F
#define MCU_CS_PST_SHIFT			6
#define MCU_CS_PST_MASK				0xF
#define MCU_PIX_TOTAL_SHIFT			0
#define MCU_PIX_TOTAL_MASK			0x3F

#define RK3562_VP0_MCU_RW_BYPASS_PORT		0xCFCU
#define MCU_WRITE_DATA_BYPASS_SHIFT		0
#define MCU_WRITE_DATA_BYPASS_MASK		0xFFFFFFFFU

#define RK3568_VP1_DSP_CTRL			0xD00U
#define RK3568_VP1_MIPI_CTRL			0xD04U
#define RK3568_VP1_COLOR_BAR_CTRL		0xD08U
#define RK3568_VP1_PRE_SCAN_HTIMING		0xD30U
#define RK3568_VP1_POST_DSP_HACT_INFO		0xD34U
#define RK3568_VP1_POST_DSP_VACT_INFO		0xD38U
#define RK3568_VP1_POST_SCL_FACTOR_YRGB		0xD3CU
#define RK3568_VP1_POST_SCL_CTRL		0xD40U
#define RK3568_VP1_DSP_HACT_INFO		0xD34U
#define RK3568_VP1_DSP_VACT_INFO		0xD38U
#define RK3568_VP1_POST_DSP_VACT_INFO_F1	0xD44U
#define RK3568_VP1_DSP_HTOTAL_HS_END		0xD48U
#define RK3568_VP1_DSP_HACT_ST_END		0xD4CU
#define RK3568_VP1_DSP_VTOTAL_VS_END		0xD50U
#define RK3568_VP1_DSP_VACT_ST_END		0xD54U
#define RK3568_VP1_DSP_VS_ST_END_F1		0xD58U
#define RK3568_VP1_DSP_VACT_ST_END_F1		0xD5CU

#define RK3568_VP2_DSP_CTRL			0xE00U
#define RK3568_VP2_MIPI_CTRL			0xE04U
#define RK3568_VP2_COLOR_BAR_CTRL		0xE08U
#define RK3568_VP2_PRE_SCAN_HTIMING		0xE30U
#define RK3568_VP2_POST_DSP_HACT_INFO		0xE34U
#define RK3568_VP2_POST_DSP_VACT_INFO		0xE38U
#define RK3568_VP2_POST_SCL_FACTOR_YRGB		0xE3CU
#define RK3568_VP2_POST_SCL_CTRL		0xE40U
#define RK3568_VP2_DSP_HACT_INFO		0xE34U
#define RK3568_VP2_DSP_VACT_INFO		0xE38U
#define RK3568_VP2_POST_DSP_VACT_INFO_F1	0xE44U
#define RK3568_VP2_DSP_HTOTAL_HS_END		0xE48U
#define RK3568_VP2_DSP_HACT_ST_END		0xE4CU
#define RK3568_VP2_DSP_VTOTAL_VS_END		0xE50U
#define RK3568_VP2_DSP_VACT_ST_END		0xE54U
#define RK3568_VP2_DSP_VS_ST_END_F1		0xE58U
#define RK3568_VP2_DSP_VACT_ST_END_F1		0xE5CU
#define RK3568_VP2_BCSH_CTRL			0xE60U
#define RK3568_VP2_BCSH_BCS			0xE64U
#define RK3568_VP2_BCSH_H			0xE68U
#define RK3568_VP2_BCSH_COLOR_BAR		0xE6CU
#define RK3576_VP2_MCU_CTRL			0xEF8U
#define RK3576_VP2_MCU_RW_BYPASS_PORT		0xEFCU

/* Cluster0 register definition */
#define RK3568_CLUSTER0_WIN0_CTRL0		0x1000U
#define CLUSTER_YUV2RGB_EN_SHIFT		8
#define CLUSTER_RGB2YUV_EN_SHIFT		9
#define CLUSTER_CSC_MODE_SHIFT			10
#define CLUSTER_DITHER_UP_EN_SHIFT		18
#define RK3568_CLUSTER0_WIN0_CTRL1		0x1004U
#define RK3568_CLUSTER_YRGB_XSCL_MODE_SHIFT	12
#define RK3568_CLUSTER_YRGB_YSCL_MODE_SHIFT	14
#define RK3528_CLUSTER_YRGB_YSCL_MODE_SHIFT	14
#define AVG2_MASK				0x1
#define CLUSTER_AVG2_SHIFT			18
#define AVG4_MASK				0x1
#define CLUSTER_AVG4_SHIFT			19
#define RK3528_CLUSTER_YRGB_XSCL_MODE_SHIFT	22
#define CLUSTER_XGT_EN_SHIFT			24
#define XGT_MODE_MASK				0x3
#define CLUSTER_XGT_MODE_SHIFT			25
#define CLUSTER_XAVG_EN_SHIFT			27
#define CLUSTER_YRGB_GT2_SHIFT			28
#define CLUSTER_YRGB_GT4_SHIFT			29
#define RK3568_CLUSTER0_WIN0_CTRL2		0x1008U
#define CLUSTER_AXI_YRGB_ID_MASK		0x1f
#define CLUSTER_AXI_YRGB_ID_SHIFT		0
#define CLUSTER_AXI_UV_ID_MASK			0x1f
#define CLUSTER_AXI_UV_ID_SHIFT			5

#define RK3568_CLUSTER0_WIN0_YRGB_MST		0x1010U
#define RK3568_CLUSTER0_WIN0_CBR_MST		0x1014U
#define RK3568_CLUSTER0_WIN0_VIR		0x1018U
#define RK3568_CLUSTER0_WIN0_ACT_INFO		0x1020U
#define RK3568_CLUSTER0_WIN0_DSP_INFO		0x1024U
#define RK3568_CLUSTER0_WIN0_DSP_ST		0x1028U
#define RK3568_CLUSTER0_WIN0_SCL_FACTOR_YRGB	0x1030U
#define RK3576_CLUSTER0_WIN0_ZME_CTRL		0x1040U
#define WIN0_ZME_DERING_EN_SHIFT		3
#define WIN0_ZME_GATING_EN_SHIFT		31
#define RK3576_CLUSTER0_WIN0_ZME_DERING_PARA	0x1044U
#define RK3568_CLUSTER0_WIN0_AFBCD_ROTATE_MODE	0x1054U
#define RK3568_CLUSTER0_WIN0_AFBCD_HDR_PTR	0x1058U
#define RK3568_CLUSTER0_WIN0_AFBCD_VIR_WIDTH	0x105CU
#define RK3568_CLUSTER0_WIN0_AFBCD_PIC_SIZE	0x1060U
#define RK3568_CLUSTER0_WIN0_AFBCD_PIC_OFFSET	0x1064U
#define RK3568_CLUSTER0_WIN0_AFBCD_DSP_OFFSET	0x1068U
#define RK3568_CLUSTER0_WIN0_AFBCD_CTRL		0x106CU
#define CLUSTER_AFBCD_HALF_BLOCK_SHIFT		7
#define RK3576_CLUSTER0_WIN0_PLD_PTR_OFFSET	0x1078U
#define RK3576_CLUSTER0_WIN0_PLD_PTR_RANGE	0x107CU

#define RK3568_CLUSTER0_WIN1_CTRL0		0x1080U
#define RK3568_CLUSTER0_WIN1_CTRL1		0x1084U
#define RK3568_CLUSTER0_WIN1_YRGB_MST		0x1090U
#define RK3568_CLUSTER0_WIN1_CBR_MST		0x1094U
#define RK3568_CLUSTER0_WIN1_VIR		0x1098U
#define RK3568_CLUSTER0_WIN1_ACT_INFO		0x10A0U
#define RK3568_CLUSTER0_WIN1_DSP_INFO		0x10A4U
#define RK3568_CLUSTER0_WIN1_DSP_ST		0x10A8U
#define RK3568_CLUSTER0_WIN1_SCL_FACTOR_YRGB	0x10B0U
#define RK3568_CLUSTER0_WIN1_AFBCD_ROTATE_MODE	0x10D4U
#define RK3568_CLUSTER0_WIN1_AFBCD_HDR_PTR	0x10D8U
#define RK3568_CLUSTER0_WIN1_AFBCD_VIR_WIDTH	0x10DCU
#define RK3568_CLUSTER0_WIN1_AFBCD_PIC_SIZE	0x10E0U
#define RK3568_CLUSTER0_WIN1_AFBCD_PIC_OFFSET	0x10E4U
#define RK3568_CLUSTER0_WIN1_AFBCD_DSP_OFFSET	0x10E8U
#define RK3568_CLUSTER0_WIN1_AFBCD_CTRL		0x10ECU
#define RK3576_CLUSTER0_WIN1_PLD_PTR_OFFSET	0x10F8U
#define RK3576_CLUSTER0_WIN1_PLD_PTR_RANGE	0x10FCU

#define RK3568_CLUSTER0_CTRL			0x1100U
#define CLUSTER_EN_SHIFT			0
#define CLUSTER_AXI_ID_MASK			0x1
#define CLUSTER_AXI_ID_SHIFT			13
#define RK3576_CLUSTER0_PORT_SEL		0x11F4U
#define CLUSTER_PORT_SEL_SHIFT			0
#define CLUSTER_PORT_SEL_MASK			0x3
#define RK3576_CLUSTER0_DLY_NUM			0x11F8U
#define CLUSTER_WIN0_DLY_NUM_SHIFT		0
#define CLUSTER_WIN0_DLY_NUM_MASK		0xff
#define CLUSTER_WIN1_DLY_NUM_SHIFT		0
#define CLUSTER_WIN1_DLY_NUM_MASK		0xff

#define RK3568_CLUSTER1_WIN0_CTRL0		0x1200U
#define RK3568_CLUSTER1_WIN0_CTRL1		0x1204U
#define RK3568_CLUSTER1_WIN0_YRGB_MST		0x1210U
#define RK3568_CLUSTER1_WIN0_CBR_MST		0x1214U
#define RK3568_CLUSTER1_WIN0_VIR		0x1218U
#define RK3568_CLUSTER1_WIN0_ACT_INFO		0x1220U
#define RK3568_CLUSTER1_WIN0_DSP_INFO		0x1224U
#define RK3568_CLUSTER1_WIN0_DSP_ST		0x1228U
#define RK3568_CLUSTER1_WIN0_SCL_FACTOR_YRGB	0x1230U
#define RK3576_CLUSTER1_WIN0_ZME_CTRL		0x1240U
#define RK3576_CLUSTER1_WIN0_ZME_DERING_PARA	0x1244U
#define RK3568_CLUSTER1_WIN0_AFBCD_ROTATE_MODE	0x1254U
#define RK3568_CLUSTER1_WIN0_AFBCD_HDR_PTR	0x1258U
#define RK3568_CLUSTER1_WIN0_AFBCD_VIR_WIDTH	0x125CU
#define RK3568_CLUSTER1_WIN0_AFBCD_PIC_SIZE	0x1260U
#define RK3568_CLUSTER1_WIN0_AFBCD_PIC_OFFSET	0x1264U
#define RK3568_CLUSTER1_WIN0_AFBCD_DSP_OFFSET	0x1268U
#define RK3568_CLUSTER1_WIN0_AFBCD_CTRL		0x126CU
#define RK3576_CLUSTER1_WIN0_PLD_PTR_OFFSET	0x1278U
#define RK3576_CLUSTER1_WIN0_PLD_PTR_RANGE	0x127CU

#define RK3568_CLUSTER1_WIN1_CTRL0		0x1280U
#define RK3568_CLUSTER1_WIN1_CTRL1		0x1284U
#define RK3568_CLUSTER1_WIN1_YRGB_MST		0x1290U
#define RK3568_CLUSTER1_WIN1_CBR_MST		0x1294U
#define RK3568_CLUSTER1_WIN1_VIR		0x1298U
#define RK3568_CLUSTER1_WIN1_ACT_INFO		0x12A0U
#define RK3568_CLUSTER1_WIN1_DSP_INFO		0x12A4U
#define RK3568_CLUSTER1_WIN1_DSP_ST		0x12A8U
#define RK3568_CLUSTER1_WIN1_SCL_FACTOR_YRGB	0x12B0U
#define RK3568_CLUSTER1_WIN1_AFBCD_ROTATE_MODE	0x12D4U
#define RK3568_CLUSTER1_WIN1_AFBCD_HDR_PTR	0x12D8U
#define RK3568_CLUSTER1_WIN1_AFBCD_VIR_WIDTH	0x12DCU
#define RK3568_CLUSTER1_WIN1_AFBCD_PIC_SIZE	0x12E0U
#define RK3568_CLUSTER1_WIN1_AFBCD_PIC_OFFSET	0x12E4U
#define RK3568_CLUSTER1_WIN1_AFBCD_DSP_OFFSET	0x12E8U
#define RK3568_CLUSTER1_WIN1_AFBCD_CTRL		0x12ECU
#define RK3576_CLUSTER1_WIN1_PLD_PTR_OFFSET	0x12F8U
#define RK3576_CLUSTER1_WIN1_PLD_PTR_RANGE	0x12FCU

#define RK3568_CLUSTER1_CTRL			0x1300U
#define RK3576_CLUSTER1_PORT_SEL		0x13F4U
#define RK3576_CLUSTER1_DLY_NUM			0x13F8U

/* Esmart register definition */
#define RK3568_ESMART0_CTRL0			0x1800U
#define RGB2YUV_EN_SHIFT			1
#define CSC_MODE_SHIFT				2
#define CSC_MODE_MASK				0x3
#define ESMART_LB_SELECT_SHIFT			12
#define ESMART_LB_SELECT_MASK			0x3

#define RK3568_ESMART0_CTRL1			0x1804U
#define ESMART_AXI_YRGB_ID_MASK			0x1f
#define ESMART_AXI_YRGB_ID_SHIFT		4
#define ESMART_AXI_UV_ID_MASK			0x1f
#define ESMART_AXI_UV_ID_SHIFT			12
#define YMIRROR_EN_SHIFT			31

#define RK3568_ESMART0_AXI_CTRL			0x1808U
#define ESMART_AXI_ID_MASK			0x1
#define ESMART_AXI_ID_SHIFT			1

#define RK3568_ESMART0_REGION0_CTRL		0x1810U
#define WIN_EN_SHIFT				0
#define WIN_FORMAT_MASK				0x1f
#define WIN_FORMAT_SHIFT			1
#define REGION0_DITHER_UP_EN_SHIFT		12
#define REGION0_RB_SWAP_SHIFT			14
#define ESMART_XAVG_EN_SHIFT			20
#define ESMART_XGT_EN_SHIFT			21
#define ESMART_XGT_MODE_SHIFT			22

#define RK3568_ESMART0_REGION0_YRGB_MST		0x1814U
#define RK3568_ESMART0_REGION0_CBR_MST		0x1818U
#define RK3568_ESMART0_REGION0_VIR		0x181CU
#define RK3568_ESMART0_REGION0_ACT_INFO		0x1820U
#define RK3568_ESMART0_REGION0_DSP_INFO		0x1824U
#define RK3568_ESMART0_REGION0_DSP_ST		0x1828U
#define RK3568_ESMART0_REGION0_SCL_CTRL		0x1830U
#define YRGB_XSCL_MODE_MASK			0x3
#define YRGB_XSCL_MODE_SHIFT			0
#define YRGB_XSCL_FILTER_MODE_MASK		0x3
#define YRGB_XSCL_FILTER_MODE_SHIFT		2
#define YRGB_YSCL_MODE_MASK			0x3
#define YRGB_YSCL_MODE_SHIFT			4
#define YRGB_YSCL_FILTER_MODE_MASK		0x3
#define YRGB_YSCL_FILTER_MODE_SHIFT		6

#define RK3568_ESMART0_REGION0_SCL_FACTOR_YRGB	0x1834U
#define RK3568_ESMART0_REGION0_SCL_FACTOR_CBR	0x1838U
#define RK3568_ESMART0_REGION0_SCL_OFFSET	0x183CU
#define RK3568_ESMART0_REGION1_CTRL		0x1840U
#define YRGB_GT2_MASK				0x1
#define YRGB_GT2_SHIFT				8
#define YRGB_GT4_MASK				0x1
#define YRGB_GT4_SHIFT				9

#define RK3568_ESMART0_REGION1_YRGB_MST		0x1844U
#define RK3568_ESMART0_REGION1_CBR_MST		0x1848U
#define RK3568_ESMART0_REGION1_VIR		0x184CU
#define RK3568_ESMART0_REGION1_ACT_INFO		0x1850U
#define RK3568_ESMART0_REGION1_DSP_INFO		0x1854U
#define RK3568_ESMART0_REGION1_DSP_ST		0x1858U
#define RK3568_ESMART0_REGION1_SCL_CTRL		0x1860U
#define RK3568_ESMART0_REGION1_SCL_FACTOR_YRGB	0x1864U
#define RK3568_ESMART0_REGION1_SCL_FACTOR_CBR	0x1868U
#define RK3568_ESMART0_REGION1_SCL_OFFSET	0x186CU
#define RK3568_ESMART0_REGION2_CTRL		0x1870U
#define RK3568_ESMART0_REGION2_YRGB_MST		0x1874U
#define RK3568_ESMART0_REGION2_CBR_MST		0x1878U
#define RK3568_ESMART0_REGION2_VIR		0x187CU
#define RK3568_ESMART0_REGION2_ACT_INFO		0x1880U
#define RK3568_ESMART0_REGION2_DSP_INFO		0x1884U
#define RK3568_ESMART0_REGION2_DSP_ST		0x1888U
#define RK3568_ESMART0_REGION2_SCL_CTRL		0x1890U
#define RK3568_ESMART0_REGION2_SCL_FACTOR_YRGB	0x1894U
#define RK3568_ESMART0_REGION2_SCL_FACTOR_CBR	0x1898U
#define RK3568_ESMART0_REGION2_SCL_OFFSET	0x189CU
#define RK3568_ESMART0_REGION3_CTRL		0x18A0U
#define RK3568_ESMART0_REGION3_YRGB_MST		0x18A4U
#define RK3568_ESMART0_REGION3_CBR_MST		0x18A8U
#define RK3568_ESMART0_REGION3_VIR		0x18ACU
#define RK3568_ESMART0_REGION3_ACT_INFO		0x18B0U
#define RK3568_ESMART0_REGION3_DSP_INFO		0x18B4U
#define RK3568_ESMART0_REGION3_DSP_ST		0x18B8U
#define RK3568_ESMART0_REGION3_SCL_CTRL		0x18C0U
#define RK3568_ESMART0_REGION3_SCL_FACTOR_YRGB	0x18C4U
#define RK3568_ESMART0_REGION3_SCL_FACTOR_CBR	0x18C8U
#define RK3568_ESMART0_REGION3_SCL_OFFSET	0x18CCU
#define RK3568_ESMART0_COLOR_KEY_CTRL		0x18D0U
#define RK3576_ESMART0_ALPHA_MAP		0x18D8U
#define RK3576_ESMART0_PORT_SEL			0x18F4U
#define ESMART_PORT_SEL_SHIFT			0
#define ESMART_PORT_SEL_MASK			0x3
#define RK3576_ESMART0_DLY_NUM			0x18F8U

#define RK3568_ESMART1_CTRL0			0x1A00U
#define RK3568_ESMART1_CTRL1			0x1A04U
#define RK3568_ESMART1_REGION0_CTRL		0x1A10U
#define RK3568_ESMART1_REGION0_YRGB_MST		0x1A14U
#define RK3568_ESMART1_REGION0_CBR_MST		0x1A18U
#define RK3568_ESMART1_REGION0_VIR		0x1A1CU
#define RK3568_ESMART1_REGION0_ACT_INFO		0x1A20U
#define RK3568_ESMART1_REGION0_DSP_INFO		0x1A24U
#define RK3568_ESMART1_REGION0_DSP_ST		0x1A28U
#define RK3568_ESMART1_REGION0_SCL_CTRL		0x1A30U
#define RK3568_ESMART1_REGION0_SCL_FACTOR_YRGB	0x1A34U
#define RK3568_ESMART1_REGION0_SCL_FACTOR_CBR	0x1A38U
#define RK3568_ESMART1_REGION0_SCL_OFFSET	0x1A3CU
#define RK3568_ESMART1_REGION1_CTRL		0x1A40U
#define RK3568_ESMART1_REGION1_YRGB_MST		0x1A44U
#define RK3568_ESMART1_REGION1_CBR_MST		0x1A48U
#define RK3568_ESMART1_REGION1_VIR		0x1A4CU
#define RK3568_ESMART1_REGION1_ACT_INFO		0x1A50U
#define RK3568_ESMART1_REGION1_DSP_INFO		0x1A54U
#define RK3568_ESMART1_REGION1_DSP_ST		0x1A58U
#define RK3568_ESMART1_REGION1_SCL_CTRL		0x1A60U
#define RK3568_ESMART1_REGION1_SCL_FACTOR_YRGB	0x1A64U
#define RK3568_ESMART1_REGION1_SCL_FACTOR_CBR	0x1A68U
#define RK3568_ESMART1_REGION1_SCL_OFFSET	0x1A6CU
#define RK3568_ESMART1_REGION2_CTRL		0x1A70U
#define RK3568_ESMART1_REGION2_YRGB_MST		0x1A74U
#define RK3568_ESMART1_REGION2_CBR_MST		0x1A78U
#define RK3568_ESMART1_REGION2_VIR		0x1A7CU
#define RK3568_ESMART1_REGION2_ACT_INFO		0x1A80U
#define RK3568_ESMART1_REGION2_DSP_INFO		0x1A84U
#define RK3568_ESMART1_REGION2_DSP_ST		0x1A88U
#define RK3568_ESMART1_REGION2_SCL_CTRL		0x1A90U
#define RK3568_ESMART1_REGION2_SCL_FACTOR_YRGB	0x1A94U
#define RK3568_ESMART1_REGION2_SCL_FACTOR_CBR	0x1A98U
#define RK3568_ESMART1_REGION2_SCL_OFFSET	0x1A9CU
#define RK3568_ESMART1_REGION3_CTRL		0x1AA0U
#define RK3568_ESMART1_REGION3_YRGB_MST		0x1AA4U
#define RK3568_ESMART1_REGION3_CBR_MST		0x1AA8U
#define RK3568_ESMART1_REGION3_VIR		0x1AACU
#define RK3568_ESMART1_REGION3_ACT_INFO		0x1AB0U
#define RK3568_ESMART1_REGION3_DSP_INFO		0x1AB4U
#define RK3568_ESMART1_REGION3_DSP_ST		0x1AB8U
#define RK3568_ESMART1_REGION3_SCL_CTRL		0x1AC0U
#define RK3568_ESMART1_REGION3_SCL_FACTOR_YRGB	0x1AC4U
#define RK3568_ESMART1_REGION3_SCL_FACTOR_CBR	0x1AC8U
#define RK3568_ESMART1_REGION3_SCL_OFFSET	0x1ACCU
#define RK3576_ESMART1_ALPHA_MAP		0x1AD8U
#define RK3576_ESMART1_PORT_SEL			0x1AF4U
#define RK3576_ESMART1_DLY_NUM			0x1AF8U

#define RK3568_SMART0_CTRL0			0x1C00U
#define RK3568_SMART0_CTRL1			0x1C04U
#define RK3568_SMART0_REGION0_CTRL		0x1C10U
#define RK3568_SMART0_REGION0_YRGB_MST		0x1C14U
#define RK3568_SMART0_REGION0_CBR_MST		0x1C18U
#define RK3568_SMART0_REGION0_VIR		0x1C1CU
#define RK3568_SMART0_REGION0_ACT_INFO		0x1C20U
#define RK3568_SMART0_REGION0_DSP_INFO		0x1C24U
#define RK3568_SMART0_REGION0_DSP_ST		0x1C28U
#define RK3568_SMART0_REGION0_SCL_CTRL		0x1C30U
#define RK3568_SMART0_REGION0_SCL_FACTOR_YRGB	0x1C34U
#define RK3568_SMART0_REGION0_SCL_FACTOR_CBR	0x1C38U
#define RK3568_SMART0_REGION0_SCL_OFFSET	0x1C3CU
#define RK3568_SMART0_REGION1_CTRL		0x1C40U
#define RK3568_SMART0_REGION1_YRGB_MST		0x1C44U
#define RK3568_SMART0_REGION1_CBR_MST		0x1C48U
#define RK3568_SMART0_REGION1_VIR		0x1C4CU
#define RK3568_SMART0_REGION1_ACT_INFO		0x1C50U
#define RK3568_SMART0_REGION1_DSP_INFO		0x1C54U
#define RK3568_SMART0_REGION1_DSP_ST		0x1C58U
#define RK3568_SMART0_REGION1_SCL_CTRL		0x1C60U
#define RK3568_SMART0_REGION1_SCL_FACTOR_YRGB	0x1C64U
#define RK3568_SMART0_REGION1_SCL_FACTOR_CBR	0x1C68U
#define RK3568_SMART0_REGION1_SCL_OFFSET	0x1C6CU
#define RK3568_SMART0_REGION2_CTRL		0x1C70U
#define RK3568_SMART0_REGION2_YRGB_MST		0x1C74U
#define RK3568_SMART0_REGION2_CBR_MST		0x1C78U
#define RK3568_SMART0_REGION2_VIR		0x1C7CU
#define RK3568_SMART0_REGION2_ACT_INFO		0x1C80U
#define RK3568_SMART0_REGION2_DSP_INFO		0x1C84U
#define RK3568_SMART0_REGION2_DSP_ST		0x1C88U
#define RK3568_SMART0_REGION2_SCL_CTRL		0x1C90U
#define RK3568_SMART0_REGION2_SCL_FACTOR_YRGB	0x1C94U
#define RK3568_SMART0_REGION2_SCL_FACTOR_CBR	0x1C98U
#define RK3568_SMART0_REGION2_SCL_OFFSET	0x1C9CU
#define RK3568_SMART0_REGION3_CTRL		0x1CA0U
#define RK3568_SMART0_REGION3_YRGB_MST		0x1CA4U
#define RK3568_SMART0_REGION3_CBR_MST		0x1CA8U
#define RK3568_SMART0_REGION3_VIR		0x1CACU
#define RK3568_SMART0_REGION3_ACT_INFO		0x1CB0U
#define RK3568_SMART0_REGION3_DSP_INFO		0x1CB4U
#define RK3568_SMART0_REGION3_DSP_ST		0x1CB8U
#define RK3568_SMART0_REGION3_SCL_CTRL		0x1CC0U
#define RK3568_SMART0_REGION3_SCL_FACTOR_YRGB	0x1CC4U
#define RK3568_SMART0_REGION3_SCL_FACTOR_CBR	0x1CC8U
#define RK3568_SMART0_REGION3_SCL_OFFSET	0x1CCCU
#define RK3576_ESMART2_ALPHA_MAP		0x1CD8U
#define RK3576_ESMART2_PORT_SEL			0x1CF4U
#define RK3576_ESMART2_DLY_NUM			0x1CF8U

#define RK3568_SMART1_CTRL0			0x1E00U
#define RK3568_SMART1_CTRL1			0x1E04U
#define RK3568_SMART1_REGION0_CTRL		0x1E10U
#define RK3568_SMART1_REGION0_YRGB_MST		0x1E14U
#define RK3568_SMART1_REGION0_CBR_MST		0x1E18U
#define RK3568_SMART1_REGION0_VIR		0x1E1CU
#define RK3568_SMART1_REGION0_ACT_INFO		0x1E20U
#define RK3568_SMART1_REGION0_DSP_INFO		0x1E24U
#define RK3568_SMART1_REGION0_DSP_ST		0x1E28U
#define RK3568_SMART1_REGION0_SCL_CTRL		0x1E30U
#define RK3568_SMART1_REGION0_SCL_FACTOR_YRGB	0x1E34U
#define RK3568_SMART1_REGION0_SCL_FACTOR_CBR	0x1E38U
#define RK3568_SMART1_REGION0_SCL_OFFSET	0x1E3CU
#define RK3568_SMART1_REGION1_CTRL		0x1E40U
#define RK3568_SMART1_REGION1_YRGB_MST		0x1E44U
#define RK3568_SMART1_REGION1_CBR_MST		0x1E48U
#define RK3568_SMART1_REGION1_VIR		0x1E4CU
#define RK3568_SMART1_REGION1_ACT_INFO		0x1E50U
#define RK3568_SMART1_REGION1_DSP_INFO		0x1E54U
#define RK3568_SMART1_REGION1_DSP_ST		0x1E58U
#define RK3568_SMART1_REGION1_SCL_CTRL		0x1E60U
#define RK3568_SMART1_REGION1_SCL_FACTOR_YRGB	0x1E64U
#define RK3568_SMART1_REGION1_SCL_FACTOR_CBR	0x1E68U
#define RK3568_SMART1_REGION1_SCL_OFFSET	0x1E6CU
#define RK3568_SMART1_REGION2_CTRL		0x1E70U
#define RK3568_SMART1_REGION2_YRGB_MST		0x1E74U
#define RK3568_SMART1_REGION2_CBR_MST		0x1E78U
#define RK3568_SMART1_REGION2_VIR		0x1E7CU
#define RK3568_SMART1_REGION2_ACT_INFO		0x1E80U
#define RK3568_SMART1_REGION2_DSP_INFO		0x1E84U
#define RK3568_SMART1_REGION2_DSP_ST		0x1E88U
#define RK3568_SMART1_REGION2_SCL_CTRL		0x1E90U
#define RK3568_SMART1_REGION2_SCL_FACTOR_YRGB	0x1E94U
#define RK3568_SMART1_REGION2_SCL_FACTOR_CBR	0x1E98U
#define RK3568_SMART1_REGION2_SCL_OFFSET	0x1E9CU
#define RK3568_SMART1_REGION3_CTRL		0x1EA0U
#define RK3568_SMART1_REGION3_YRGB_MST		0x1EA4U
#define RK3568_SMART1_REGION3_CBR_MST		0x1EA8U
#define RK3568_SMART1_REGION3_VIR		0x1EACU
#define RK3568_SMART1_REGION3_ACT_INFO		0x1EB0U
#define RK3568_SMART1_REGION3_DSP_INFO		0x1EB4U
#define RK3568_SMART1_REGION3_DSP_ST		0x1EB8U
#define RK3568_SMART1_REGION3_SCL_CTRL		0x1EC0U
#define RK3568_SMART1_REGION3_SCL_FACTOR_YRGB	0x1EC4U
#define RK3568_SMART1_REGION3_SCL_FACTOR_CBR	0x1EC8U
#define RK3568_SMART1_REGION3_SCL_OFFSET	0x1ECCU
#define RK3576_ESMART3_ALPHA_MAP		0x1ED8U
#define RK3576_ESMART3_PORT_SEL			0x1EF4U
#define RK3576_ESMART3_DLY_NUM			0x1EF8U

/* HDR register definition */
#define RK3568_HDR_LUT_CTRL			0x2000U

#define RK3588_VP3_DSP_CTRL			0xf00U
#define RK3588_CLUSTER2_WIN0_CTRL0		0x1400U
#define RK3588_CLUSTER3_WIN0_CTRL0		0x1600U

/* DSC 8K/4K register definition */
#define RK3588_DSC_8K_PPS0_3			0x4000U
#define RK3588_DSC_8K_CTRL0			0x40A0U
#define DSC_EN_SHIFT				0U
#define DSC_RBIT_SHIFT				2U
#define DSC_RBYT_SHIFT				3U
#define DSC_FLAL_SHIFT				4U
#define DSC_MER_SHIFT				5U
#define DSC_EPB_SHIFT				6U
#define DSC_EPL_SHIFT				7U
#define DSC_NSLC_MASK				0x7U
#define DSC_NSLC_SHIFT				16U
#define DSC_SBO_SHIFT				28U
#define DSC_IFEP_SHIFT				29U
#define DSC_PPS_UPD_SHIFT			31U
#define DSC_CTRL0_DEF_CON ((1UL << DSC_EN_SHIFT)   | (1UL << DSC_RBIT_SHIFT) | (0UL << DSC_RBYT_SHIFT) | \
			   (1UL << DSC_FLAL_SHIFT) | (1UL << DSC_MER_SHIFT)  | (0UL << DSC_EPB_SHIFT)  | \
			   (1UL << DSC_EPL_SHIFT)  | (1UL << DSC_SBO_SHIFT))

#define RK3588_DSC_8K_CTRL1			0x40A4U
#define RK3588_DSC_8K_STS0			0x40A8U
#define RK3588_DSC_8K_ERS			0x40C4U

#define RK3588_DSC_4K_PPS0_3			0x4100U
#define RK3588_DSC_4K_CTRL0			0x41A0U
#define RK3588_DSC_4K_CTRL1			0x41A4U
#define RK3588_DSC_4K_STS0			0x41A8U
#define RK3588_DSC_4K_ERS			0x41C4U

/* RK3528 HDR register definition */
#define RK3528_HDR_LUT_CTRL			0x2000U

/* RK3528 ACM register definition */
#define RK3528_ACM_CTRL				0x6400U
#define RK3528_ACM_DELTA_RANGE			0x6404U
#define RK3528_ACM_FETCH_START			0x6408U
#define RK3528_ACM_FETCH_DONE			0x6420U
#define RK3528_ACM_YHS_DEL_HY_SEG0		0x6500U
#define RK3528_ACM_YHS_DEL_HY_SEG152		0x6760U
#define RK3528_ACM_YHS_DEL_HS_SEG0		0x6764U
#define RK3528_ACM_YHS_DEL_HS_SEG220		0x6ad4U
#define RK3528_ACM_YHS_DEL_HGAIN_SEG0		0x6ad8U
#define RK3528_ACM_YHS_DEL_HGAIN_SEG64		0x6bd8U

#define RK3568_MAX_REG				0x1ED0U

#define RK3562_GRF_IOC_VO_IO_CON		0x10500U
#define RK3568_GRF_VO_CON1			0x0364U
#define GRF_BT656_CLK_INV_SHIFT			1
#define GRF_BT1120_CLK_INV_SHIFT		2
#define GRF_RGB_DCLK_INV_SHIFT			3

/* Base SYS_GRF: 0x2600a000*/
#define RK3576_SYS_GRF_MEMFAULT_STATUS0		0x0148U

/* Base IOC_GRF: 0x26040000 */
#define RK3576_VCCIO_IOC_MISC_CON8		0x6420U
#define RK3576_IOC_VOP_DCLK_INV_SEL_SHIFT	9
#define RK3576_IOC_VOPLITE_SEL_SHIFT		11

/* Base PMU2: 0x27380000 */
#define RK3576_PMU_PWR_GATE_STS			0x0230U
#define PD_VOP_ESMART_DWN_STAT			12
#define PD_VOP_CLUSTER_DWN_STAT			13
#define RK3576_PMU_BISR_PDGEN_CON0		0x0510U
#define PD_VOP_ESMART_REPAIR_ENA_SHIFT		12
#define PD_VOP_CLUSTER_REPAIR_ENA_SHIFT		13
#define RK3576_PMU_BISR_PWR_REPAIR_STATUS0	0x0570U
#define PD_VOP_ESMART_PWR_REPAIR_STAT_SHIFT	12
#define PD_VOP_CLUSTER_PWR_REPAIR_STAT_SHIFT	13

#define RK3588_GRF_SOC_CON1			0x0304U
#define RK3588_GRF_VOP_DCLK_INV_SEL_SHIFT	14

#define RK3588_GRF_VOP_CON2			0x0008U
#define RK3588_GRF_EDP0_ENABLE_SHIFT		0
#define RK3588_GRF_HDMITX0_ENABLE_SHIFT		1
#define RK3588_GRF_EDP1_ENABLE_SHIFT		3
#define RK3588_GRF_HDMITX1_ENABLE_SHIFT		4

#define RK3588_GRF_VO1_CON0			0x0000U
#define HDMI_SYNC_POL_MASK			0x3
#define HDMI0_SYNC_POL_SHIFT			5
#define HDMI1_SYNC_POL_SHIFT			7

#define RK3588_PMU_BISR_CON3			0x20CU
#define RK3588_PD_CLUSTER0_REPAIR_EN_SHIFT	9U
#define RK3588_PD_CLUSTER1_REPAIR_EN_SHIFT	10U
#define RK3588_PD_CLUSTER2_REPAIR_EN_SHIFT	11U
#define RK3588_PD_CLUSTER3_REPAIR_EN_SHIFT	12U
#define RK3588_PD_DSC_8K_REPAIR_EN_SHIFT	13U
#define RK3588_PD_DSC_4K_REPAIR_EN_SHIFT	14U
#define RK3588_PD_ESMART_REPAIR_EN_SHIFT	15U

#define RK3588_PMU_BISR_STATUS5			0x294U
#define RK3588_PD_CLUSTER0_PWR_STAT_SHIFI	9U
#define RK3588_PD_CLUSTER1_PWR_STAT_SHIFI	10U
#define RK3588_PD_CLUSTER2_PWR_STAT_SHIFI	11U
#define RK3588_PD_CLUSTER3_PWR_STAT_SHIFI	12U
#define RK3588_PD_DSC_8K_PWR_STAT_SHIFI		13U
#define RK3588_PD_DSC_4K_PWR_STAT_SHIFI		14U
#define RK3588_PD_ESMART_PWR_STAT_SHIFI		15U

#define VOP2_LAYER_MAX				8

#define VOP2_MAX_VP_OUTPUT_WIDTH		4096

/* KHz */
#define VOP2_MAX_DCLK_RATE			600000U

/*
 * vop2 dsc id
 */
#define ROCKCHIP_VOP2_DSC_8K	0
#define ROCKCHIP_VOP2_DSC_4K	1

/*
 * vop2 internal power domain id,
 * should be all none zero, 0 will be
 * treat as invalid;
 */
#define VOP2_PD_CLUSTER0			BIT(0)
#define VOP2_PD_CLUSTER1			BIT(1)
#define VOP2_PD_CLUSTER2			BIT(2)
#define VOP2_PD_CLUSTER3			BIT(3)
#define VOP2_PD_DSC_8K				BIT(5)
#define VOP2_PD_DSC_4K				BIT(6)
#define VOP2_PD_ESMART				BIT(7)
#define VOP2_PD_CLUSTER				BIT(8)

#define VOP2_PLANE_NO_SCALING			BIT(16)

#define VOP_FEATURE_OUTPUT_10BIT	BIT(0)
#define VOP_FEATURE_AFBDC		BIT(1)
#define VOP_FEATURE_ALPHA_SCALE		BIT(2)
#define VOP_FEATURE_HDR10		BIT(3)
#define VOP_FEATURE_NEXT_HDR		BIT(4)
/* a feature to splice two windows and two vps to support resolution > 4096 */
#define VOP_FEATURE_SPLICE		BIT(5)
#define VOP_FEATURE_OVERSCAN		BIT(6)
#define VOP_FEATURE_VIVID_HDR		BIT(7)
#define VOP_FEATURE_POST_ACM		BIT(8)
#define VOP_FEATURE_POST_CSC		BIT(9)
#define VOP_FEATURE_POST_FRC_V2		BIT(10)
#define VOP_FEATURE_POST_SHARP		BIT(11)

#define WIN_FEATURE_HDR2SDR		BIT(0)
#define WIN_FEATURE_SDR2HDR		BIT(1)
#define WIN_FEATURE_PRE_OVERLAY		BIT(2)
#define WIN_FEATURE_AFBDC		BIT(3)
#define WIN_FEATURE_CLUSTER_MAIN	BIT(4)
#define WIN_FEATURE_CLUSTER_SUB		BIT(5)
/* a mirror win can only get fb address
 * from source win:
 * Cluster1---->Cluster0
 * Esmart1 ---->Esmart0
 * Smart1  ---->Smart0
 * This is a feather on rk3566
 */
#define WIN_FEATURE_MIRROR		BIT(6)
#define WIN_FEATURE_MULTI_AREA		BIT(7)
#define WIN_FEATURE_Y2R_13BIT_DEPTH	BIT(8)
#define WIN_FEATURE_DCI			BIT(9)

#define V4L2_COLORSPACE_BT709F		0xfe
#define V4L2_COLORSPACE_BT2020F		0xff

enum vop_csc_bit_depth {
	CSC_10BIT_DEPTH,
	CSC_13BIT_DEPTH,
};

enum vop2_pol {
	HSYNC_POSITIVE = 0,
	VSYNC_POSITIVE = 1,
	DEN_NEGATIVE   = 2,
	DCLK_INVERT    = 3
};

enum vop2_bcsh_out_mode {
	BCSH_OUT_MODE_BLACK,
	BCSH_OUT_MODE_BLUE,
	BCSH_OUT_MODE_COLOR_BAR,
	BCSH_OUT_MODE_NORMAL_VIDEO,
};

#define VOP_REG_(off, _mask, _shift, _write_mask) \
		{ \
		 .offset = off, \
		 .mask = _mask, \
		 .shift = _shift, \
		 .write_mask = _write_mask, \
		}

#define VOP_REG(off, _mask, _shift) \
		VOP_REG_(off, _mask, _shift, false)
enum dither_down_mode {
	RGB888_TO_RGB565 = 0x0,
	RGB888_TO_RGB666 = 0x1
};

enum dither_down_mode_sel {
	DITHER_DOWN_ALLEGRO = 0x0,
	DITHER_DOWN_FRC = 0x1
};

enum vop2_video_ports_id {
	VOP2_VP0,
	VOP2_VP1,
	VOP2_VP2,
	VOP2_VP3,
	VOP2_VP_MAX,
};

enum vop2_layer_type {
	CLUSTER_LAYER = 0,
	ESMART_LAYER = 1,
	SMART_LAYER = 2,
};

/* This define must same with kernel win phy id */
enum vop2_layer_phy_id {
	ROCKCHIP_VOP2_CLUSTER0 = 0,
	ROCKCHIP_VOP2_CLUSTER1,
	ROCKCHIP_VOP2_ESMART0,
	ROCKCHIP_VOP2_ESMART1,
	ROCKCHIP_VOP2_SMART0,
	ROCKCHIP_VOP2_SMART1,
	ROCKCHIP_VOP2_CLUSTER2,
	ROCKCHIP_VOP2_CLUSTER3,
	ROCKCHIP_VOP2_ESMART2,
	ROCKCHIP_VOP2_ESMART3,
	ROCKCHIP_VOP2_LAYER_MAX,
	ROCKCHIP_VOP2_PHY_ID_INVALID = -1,
};

enum vop2_scale_up_mode {
	VOP2_SCALE_UP_NRST_NBOR,
	VOP2_SCALE_UP_BIL,
	VOP2_SCALE_UP_BIC,
	VOP2_SCALE_UP_ZME,
};

enum vop2_scale_down_mode {
	VOP2_SCALE_DOWN_NRST_NBOR,
	VOP2_SCALE_DOWN_BIL,
	VOP2_SCALE_DOWN_AVG,
	VOP2_SCALE_DOWN_ZME,
};

enum scale_mode {
	SCALE_NONE = 0x0,
	SCALE_UP   = 0x1,
	SCALE_DOWN = 0x2
};

enum vop_dsc_interface_mode {
	VOP_DSC_IF_DISABLE = 0,
	VOP_DSC_IF_HDMI = 1,
	VOP_DSC_IF_MIPI_DS_MODE = 2,
	VOP_DSC_IF_MIPI_VIDEO_MODE = 3,
};

enum vop3_pre_scale_down_mode {
	VOP3_PRE_SCALE_UNSPPORT,
	VOP3_PRE_SCALE_DOWN_GT,
	VOP3_PRE_SCALE_DOWN_AVG,
};

enum vop3_esmart_lb_mode {
	VOP3_ESMART_8K_MODE,
	VOP3_ESMART_4K_4K_MODE,
	VOP3_ESMART_4K_2K_2K_MODE,
	VOP3_ESMART_2K_2K_2K_2K_MODE,
	VOP3_ESMART_4K_4K_4K_MODE,
	VOP3_ESMART_4K_4K_2K_2K_MODE,
};

struct vop2_layer {
	u8 id;
	/**
	 * @win_phys_id: window id of the layer selected.
	 * Every layer must make sure to select different
	 * windows of others.
	 */
	u8 win_phys_id;
};

struct vop2_power_domain_data {
	u16 id;
	u16 parent_id;
	/*
	 * @module_id_mask: module id of which module this power domain is belongs to.
	 * PD_CLUSTER0,1,2,3 only belongs to CLUSTER0/1/2/3, PD_Esmart0 shared by Esmart1/2/3
	 */
	u32 module_id_mask;
};

struct vop2_win_data {
	const char *name;
	u8 phys_id;
	enum vop2_layer_type type;
	u8 win_sel_port_offset;
	u8 layer_sel_win_id[VOP2_VP_MAX];
	u8 axi_id;
	u8 axi_uv_id;
	u8 axi_yrgb_id;
	u8 splice_win_id;
	u8 hsu_filter_mode;
	u8 hsd_filter_mode;
	u8 vsu_filter_mode;
	u8 vsd_filter_mode;
	u8 hsd_pre_filter_mode;
	u8 vsd_pre_filter_mode;
	u8 scale_engine_num;
	u8 source_win_id;
	u8 possible_crtcs;
	u16 pd_id;
	u32 reg_offset;
	u32 max_upscale_factor;
	u32 max_downscale_factor;
	u32 feature;
	u32 supported_rotations;
	bool splice_mode_right;
};

struct vop2_vp_data {
	u32 feature;
	u32 max_dclk;
	u8 pre_scan_max_dly;
	u8 layer_mix_dly;
	u8 hdrvivid_dly;
	u8 sdr2hdr_dly;
	u8 hdr_mix_dly;
	u8 win_dly;
	u8 splice_vp_id;
	u8 pixel_rate;
	struct vop_rect max_output;
	struct vop_urgency *urgency;
};

struct vop2_plane_table {
	enum vop2_layer_phy_id plane_id;
	enum vop2_layer_type plane_type;
};

struct vop2_vp_plane_mask {
	u8 primary_plane_id; /* use this win to show logo */
	u8 attached_layers_nr; /* number layers attach to this vp */
	u8 attached_layers[VOP2_LAYER_MAX]; /* the layers attached to this vp */
	u32 plane_mask;
	int cursor_plane_id;
};

struct vop2_dsc_data {
	u8 id;
	u8 max_slice_num;
	u8 max_linebuf_depth;	/* used to generate the bitstream */
	u8 min_bits_per_pixel;	/* bit num after encoder compress */
	u16 pd_id;
	const char *dsc_txp_clk_src_name;
	const char *dsc_txp_clk_name;
	const char *dsc_pxl_clk_name;
	const char *dsc_cds_clk_name;
};

struct dsc_error_info {
	u32 dsc_error_val;
	char dsc_error_info[50];
};

struct vop2_dump_regs {
	u32 offset;
	const char *name;
	u32 state_base;
	u32 state_mask;
	u32 state_shift;
	bool enable_state;
	u32 size;
};

struct vop2_esmart_lb_map {
	enum vop3_esmart_lb_mode lb_mode;
	u8 lb_map_value;
};

struct vop2_data {
	u32 version;
	enum vop3_esmart_lb_mode esmart_lb_mode;
	struct vop2_vp_data *vp_data;
	struct vop2_win_data *win_data;
	struct vop2_vp_plane_mask *plane_mask;
	struct vop2_plane_table *plane_table;
	struct vop2_power_domain_data *pd;
	struct vop2_dsc_data *dsc;
	struct dsc_error_info *dsc_error_ecw;
	struct dsc_error_info *dsc_error_buffer_flow;
	struct vop2_dump_regs *dump_regs;
	const struct vop2_esmart_lb_map *esmart_lb_mode_map;
	u8 *vp_primary_plane_order;
	u8 *vp_default_primary_plane;
	u8 nr_vps;
	u8 nr_layers;
	u8 nr_mixers;
	u8 nr_gammas;
	u8 nr_pd;
	u8 nr_dscs;
	u8 nr_dsc_ecw;
	u8 nr_dsc_buffer_flow;
	u8 esmart_lb_mode_num;
	u32 reg_len;
	u32 dump_regs_size;
};

struct vop2 {
	u32 *regsbak;
	void *regs;
	void *grf;
	void *vop_grf;
	void *vo1_grf;
	void *sys_pmu;
	void *ioc_grf;
	u32 reg_len;
	u32 version;
	enum vop3_esmart_lb_mode esmart_lb_mode;
	bool global_init;
	bool merge_irq;
	const struct vop2_data *data;
	struct vop2_vp_plane_mask vp_plane_mask[VOP2_VP_MAX];
};

static struct vop2 *rockchip_vop2;

/* vop2_layer_phy_id */
static const char *const vop2_layer_name_list[] = {
	"Cluster0",
	"Cluster1",
	"Esmart0",
	"Esmart1",
	"Smart0",
	"Smart1",
	"Cluster2",
	"Cluster3",
	"Esmart2",
	"Esmart3",
};

static inline const char *vop2_plane_id_to_string(unsigned long phy)
{
	if (phy == (unsigned long)ROCKCHIP_VOP2_PHY_ID_INVALID) {
		return "INVALID";
	}

	if (phy >= ARRAY_SIZE(vop2_layer_name_list)) {
		return NULL;
	}

	return vop2_layer_name_list[phy];
}

static inline bool is_vop3(struct vop2 *vop2)
{
	if (vop2->version == VOP_VERSION_RK3568 || vop2->version == VOP_VERSION_RK3588) {
		return false;
	} else {
		return true;
	}
}

/*
 * bli_sd_factor = (src - 1) / (dst - 1) << 12;
 * avg_sd_factor:
 * bli_su_factor:
 * bic_su_factor:
 * = (src - 1) / (dst - 1) << 16;
 *
 * ygt2 enable: dst get one line from two line of the src
 * ygt4 enable: dst get one line from four line of the src.
 *
 */
#define VOP2_BILI_SCL_DN(src, dst)	(((src - 1U) << 12) / (dst - 1U))
#define VOP2_COMMON_SCL(src, dst)	(((src - 1U) << 16) / (dst - 1U))

#define VOP2_BILI_SCL_FAC_CHECK(src, dst, fac)	 \
				(fac * (dst - 1U) >> 12 < (src - 1U))
#define VOP2_COMMON_SCL_FAC_CHECK(src, dst, fac) \
				(fac * (dst - 1U) >> 16 < (src - 1U))
#define VOP3_COMMON_HOR_SCL_FAC_CHECK(src, dst, fac) \
				(fac * (dst - 1U) >> 16 < (src - 1U))

static uint16_t vop2_scale_factor(enum scale_mode mode,
				  enum vop2_scale_down_mode filter_mode,
				  uint32_t src, uint32_t dst)
{
	uint32_t fac;
	int i;

	if (mode == SCALE_NONE) {
		return 0;
	}

	/*
	 * A workaround to avoid zero div.
	 */
	if ((dst == 1U) || (src == 1U)) {
		dst = dst + 1U;
		src = src + 1U;
	}

	if ((mode == SCALE_DOWN) && (filter_mode == VOP2_SCALE_DOWN_BIL)) {
		fac = VOP2_BILI_SCL_DN((src), (dst));
		for (i = 0; i < 100; i++) {
			if (VOP2_BILI_SCL_FAC_CHECK((src), (dst), (fac))) {
				break;
			}
			fac -= 1U;
			(void)printf("down fac cali: src:%d, dst:%d, fac:0x%x\n", src, dst, fac);
		}
	} else {
		fac = VOP2_COMMON_SCL((src), (dst));
		for (i = 0; i < 100; i++) {
			if (VOP2_COMMON_SCL_FAC_CHECK((src), (dst), (fac))) {
				break;
			}
			fac -= 1U;
			(void)printf("up fac cali:  src:%d, dst:%d, fac:0x%x\n", src, dst, fac);
		}
	}

	return (uint16_t)fac;
}

static bool vop3_scale_up_fac_check(uint32_t src, uint32_t dst, uint32_t fac, bool is_hor)
{
	if (is_hor) {
		return VOP3_COMMON_HOR_SCL_FAC_CHECK((src), (dst), (fac));
	}
	return VOP2_COMMON_SCL_FAC_CHECK((src), (dst), (fac));
}

static uint16_t vop3_scale_factor(enum scale_mode mode,
				  uint32_t src, uint32_t dst, bool is_hor)
{
	uint32_t fac;
	int i;

	if (mode == SCALE_NONE) {
		return 0;
	}

	/*
	 * A workaround to avoid zero div.
	 */
	if ((dst == 1U) || (src == 1U)) {
		dst = dst + 1U;
		src = src + 1U;
	}

	if (mode == SCALE_DOWN) {
		fac = VOP2_BILI_SCL_DN((src), (dst));
		for (i = 0; i < 100; i++) {
			if (VOP2_BILI_SCL_FAC_CHECK((src), (dst), (fac))) {
				break;
			}
			fac -= 1U;
			(void)printf("down fac cali: src:%d, dst:%d, fac:0x%x\n", src, dst, fac);
		}
	} else {
		fac = VOP2_COMMON_SCL((src), (dst));
		for (i = 0; i < 100; i++) {
			if (vop3_scale_up_fac_check((src), (dst), (fac), (is_hor))) {
				break;
			}
			fac -= 1U;
			(void)printf("up fac cali:  src:%d, dst:%d, fac:0x%x\n", src, dst, fac);
		}
	}

	return (uint16_t)fac;
}

static inline enum scale_mode scl_get_scl_mode(u32 src, u32 dst)
{
	if (src < dst) {
		return SCALE_UP;
	} else if (src > dst) {
		return SCALE_DOWN;
	} else {
		return SCALE_NONE;
	}
}

static inline int interpolate(int x1, int y1, int x2, int y2, int x)
{
	return y1 + (y2 - y1) * (x - x1) / (x2 - x1);
}

static u8 vop2_get_primary_plane(struct vop2 *vop2, u32 plane_mask)
{
	int i;

	for (i = 0; i < (int)vop2->data->nr_layers; i++) {
		if ((plane_mask & BIT(vop2->data->vp_primary_plane_order[i])) != 0U) {
			return vop2->data->vp_primary_plane_order[i];
		}
	}

	return vop2->data->vp_primary_plane_order[0];
}

static inline u16 scl_cal_scale2(u16 src, u16 dst)
{
	return ((src - 1U) << 12U) / (dst - 1U);
}

static inline void vop2_writel(struct vop2 *vop2, u32 offset, u32 v)
{
	(void)writel((v), (vop2->regs + offset));
	vop2->regsbak[offset >> 2] = v;
}

static inline u32 vop2_readl(struct vop2 *vop2, u32 offset)
{
	return readl(vop2->regs + offset);
}

static inline void vop2_mask_write(struct vop2 *vop2, u32 offset,
				   u32 mask, u32 shift, u32 v,
				   bool write_mask)
{
	if (mask == 0U) {
		return;
	}

	if (write_mask) {
		v = ((v & mask) << shift) | (mask << (shift + 16U));
	} else {
		u32 cached_val = vop2->regsbak[offset >> 2];

		v = (cached_val & ~(mask << shift)) | ((v & mask) << shift);
		vop2->regsbak[offset >> 2] = v;
	}

	(void)writel((v), (vop2->regs + offset));
}

static inline void vop2_grf_writel(struct vop2 *vop, void *grf_base, u32 offset,
				   u32 mask, u32 shift, u32 v)
{
	u32 val;

	val = (v << shift) | (mask << (shift + 16U));
	(void)writel((val), (grf_base + offset));
}

static inline u32 vop2_grf_readl(struct vop2 *vop, void *grf_base, u32 offset,
				  u32 mask, u32 shift)
{
	return (readl(grf_base + offset) >> shift) & mask;
}

static bool is_yuv_output(int bus_format)
{
	bool ret;

	switch (bus_format) {
	case MEDIA_BUS_FMT_YUV8_1X24:
	case MEDIA_BUS_FMT_YUV10_1X30:
	case MEDIA_BUS_FMT_YUYV10_1X20:
	case MEDIA_BUS_FMT_UYYVYY8_0_5X24:
	case MEDIA_BUS_FMT_UYYVYY10_0_5X30:
	case MEDIA_BUS_FMT_YUYV8_2X8:
	case MEDIA_BUS_FMT_YVYU8_2X8:
	case MEDIA_BUS_FMT_UYVY8_2X8:
	case MEDIA_BUS_FMT_VYUY8_2X8:
	case MEDIA_BUS_FMT_YUYV8_1X16:
	case MEDIA_BUS_FMT_YVYU8_1X16:
	case MEDIA_BUS_FMT_UYVY8_1X16:
	case MEDIA_BUS_FMT_VYUY8_1X16:
		ret = true;
		break;
	default:
		ret = false;
		break;
	}

	return ret;
}

static enum vop_csc_format vop2_convert_csc_mode(enum drm_color_encoding color_encoding,
						 enum drm_color_range color_range,
						 enum vop_csc_bit_depth bit_depth)
{
	bool full_range = color_range == DRM_COLOR_YCBCR_FULL_RANGE ? true : false;
	enum vop_csc_format csc_mode = CSC_BT709L;


	switch (color_encoding) {
	case DRM_COLOR_YCBCR_BT601:
		if (full_range) {
			csc_mode = CSC_BT601F;
		} else {
			csc_mode = CSC_BT601L;
		}
		break;

	case DRM_COLOR_YCBCR_BT709:
		if (full_range) {
			csc_mode = bit_depth == CSC_13BIT_DEPTH ? CSC_BT709F_13BIT : CSC_BT601F;
			if (bit_depth != CSC_13BIT_DEPTH) {
				(void)printf("Unsupported bt709f at 10bit csc depth, use bt601f instead\n");
			}
		} else {
			csc_mode = CSC_BT709L;
		}
		break;

	case DRM_COLOR_YCBCR_BT2020:
		if (full_range) {
			csc_mode = bit_depth == CSC_13BIT_DEPTH ? CSC_BT2020F_13BIT : CSC_BT601F;
			if (bit_depth != CSC_13BIT_DEPTH) {
				(void)printf("Unsupported bt2020f at 10bit csc depth, use bt601f instead\n");
			}
		} else {
			csc_mode = bit_depth == CSC_13BIT_DEPTH ? CSC_BT2020L_13BIT : CSC_BT2020L;
		}
		break;

	default:
		(void)printf("Unsuport color_encoding:%d\n", color_encoding);
		break;
	}

	return csc_mode;
}

static bool is_uv_swap(struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	int bus_format = conn_state->bus_format;
	int output_mode = conn_state->output_mode;
	int output_type = conn_state->type;

	/*
	 * FIXME:
	 *
	 * There is no media type for YUV444 output,
	 * so when out_mode is AAAA or P888, assume output is YUV444 on
	 * yuv format.
	 *
	 * From H/W testing, YUV444 mode need a rb swap except eDP.
	 */
	if (bus_format == MEDIA_BUS_FMT_YVYU8_1X16 ||
	    bus_format == MEDIA_BUS_FMT_VYUY8_1X16 ||
	    bus_format == MEDIA_BUS_FMT_YVYU8_2X8 ||
	    bus_format == MEDIA_BUS_FMT_VYUY8_2X8 ||
	    ((bus_format == MEDIA_BUS_FMT_YUV8_1X24 ||
	     bus_format == MEDIA_BUS_FMT_YUV10_1X30) &&
	    (output_mode == ROCKCHIP_OUT_MODE_AAAA ||
	     output_mode == ROCKCHIP_OUT_MODE_P888) &&
	     !(output_type == DRM_MODE_CONNECTOR_eDP))) {
		return true;
	} else {
		return false;
	}
}

static bool is_rb_swap(struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	int bus_format = conn_state->bus_format;

	/*
	 * The default component order of serial rgb3x8 formats
	 * is BGR. So it is needed to enable RB swap.
	 */
	if (bus_format == MEDIA_BUS_FMT_RGB888_3X8 ||
	    bus_format == MEDIA_BUS_FMT_RGB888_DUMMY_4X8) {
		return true;
	} else {
		return false;
	}
}

static bool is_yc_swap(int bus_format)
{
	bool ret;

	switch (bus_format) {
	case MEDIA_BUS_FMT_YUYV8_1X16:
	case MEDIA_BUS_FMT_YVYU8_1X16:
	case MEDIA_BUS_FMT_YUYV8_2X8:
	case MEDIA_BUS_FMT_YVYU8_2X8:
		ret = true;
		break;
	default:
		ret = false;
		break;
	}

	return ret;
}

static inline bool is_hot_plug_devices(int output_type)
{
	bool ret;

	switch (output_type) {
	case DRM_MODE_CONNECTOR_HDMIA:
	case DRM_MODE_CONNECTOR_HDMIB:
	case DRM_MODE_CONNECTOR_TV:
	case DRM_MODE_CONNECTOR_DisplayPort:
	case DRM_MODE_CONNECTOR_VGA:
	case DRM_MODE_CONNECTOR_Unknown:
		ret = true;
		break;
	default:
		ret = false;
		break;
	}

	return ret;
}

static struct vop2_win_data *vop2_find_win_by_phys_id(struct vop2 *vop2, u8 phys_id)
{
	u32 i;

	for (i = 0; i < vop2->data->nr_layers; i++) {
		if (vop2->data->win_data[i].phys_id == phys_id) {
			return &vop2->data->win_data[i];
		}
	}

	return NULL;
}

static struct vop2_power_domain_data *vop2_find_pd_data_by_id(struct vop2 *vop2, u16 pd_id)
{
	u32 i;

	for (i = 0; i < vop2->data->nr_pd; i++) {
		if (vop2->data->pd[i].id == pd_id) {
			return &vop2->data->pd[i];
		}
	}

	return NULL;
}

static void rk3568_vop2_load_lut(struct vop2 *vop2, u32 crtc_id,
				 u32 *lut_regs, u32 *lut_val, u32 lut_len)
{
	u32 vp_offset = crtc_id * 0x100U;
	u32 i;

	vop2_mask_write(vop2, RK3568_SYS_LUT_PORT_SEL,
			GAMMA_PORT_SEL_MASK, GAMMA_PORT_SEL_SHIFT,
			crtc_id, false);

	for (i = 0; i < lut_len; i++) {
		(void)writel((lut_val[i]), (lut_regs + i));
	}

	vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset,
			EN_MASK, DSP_LUT_EN_SHIFT, 1, false);
}

static void rk3588_vop2_load_lut(struct vop2 *vop2, u32 crtc_id,
				 u32 *lut_regs, u32 *lut_val, u32 lut_len)
{
	u32 vp_offset = crtc_id * 0x100U;
	u32 i;

	vop2_mask_write(vop2, RK3568_SYS_LUT_PORT_SEL,
			GAMMA_AHB_WRITE_SEL_MASK, GAMMA_AHB_WRITE_SEL_SHIFT,
			crtc_id, false);

	for (i = 0; i < lut_len; i++) {
		(void)writel((lut_val[i]), (lut_regs + i));
	}

	vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset,
			EN_MASK, DSP_LUT_EN_SHIFT, 1, false);
	vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset,
			EN_MASK, GAMMA_UPDATE_EN_SHIFT, 1, false);
}

static int rockchip_vop2_gamma_lut_init(struct vop2 *vop2,
					struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	struct crtc_state *cstate = &state->crtc_state;
	struct resource gamma_res;
	fdt_size_t lut_size;
	int ret;
	u32 i;
	u32 lut_len;
	u32 *lut_regs;
	u32 r, g, b;
	struct base2_disp_info *disp_info = conn_state->disp_info;
	static u8 gamma_lut_en_num = 1;

	if (gamma_lut_en_num > vop2->data->nr_gammas) {
		(void)printf("warn: only %d vp support gamma\n", vop2->data->nr_gammas);
		return 0;
	}

	ret = ofnode_read_resource_byname(cstate->node, "gamma_lut", &gamma_res);
	if (ret != 0) {
		(void)printf("failed to get gamma lut res\n");
	}
	lut_regs = (u32 *)gamma_res.start;
	lut_size = gamma_res.end - gamma_res.start + 1UL;
	if (lut_regs == (u32 *)FDT_ADDR_T_NONE) {
		(void)printf("failed to get gamma lut register\n");
		return 0;
	}
	lut_len = (u32)lut_size / 4U;
	if (lut_len != 256U && lut_len != 1024U) {
		(void)printf("Warning: unsupport gamma lut table[%d]\n", lut_len);
		return 0;
	}

	if (!cstate->lut_val) {
		if (!disp_info) {
			return 0;
		}

		if (disp_info->gamma_lut_data.size == 0U) {
			return 0;
		}

		cstate->lut_val = (u32 *)calloc(1, lut_size);
		for (i = 0; i < lut_len; i++) {
			r = disp_info->gamma_lut_data.lred[i] * (lut_len - 1U) / 0xffffU;
			g = disp_info->gamma_lut_data.lgreen[i] * (lut_len - 1U) / 0xffffU;
			b = disp_info->gamma_lut_data.lblue[i] * (lut_len - 1U) / 0xffffU;

			cstate->lut_val[i] = b * lut_len * lut_len + g * lut_len + r;
		}
	}

	if (vop2->version == VOP_VERSION_RK3568) {
		rk3568_vop2_load_lut(vop2, (u32)cstate->crtc_id, lut_regs,
				     cstate->lut_val, lut_len);
		gamma_lut_en_num++;
	} else {
		rk3588_vop2_load_lut(vop2, (u32)cstate->crtc_id, lut_regs,
				     cstate->lut_val, lut_len);
		if (cstate->splice_mode) {
			rk3588_vop2_load_lut(vop2, cstate->splice_crtc_id, lut_regs,
					     cstate->lut_val, lut_len);
			gamma_lut_en_num++;
		}
		gamma_lut_en_num++;
	}

	free(cstate->lut_val);

	return 0;
}

static int rockchip_vop2_cubic_lut_init(struct vop2 *vop2,
					struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	struct crtc_state *cstate = &state->crtc_state;
	u32 i, cubic_lut_len;
	struct base2_disp_info *disp_info = conn_state->disp_info;
	u32 vp_offset = (u32)cstate->crtc_id * 0x100U;
	struct base2_cubic_lut_data *lut = &conn_state->disp_info->cubic_lut_data;
	u32 *cubic_lut_addr;

	if (!disp_info) {
		return 0;
	}

	if (disp_info->cubic_lut_data.size == 0U) {
		return 0;
	}

	cubic_lut_addr = (u32 *)get_cubic_lut_buffer(cstate->crtc_id);
	cubic_lut_len = disp_info->cubic_lut_data.size;

	for (i = 0; i < cubic_lut_len / 2U; i++) {
		if (2U * i  + 1U > 4912U) {
			break;
		}

		*cubic_lut_addr = (((u32)lut->lred[2U * i]) & 0xfffU) +
					(((u32)lut->lgreen[2U * i] & 0xfffU) << 12) +
					(((u32)lut->lblue[2U * i] & 0xffU) << 24);
		cubic_lut_addr++;
		*cubic_lut_addr = ((((u32)lut->lblue[2U * i] & 0xf00U) >> 8) +
					(((u32)lut->lred[2U * i + 1U] & 0xfffU) << 4) +
					(((u32)lut->lgreen[2U * i + 1U] & 0xfffU) << 16) +
					(((u32)lut->lblue[2U * i + 1U] & 0xfU) << 28));
		cubic_lut_addr++;
		*cubic_lut_addr = ((u32)lut->lblue[2U * i + 1U] & 0xff0U) >> 4;
		cubic_lut_addr++;
		*cubic_lut_addr = 0;
		cubic_lut_addr++;
	}

	if (cubic_lut_len % 2U != 0U && 2U * i < 4913U) {
		*cubic_lut_addr = ((u32)lut->lred[2U * i] & 0xfffU) +
					(((u32)lut->lgreen[2U * i] & 0xfffU) << 12) +
					(((u32)lut->lblue[2U * i] & 0xffU) << 24);
		cubic_lut_addr++;
		*cubic_lut_addr = ((u32)lut->lblue[2U * i] & 0xf00U) >> 8;
		cubic_lut_addr++;
		*cubic_lut_addr = 0;
		cubic_lut_addr++;
		*cubic_lut_addr = 0;
	}

	vop2_writel(vop2, RK3568_VP0_3D_LUT_MST + vp_offset,
		    (u32)get_cubic_lut_buffer(cstate->crtc_id));
	vop2_mask_write(vop2, RK3568_SYS_AXI_LUT_CTRL,
			EN_MASK, LUT_DMA_EN_SHIFT, 1, false);
	vop2_mask_write(vop2, RK3568_VP0_3D_LUT_CTRL + vp_offset,
			EN_MASK, VP0_3D_LUT_EN_SHIFT, 1, false);
	vop2_mask_write(vop2, RK3568_VP0_3D_LUT_CTRL + vp_offset,
			EN_MASK, VP0_3D_LUT_UPDATE_SHIFT, 1, false);

	return 0;
}

static void vop2_bcsh_reg_update(struct display_state *state, struct vop2 *vop2,
				 struct bcsh_state *bcsh_state, u32 crtc_id)
{
	struct crtc_state *cstate = &state->crtc_state;
	u32 vp_offset = crtc_id * 0x100U;

	vop2_mask_write(vop2, RK3568_VP0_BCSH_CTRL + vp_offset, BCSH_CTRL_R2Y_MASK,
			BCSH_CTRL_R2Y_SHIFT, cstate->post_r2y_en ? 1U : 0U, false);
	vop2_mask_write(vop2, RK3568_VP0_BCSH_CTRL + vp_offset, BCSH_CTRL_Y2R_MASK,
			BCSH_CTRL_Y2R_SHIFT, cstate->post_y2r_en ? 1U : 0U, false);

	vop2_mask_write(vop2, RK3568_VP0_BCSH_CTRL + vp_offset, BCSH_CTRL_R2Y_CSC_MODE_MASK,
			BCSH_CTRL_R2Y_CSC_MODE_SHIFT, (u32)cstate->post_csc_mode, false);
	vop2_mask_write(vop2, RK3568_VP0_BCSH_CTRL + vp_offset, BCSH_CTRL_Y2R_CSC_MODE_MASK,
			BCSH_CTRL_Y2R_CSC_MODE_SHIFT, (u32)cstate->post_csc_mode, false);

	if (!cstate->bcsh_en) {
		vop2_mask_write(vop2, RK3568_VP0_BCSH_COLOR + vp_offset,
				BCSH_EN_MASK, BCSH_EN_SHIFT, 0, false);
		return;
	}

	vop2_mask_write(vop2, RK3568_VP0_BCSH_BCS + vp_offset,
			BCSH_BRIGHTNESS_MASK, BCSH_BRIGHTNESS_SHIFT,
			(u32)bcsh_state->brightness, false);
	vop2_mask_write(vop2, RK3568_VP0_BCSH_BCS + vp_offset,
			BCSH_CONTRAST_MASK, BCSH_CONTRAST_SHIFT, (u32)bcsh_state->contrast, false);
	vop2_mask_write(vop2, RK3568_VP0_BCSH_BCS + vp_offset,
			BCSH_SATURATION_MASK, BCSH_SATURATION_SHIFT,
			(u32)bcsh_state->saturation * (u32)bcsh_state->contrast / 0x100U, false);
	vop2_mask_write(vop2, RK3568_VP0_BCSH_H + vp_offset,
			BCSH_SIN_HUE_MASK, BCSH_SIN_HUE_SHIFT, (u32)bcsh_state->sin_hue, false);
	vop2_mask_write(vop2, RK3568_VP0_BCSH_H + vp_offset,
			BCSH_COS_HUE_MASK, BCSH_COS_HUE_SHIFT, (u32)bcsh_state->cos_hue, false);
	vop2_mask_write(vop2, RK3568_VP0_BCSH_BCS + vp_offset,
			BCSH_OUT_MODE_MASK, BCSH_OUT_MODE_SHIFT,
			(u32)BCSH_OUT_MODE_NORMAL_VIDEO, false);
	vop2_mask_write(vop2, RK3568_VP0_BCSH_COLOR + vp_offset,
			BCSH_EN_MASK, BCSH_EN_SHIFT, 1U, false);
}

static void vop2_tv_config_update(struct display_state *state, struct vop2 *vop2)
{
	struct connector_state *conn_state = &state->conn_state;
	struct base_bcsh_info *bcsh_info;
	struct crtc_state *cstate = &state->crtc_state;
	struct bcsh_state bcsh_state;
	int brightness, contrast, saturation, hue;
	u32 sin_hue, cos_hue;

	if (!conn_state->disp_info) {
		return;
	}

	bcsh_info = &conn_state->disp_info->bcsh_info;
	if (bcsh_info->brightness != 50U ||
	    bcsh_info->contrast != 50U ||
	    bcsh_info->saturation != 50U || bcsh_info->hue != 50U) {
		cstate->bcsh_en = true;
	}

	if (cstate->bcsh_en) {
		if (!cstate->yuv_overlay) {
			cstate->post_r2y_en = true;
		}
		if (!is_yuv_output(conn_state->bus_format)) {
			cstate->post_y2r_en = true;
		}
	} else {
		if (!cstate->yuv_overlay && is_yuv_output(conn_state->bus_format)) {
			cstate->post_r2y_en = true;
		}
		if (cstate->yuv_overlay && !is_yuv_output(conn_state->bus_format)) {
			cstate->post_y2r_en = true;
		}
	}

	cstate->post_csc_mode = vop2_convert_csc_mode(conn_state->color_encoding,
						      conn_state->color_range,
						      CSC_10BIT_DEPTH);

	if ((cstate->feature & VOP_FEATURE_OUTPUT_10BIT) != 0U) {
		brightness = interpolate(0, -128, 100, 127,
					 (int)bcsh_info->brightness);
	} else {
		brightness = interpolate(0, -32, 100, 31,
					 (int)bcsh_info->brightness);
	}
	contrast = interpolate(0, 0, 100, 511, (int)bcsh_info->contrast);
	saturation = interpolate(0, 0, 100, 511, (int)bcsh_info->saturation);
	hue = interpolate(0, -30, 100, 30, (int)bcsh_info->hue);


	/*
	 *  a:[-30~0):
	 *    sin_hue = 0x100 - sin(a)*256;
	 *    cos_hue = cos(a)*256;
	 *  a:[0~30]
	 *    sin_hue = sin(a)*256;
	 *    cos_hue = cos(a)*256;
	 */
	sin_hue = (u32)fixp_sin32(hue) >> 23U;
	cos_hue = (u32)fixp_cos32(hue) >> 23U;

	bcsh_state.brightness = brightness;
	bcsh_state.contrast = contrast;
	bcsh_state.saturation = saturation;
	bcsh_state.sin_hue = (int)sin_hue;
	bcsh_state.cos_hue = (int)cos_hue;

	vop2_bcsh_reg_update(state, vop2, &bcsh_state, (u32)cstate->crtc_id);
	if (cstate->splice_mode) {
		vop2_bcsh_reg_update(state, vop2, &bcsh_state, cstate->splice_crtc_id);
	}
}

static void vop2_setup_dly_for_vp(struct display_state *state, struct vop2 *vop2, u32 crtc_id)
{
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct crtc_state *cstate = &state->crtc_state;
	u32 bg_ovl_dly, bg_dly, pre_scan_dly;
	u16 hdisplay = (u16)mode->crtc_hdisplay;
	u16 hsync_len = (u16)mode->crtc_hsync_end - (u16)mode->crtc_hsync_start;

	if (cstate->crtc == NULL || crtc_id > ARRAY_SIZE(cstate->crtc->vps) - 1U) {
		return;
	}

	bg_ovl_dly = cstate->crtc->vps[crtc_id].bg_ovl_dly;
	bg_dly = vop2->data->vp_data[crtc_id].pre_scan_max_dly;
	bg_dly -= bg_ovl_dly;

	/*
	 * splice mode: hdisplay must roundup as 4 pixel,
	 * no splice mode: hdisplay must roundup as 2 pixel.
	 */
	if (cstate->splice_mode) {
		pre_scan_dly = bg_dly + (u32)((u32)roundup((u32)hdisplay, (4U)) >> 2U) - 1U;
	} else {
		pre_scan_dly = bg_dly + (u32)((u32)roundup((u32)hdisplay, (2U)) >> 1U) - 1U;
	}

	if (vop2->version == VOP_VERSION_RK3588 && hsync_len < 8U) {
		hsync_len = 8;
	}
	pre_scan_dly = (pre_scan_dly << 16) | hsync_len;
	vop2_mask_write(vop2, RK3568_VP0_BG_MIX_CTRL + crtc_id * 4U,
			BG_MIX_CTRL_MASK, BG_MIX_CTRL_SHIFT, bg_dly, false);
	vop2_writel(vop2, RK3568_VP0_PRE_SCAN_HTIMING + (crtc_id * 0x100U), pre_scan_dly);
}

static void vop3_setup_pipe_dly(struct display_state *state, struct vop2 *vop2, u32 crtc_id)
{
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2_win_data *win_data;
	u32 bg_dly, pre_scan_dly;
	u32 win_id;
	u16 hdisplay = (u16)mode->crtc_hdisplay;
	u16 hsync_len = (u16)mode->crtc_hsync_end - (u16)mode->crtc_hsync_start;
	u8 primary_plane_id;

	if ((u32)cstate->crtc_id > ARRAY_SIZE(vop2->vp_plane_mask) - 1U) {
		return;
	}
	primary_plane_id = vop2->vp_plane_mask[cstate->crtc_id].primary_plane_id;

	win_data = vop2_find_win_by_phys_id(vop2, primary_plane_id);
	if (win_data == NULL || win_data->name == NULL) {
		return;
	}
	win_id = (u32)atoi(&win_data->name[strlen(win_data->name) - 1U]);
	vop2_mask_write(vop2, RK3528_OVL_SYS_ESMART0_CTRL + win_id * 4U,
			ESMART_DLY_NUM_MASK, ESMART_DLY_NUM_SHIFT, 0, false);

	bg_dly = (u32)vop2->data->vp_data[crtc_id].win_dly +
		 (u32)vop2->data->vp_data[crtc_id].layer_mix_dly +
		 (u32)vop2->data->vp_data[crtc_id].hdr_mix_dly;
	/* hdisplay must roundup as 2 pixel */
	pre_scan_dly = bg_dly + (u32)((u32)roundup((u32)(hdisplay), (2U)) >> 1U) - 1U;
	/**
	 * pre_scan_hblank minimum value is 8, otherwise the win reset signal will
	 * lead to first line data be zero.
	 */
	pre_scan_dly = (pre_scan_dly << 16U) | (hsync_len < 8U ? 8U : hsync_len);
	vop2_mask_write(vop2, RK3528_OVL_PORT0_BG_MIX_CTRL + crtc_id * 0x100U,
			BG_MIX_CTRL_MASK, BG_MIX_CTRL_SHIFT, bg_dly, false);
	vop2_writel(vop2, RK3568_VP0_PRE_SCAN_HTIMING + (crtc_id * 0x100U), pre_scan_dly);
}

static void vop2_post_config(struct display_state *state, struct vop2 *vop2)
{
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct crtc_state *cstate = &state->crtc_state;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	u16 vtotal = (u16)mode->crtc_vtotal;
	u16 hact_st = (u16)mode->crtc_htotal - (u16)mode->crtc_hsync_start;
	u16 vact_st = (u16)mode->crtc_vtotal - (u16)mode->crtc_vsync_start;
	u16 hdisplay = (u16)mode->crtc_hdisplay;
	u16 vdisplay = (u16)mode->crtc_vdisplay;
	u16 hsize =
	    (u16)hdisplay * ((u16)conn_state->overscan.left_margin +
			(u16)conn_state->overscan.right_margin) / 200U;
	u16 vsize =
	    (u16)vdisplay * ((u16)conn_state->overscan.top_margin +
			(u16)conn_state->overscan.bottom_margin) / 200U;
	u16 hact_end, vact_end;
	u32 val;

	hsize = round_down(hsize, 2);
	vsize = round_down(vsize, 2);

	hact_st += hdisplay * (100U - (u16)conn_state->overscan.left_margin) / 200U;
	hact_end = hact_st + hsize;
	val = (u32)hact_st << 16U;
	val |= hact_end;

	vop2_writel(vop2, RK3568_VP0_POST_DSP_HACT_INFO + vp_offset, val);
	vact_st += vdisplay * (100U - (u16)conn_state->overscan.top_margin) / 200U;
	vact_end = vact_st + vsize;
	val = (u32)vact_st << 16U;
	val |= vact_end;
	vop2_writel(vop2, RK3568_VP0_POST_DSP_VACT_INFO + vp_offset, val);
	val = (u32)scl_cal_scale2(vdisplay, vsize) << 16U;
	val |= (u32)scl_cal_scale2(hdisplay, hsize);
	vop2_writel(vop2, RK3568_VP0_POST_SCL_FACTOR_YRGB + vp_offset, val);
#define POST_HORIZONTAL_SCALEDOWN_EN(x)		((u32)(x))
#define POST_VERTICAL_SCALEDOWN_EN(x)		((u32)(x) << 1U)
	vop2_mask_write(vop2, RK3568_VP0_POST_SCL_CTRL + vp_offset,
			RK3568_VP0_POST_SCALE_MASK, RK3568_VP0_POST_SCALE_SHIFT,
			POST_HORIZONTAL_SCALEDOWN_EN(hdisplay != hsize ? 1U : 0U) |
			POST_VERTICAL_SCALEDOWN_EN(vdisplay != vsize ? 1U : 0U),
			false);
	if ((mode->flags & (u32)DRM_MODE_FLAG_INTERLACE) != 0U) {
		u16 vact_st_f1 = vtotal + vact_st + 1U;
		u16 vact_end_f1 = vact_st_f1 + vsize;

		val = (u32)vact_st_f1 << 16U | (u32)vact_end_f1;
		vop2_writel(vop2, RK3568_VP0_POST_DSP_VACT_INFO_F1 + vp_offset, val);
	}

	if (is_vop3(vop2)) {
		vop3_setup_pipe_dly(state, vop2, (u32)cstate->crtc_id);
	} else {
		vop2_setup_dly_for_vp(state, vop2, (u32)cstate->crtc_id);
		if (cstate->splice_mode) {
			vop2_setup_dly_for_vp(state, vop2, cstate->splice_crtc_id);
		}
	}
}

static void vop3_post_acm_config(struct display_state *state, struct vop2 *vop2)
{
	struct connector_state *conn_state = &state->conn_state;
	struct crtc_state *cstate = &state->crtc_state;
	struct acm_data *acm = &conn_state->disp_info->acm_data;
	struct drm_display_mode *mode = &conn_state->mode;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	s16 *lut_y;
	s16 *lut_h;
	s16 *lut_s;
	u32 value;
	u32 i;

	vop2_mask_write(vop2, RK3528_VP0_ACM_CTRL + vp_offset,
		POST_ACM_BYPASS_EN_MASK, POST_ACM_BYPASS_EN_SHIFT, 0, false);
	if (acm->acm_enable == 0U) {
		(void)writel((0), (vop2->regs + RK3528_ACM_CTRL));
		return;
	}

	(void)printf("post acm enable\n");

	(void)writel((1), (vop2->regs + RK3528_ACM_FETCH_START));

	value = (u32)((u32)acm->acm_enable & 0x1U) + (u32)(((u32)mode->hdisplay & 0xfffU) << 8U) +
		(u32)(((u32)mode->vdisplay & 0xfffU) << 20U);
	(void)writel((value), (vop2->regs + RK3528_ACM_CTRL));

	value = (u32)((u32)acm->y_gain & 0x3ffU) + (u32)(((u32)acm->h_gain << 10U) & 0xffc00U) +
		(u32)(((u32)acm->s_gain << 20U) & 0x3ff00000U);
	(void)writel((value), (vop2->regs + RK3528_ACM_DELTA_RANGE));

	lut_y = &acm->gain_lut_hy[0];
	lut_h = &acm->gain_lut_hy[ACM_GAIN_LUT_HY_LENGTH];
	lut_s = &acm->gain_lut_hy[ACM_GAIN_LUT_HY_LENGTH * 2U];
	for (i = 0; i < ACM_GAIN_LUT_HY_LENGTH; i++) {
		value = ((u32)lut_y[i] & 0xffU) + (((u32)lut_h[i] << 8U) & 0xff00U) +
			(((u32)lut_s[i] << 16U) & 0xff0000U);
		(void)writel((value), (vop2->regs + RK3528_ACM_YHS_DEL_HY_SEG0 + (i << 2U)));
	}

	lut_y = &acm->gain_lut_hs[0];
	lut_h = &acm->gain_lut_hs[ACM_GAIN_LUT_HS_LENGTH];
	lut_s = &acm->gain_lut_hs[ACM_GAIN_LUT_HS_LENGTH * 2U];
	for (i = 0; i < ACM_GAIN_LUT_HS_LENGTH; i++) {
		value = ((u32)lut_y[i] & 0xffU) + (((u32)lut_h[i] << 8U) & 0xff00U) +
			(((u32)lut_s[i] << 16U) & 0xff0000U);
		(void)writel((value), (vop2->regs + RK3528_ACM_YHS_DEL_HS_SEG0 + (i << 2U)));
	}

	lut_y = &acm->delta_lut_h[0];
	lut_h = &acm->delta_lut_h[ACM_DELTA_LUT_H_LENGTH];
	lut_s = &acm->delta_lut_h[ACM_DELTA_LUT_H_LENGTH * 2U];
	for (i = 0; i < ACM_DELTA_LUT_H_LENGTH; i++) {
		value = ((u32)lut_y[i] & 0x3ffU) + (((u32)lut_h[i] << 12) & 0xff000U) +
			(((u32)lut_s[i] << 20U) & 0x3ff00000U);
		(void)writel((value), (vop2->regs + RK3528_ACM_YHS_DEL_HGAIN_SEG0 + (i << 2U)));
	}

	(void)writel((1), (vop2->regs + RK3528_ACM_FETCH_DONE));
}

static void vop3_post_csc_config(struct display_state *state, struct vop2 *vop2)
{
	struct connector_state *conn_state = &state->conn_state;
	struct crtc_state *cstate = &state->crtc_state;
	struct acm_data *acm = &conn_state->disp_info->acm_data;
	struct csc_info *csc = &conn_state->disp_info->csc_info;
	struct post_csc_coef csc_coef;
	bool is_input_yuv = false;
	bool is_output_yuv = false;
	bool post_r2y_en = false;
	bool post_csc_en = false;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	u32 value;
	u32 range_type;

	(void)printf("post csc enable\n");

	if (acm->acm_enable == 1U) {
		if (!cstate->yuv_overlay) {
			post_r2y_en = true;
		}

		/* do y2r in csc module */
		if (!is_yuv_output(conn_state->bus_format)) {
			post_csc_en = true;
		}
	} else {
		if (!cstate->yuv_overlay && is_yuv_output(conn_state->bus_format)) {
			post_r2y_en = true;
		}

		/* do y2r in csc module */
		if (cstate->yuv_overlay && !is_yuv_output(conn_state->bus_format)) {
			post_csc_en = true;
		}
	}

	if (csc->csc_enable == 0U) {
		post_csc_en = true;
	}

	if (cstate->yuv_overlay || post_r2y_en) {
		is_input_yuv = true;
	}

	if (is_yuv_output(conn_state->bus_format)) {
		is_output_yuv = true;
	}

	cstate->post_csc_mode = vop2_convert_csc_mode(conn_state->color_encoding,
						      conn_state->color_range,
						      CSC_13BIT_DEPTH);

	if (post_csc_en) {
		(void)rockchip_calc_post_csc(csc, &csc_coef, cstate->post_csc_mode, is_input_yuv,
					     is_output_yuv);

		vop2_mask_write(vop2, RK3528_VP0_ACM_CTRL + vp_offset,
				POST_CSC_COE00_MASK, POST_CSC_COE00_SHIFT,
				(u32)csc_coef.csc_coef00, false);
		value = (u32)csc_coef.csc_coef01 & 0xffffU;
		value |= ((u32)csc_coef.csc_coef02 << 16U);
		(void)writel((value), (vop2->regs + RK3528_VP0_CSC_COE01_02));
		value = (u32)csc_coef.csc_coef10 & 0xffffU;
		value |= ((u32)csc_coef.csc_coef11 << 16U);
		(void)writel((value), (vop2->regs + RK3528_VP0_CSC_COE10_11));
		value = (u32)csc_coef.csc_coef12 & 0xffffU;
		value |= ((u32)csc_coef.csc_coef20 << 16U);
		(void)writel((value), (vop2->regs + RK3528_VP0_CSC_COE12_20));
		value =(u32) csc_coef.csc_coef21 & 0xffffU;
		value |= ((u32)csc_coef.csc_coef22 << 16U);
		(void)writel((value), (vop2->regs + RK3528_VP0_CSC_COE21_22));
		(void)writel(((u32)csc_coef.csc_dc0), (vop2->regs + RK3528_VP0_CSC_OFFSET0));
		(void)writel(((u32)csc_coef.csc_dc1), (vop2->regs + RK3528_VP0_CSC_OFFSET1));
		(void)writel(((u32)csc_coef.csc_dc2), (vop2->regs + RK3528_VP0_CSC_OFFSET2));

		range_type = csc_coef.range_type == 1U ? 0U : 1U;
		range_type <<= is_input_yuv ? 0U : 1U;
		vop2_mask_write(vop2, RK3528_VP0_ACM_CTRL + vp_offset,
				POST_CSC_MODE_MASK, POST_CSC_MODE_SHIFT, range_type, false);
	}

	vop2_mask_write(vop2, RK3528_VP0_ACM_CTRL + vp_offset,
			POST_R2Y_EN_MASK, POST_R2Y_EN_SHIFT, post_r2y_en ? 1U : 0U, false);
	vop2_mask_write(vop2, RK3528_VP0_ACM_CTRL + vp_offset,
			POST_CSC_EN_MASK, POST_CSC_EN_SHIFT, post_csc_en ? 1U : 0U, false);
	vop2_mask_write(vop2, RK3528_VP0_ACM_CTRL + vp_offset,
			POST_R2Y_MODE_MASK, POST_R2Y_MODE_SHIFT, (u32)cstate->post_csc_mode, false);
}

static void vop3_post_config(struct display_state *state, struct vop2 *vop2)
{
	struct connector_state *conn_state = &state->conn_state;
	struct base2_disp_info *disp_info = conn_state->disp_info;
	const char *enable_flag;
	if (!disp_info) {
		(void)printf("disp_info is empty\n");
		return;
	}

	enable_flag = (const char *)&disp_info->cacm_header;
	if (strncasecmp(enable_flag, "CACM", 4) != 0) {
		(void)printf("acm and csc is not support\n");
		return;
	}

	vop3_post_acm_config(state, vop2);
	vop3_post_csc_config(state, vop2);
}

static int rk3576_vop2_wait_power_domain_on(struct vop2 *vop2,
					    struct vop2_power_domain_data *pd_data)
{
	u32 val = 0;
	bool is_bisr_en, is_otp_bisr_en;

	if (pd_data->id == VOP2_PD_CLUSTER) {
		is_bisr_en = vop2_grf_readl(vop2, vop2->sys_pmu, RK3576_PMU_BISR_PDGEN_CON0,
					    EN_MASK, PD_VOP_CLUSTER_REPAIR_ENA_SHIFT) == 1U ? true : false;
		is_otp_bisr_en = vop2_grf_readl(vop2, vop2->grf, RK3576_SYS_GRF_MEMFAULT_STATUS0,
						EN_MASK, PD_VOP_CLUSTER_REPAIR_ENA_SHIFT) == 1U ? true : false;
		if (is_bisr_en && is_otp_bisr_en) {
			return (int)readl_poll_timeout((vop2->sys_pmu + RK3576_PMU_BISR_PWR_REPAIR_STATUS0),
							(val), (((u32)(val >> PD_VOP_CLUSTER_PWR_REPAIR_STAT_SHIFT) & 0x1U) != 0U),
							(50UL * 1000UL));
		} else {
			return (int)readl_poll_timeout((vop2->sys_pmu + RK3576_PMU_PWR_GATE_STS),
							(val), (((u32)(val >> PD_VOP_CLUSTER_DWN_STAT) & 0x1U) != 0U),
							(50UL * 1000UL));
		}
	} else {
		is_bisr_en = vop2_grf_readl(vop2, vop2->sys_pmu, RK3576_PMU_BISR_PDGEN_CON0,
					    EN_MASK, PD_VOP_ESMART_REPAIR_ENA_SHIFT) == 1U ? true : false;
		is_otp_bisr_en = vop2_grf_readl(vop2, vop2->grf, RK3576_SYS_GRF_MEMFAULT_STATUS0,
						EN_MASK, PD_VOP_ESMART_REPAIR_ENA_SHIFT) == 1U ? true : false;
		if (is_bisr_en && is_otp_bisr_en) {
			return (int)readl_poll_timeout((vop2->sys_pmu + RK3576_PMU_BISR_PWR_REPAIR_STATUS0),
							(val), (((u32)(val >> PD_VOP_ESMART_PWR_REPAIR_STAT_SHIFT) & 0x1U) != 0U),
							(50UL * 1000UL));
		} else {
			return (int)readl_poll_timeout((vop2->sys_pmu + RK3576_PMU_PWR_GATE_STS),
							(val), (((u32)(val >> PD_VOP_ESMART_DWN_STAT) & 0x1U) == 0U),
							(50UL * 1000UL));
		}
	}
}

static int rk3576_vop2_power_domain_on(struct vop2 *vop2, struct vop2_power_domain_data *pd_data)
{
	int ret;

	if (pd_data->id == VOP2_PD_CLUSTER) {
		vop2_mask_write(vop2, RK3576_SYS_CLUSTER_PD_CTRL, EN_MASK,
				RK3576_CLUSTER_PD_EN_SHIFT, 0, true);
	} else {
		vop2_mask_write(vop2, RK3576_SYS_ESMART_PD_CTRL, EN_MASK,
				RK3576_ESMART_PD_EN_SHIFT, 0, true);
	}
	ret = rk3576_vop2_wait_power_domain_on(vop2, pd_data);
	if (ret != 0) {
		(void)printf("wait vop2 power domain timeout\n");
		return ret;
	}

	return 0;
}

static int rk3588_vop2_wait_power_domain_on(struct vop2 *vop2,
					    struct vop2_power_domain_data *pd_data)
{
	u32 val = 0;
	u32 shift;
	u32 shift_factor = 0;
	bool is_bisr_en;

	/*
	 * The order of pd status bits in BISR_STS register
	 * is different from that in VOP SYS_STS register.
	 */
	if (pd_data->id == VOP2_PD_DSC_8K ||
	    pd_data->id == VOP2_PD_DSC_4K ||
	    pd_data->id == VOP2_PD_ESMART) {
		shift_factor = 1;
	}

	shift = RK3588_PD_CLUSTER0_REPAIR_EN_SHIFT + (u32)generic_ffs((int)pd_data->id) - 1U - shift_factor;
	is_bisr_en = vop2_grf_readl(vop2, vop2->sys_pmu, RK3588_PMU_BISR_CON3, EN_MASK, shift) == 1U ? true : false;
	if (is_bisr_en) {
		shift = RK3588_PD_CLUSTER0_PWR_STAT_SHIFI + (u32)generic_ffs((int)pd_data->id) - 1U - shift_factor;

		return (int)readl_poll_timeout((vop2->sys_pmu + RK3588_PMU_BISR_STATUS5), (val),
						((u32)((u32)(val >> shift) & 0x1U) != 0U), (50UL * 1000UL));
	} else {
		shift = RK3588_CLUSTER0_PD_STATUS_SHIFT + (u32)generic_ffs((int)pd_data->id) - 1U;

		return (int)readl_poll_timeout((vop2->regs + RK3568_SYS_STATUS0), (val),
						((u32)((u32)(val >> shift) & 0x1U) == 0U), (50UL * 1000UL));
	}
}

static int rk3588_vop2_power_domain_on(struct vop2 *vop2, struct vop2_power_domain_data *pd_data)
{
	int ret;

	vop2_mask_write(vop2, RK3588_SYS_PD_CTRL, EN_MASK,
			(u32)generic_ffs((int)pd_data->id) - 1U, 0U, false);
	ret = rk3588_vop2_wait_power_domain_on(vop2, pd_data);
	if (ret != 0) {
		(void)printf("wait vop2 power domain timeout\n");
		return ret;
	}

	return 0;
}

static int vop2_power_domain_on(struct vop2 *vop2, u16 pd_id)
{
	struct vop2_power_domain_data *pd_data;
	int ret;

	if (pd_id == 0U) {
		return 0;
	}

	pd_data = vop2_find_pd_data_by_id(vop2, pd_id);
	if (!pd_data) {
		(void)printf("can't find pd_data by id\n");
		return -EINVAL;
	}

	if (pd_data->parent_id != 0U) {
		ret = vop2_power_domain_on(vop2, pd_data->parent_id);
		if (ret != 0) {
			(void)printf("can't open parent power domain\n");
			return -EINVAL;
		}
	}

	/*
	 * Read VOP internal power domain on/off status.
	 * We should query BISR_STS register in PMU for
	 * power up/down status when memory repair is enabled.
	 * Return value: 1 for power on, 0 for power off;
	 */
	if (vop2->version == VOP_VERSION_RK3576) {
		ret = rk3576_vop2_power_domain_on(vop2, pd_data);
	} else {
		ret = rk3588_vop2_power_domain_on(vop2, pd_data);
	}

	return ret;
}

static void rk3588_vop2_regsbak(struct vop2 *vop2)
{
	u32 *base = vop2->regs;
	u32 i;

	/*
	 * No need to backup HDR/DSC/GAMMA_LUT/BPP_LUT/MMU
	 */
	for (i = 0; i < (vop2->reg_len >> 2); i++) {
		vop2->regsbak[i] = base[i];
	}
}

static void vop3_overlay_init(struct vop2 *vop2, struct display_state *state)
{
	struct vop2_win_data *win_data;
	u8 layer_phy_id;
	u32 i, j;
	u32 ovl_port_offset;
	u32 layer_nr;
	u8 shift;

	/* layer sel win id */
	for (i = 0; i < vop2->data->nr_vps; i++) {
		if (i > (u32)VOP2_VP_MAX - 1U) {
			break;
		}

		shift = 0U;
		ovl_port_offset = 0x100U * i;
		layer_nr = vop2->vp_plane_mask[i].attached_layers_nr;
		for (j = 0; j < layer_nr; j++) {
			if (j > ARRAY_SIZE(vop2->vp_plane_mask[i].attached_layers) - 1U) {
				break;
			}

			layer_phy_id = vop2->vp_plane_mask[i].attached_layers[j];
			win_data = vop2_find_win_by_phys_id(vop2, layer_phy_id);
			vop2_mask_write(vop2, RK3528_OVL_PORT0_LAYER_SEL + ovl_port_offset, LAYER_SEL_MASK,
					shift, win_data->layer_sel_win_id[i], false);
			shift += 4U;
		}
	}

	if (vop2->version != VOP_VERSION_RK3576) {
		/* win sel port */
		for (i = 0; i < vop2->data->nr_vps; i++) {
			if (i > ARRAY_SIZE(vop2->vp_plane_mask) - 1U) {
				break;
			}

			layer_nr = vop2->vp_plane_mask[i].attached_layers_nr;
			for (j = 0; j < layer_nr; j++) {
				if (j > ARRAY_SIZE(vop2->vp_plane_mask[i].attached_layers) - 1U ||
				    vop2->vp_plane_mask[i].attached_layers[j] == 0U) {
					break;
				}

				layer_phy_id = vop2->vp_plane_mask[i].attached_layers[j];
				win_data = vop2_find_win_by_phys_id(vop2, layer_phy_id);
				shift = win_data->win_sel_port_offset * 2U;
				vop2_mask_write(vop2, RK3528_OVL_SYS_PORT_SEL,
						LAYER_SEL_PORT_MASK, shift, i, false);
			}
		}
	}
}

static void vop2_overlay_init(struct vop2 *vop2, struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2_win_data *win_data;
	u32 i, j;
	u32 layer_nr;
	u32 shift = 0;
	u8 total_used_layer = 0;
	u8 port_mux;
	u8 layer_phy_id;

	/* layer sel win id */
	for (i = 0; i < vop2->data->nr_vps; i++) {
		if (i > ARRAY_SIZE(vop2->vp_plane_mask) - 1U) {
			break;
		}

		layer_nr = vop2->vp_plane_mask[i].attached_layers_nr;
		for (j = 0; j < layer_nr; j++) {
			if (j > ARRAY_SIZE(vop2->vp_plane_mask[i].attached_layers) - 1U) {
				break;
			}

			layer_phy_id = vop2->vp_plane_mask[i].attached_layers[j];
			win_data = vop2_find_win_by_phys_id(vop2, layer_phy_id);
			vop2_mask_write(vop2, RK3568_OVL_LAYER_SEL, LAYER_SEL_MASK,
					shift, win_data->layer_sel_win_id[i], false);
			shift += 4U;
		}
	}

	/* win sel port */
	for (i = 0; i < vop2->data->nr_vps; i++) {
		if (i > ARRAY_SIZE(vop2->vp_plane_mask) - 1U) {
			break;
		}

		layer_nr = vop2->vp_plane_mask[i].attached_layers_nr;
		for (j = 0; j < layer_nr; j++) {
			if (j > ARRAY_SIZE(vop2->vp_plane_mask[i].attached_layers) - 1U ||
			    vop2->vp_plane_mask[i].attached_layers[j] == 0U) {
				break;
			}

			layer_phy_id = vop2->vp_plane_mask[i].attached_layers[j];
			win_data = vop2_find_win_by_phys_id(vop2, layer_phy_id);
			shift = (u32)win_data->win_sel_port_offset * 2U;
			vop2_mask_write(vop2, RK3568_OVL_PORT_SEL, LAYER_SEL_PORT_MASK,
					LAYER_SEL_PORT_SHIFT + shift, i, false);
		}
	}

	/**
	 * port mux config
	 */
	for (i = 0; i < vop2->data->nr_vps; i++) {
		if (i > ARRAY_SIZE(vop2->vp_plane_mask) - 1U) {
			break;
		}

		shift = i * 4U;
		if (vop2->vp_plane_mask[i].attached_layers_nr != 0U) {
			total_used_layer += vop2->vp_plane_mask[i].attached_layers_nr;
			port_mux = total_used_layer - 1U;
		} else {
			port_mux = 8U;
		}

		if (i == (u32)vop2->data->nr_vps - 1U) {
			port_mux = vop2->data->nr_mixers;
		}

		cstate->crtc->vps[i].bg_ovl_dly = (vop2->data->nr_mixers - port_mux) << 1U;
		vop2_mask_write(vop2, RK3568_OVL_PORT_SEL, PORT_MUX_MASK,
				PORT_MUX_SHIFT + shift, port_mux, false);
	}
}

static bool vop3_ignore_plane(struct vop2 *vop2, struct vop2_win_data *win)
{
	if (!is_vop3(vop2)) {
		return false;
	}

	if (vop2->esmart_lb_mode == VOP3_ESMART_8K_MODE &&
	    win->phys_id != (u8)ROCKCHIP_VOP2_ESMART0) {
		return true;
	} else if (vop2->esmart_lb_mode == VOP3_ESMART_4K_4K_MODE &&
		   (win->phys_id == (u8)ROCKCHIP_VOP2_ESMART1 || win->phys_id == (u8)ROCKCHIP_VOP2_ESMART3)) {
		return true;
	} else if (vop2->esmart_lb_mode == VOP3_ESMART_4K_2K_2K_MODE &&
		   win->phys_id == (u8)ROCKCHIP_VOP2_ESMART1) {
		return true;
	} else if (vop2->esmart_lb_mode == VOP3_ESMART_4K_4K_4K_MODE &&
		   win->phys_id == (u8)ROCKCHIP_VOP2_ESMART3) {
		return true;
	} else {
		return false;
	}
}

static void vop3_init_esmart_scale_engine(struct vop2 *vop2)
{
	struct vop2_win_data *win_data;
	u32 i;
	u8 scale_engine_num = 0;

	/* store plane mask for vop2_fixup_dts */
	for (i = 0; i < vop2->data->nr_layers; i++) {
		win_data = &vop2->data->win_data[i];
		if (vop3_ignore_plane(vop2, win_data) == true || win_data->type == CLUSTER_LAYER) {
			continue;
		}

		win_data->scale_engine_num = scale_engine_num;
		scale_engine_num++;
	}
}

static u8 vop3_get_esmart_lb_mode(struct vop2 *vop2)
{
	const struct vop2_esmart_lb_map *esmart_lb_mode_map = vop2->data->esmart_lb_mode_map;
	u32 i;

	if (esmart_lb_mode_map == NULL) {
		return (u8)vop2->esmart_lb_mode;
	}

	for (i = 0; i < vop2->data->esmart_lb_mode_num; i++) {
		if (vop2->esmart_lb_mode == esmart_lb_mode_map->lb_mode) {
			return esmart_lb_mode_map->lb_map_value;
		}
		esmart_lb_mode_map++;
	}

	if (i == vop2->data->esmart_lb_mode_num) {
		(void)printf("Unsupported esmart_lb_mode:%d\n", vop2->esmart_lb_mode);
	}

	return vop2->data->esmart_lb_mode_map[0].lb_map_value;
}

static void vop2_global_initial(struct vop2 *vop2, struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2_vp_plane_mask *plane_mask;
	u32 mask;
	u32 active_vp_num = 0;
	u32 i, j;
	u8 primary_plane_id;
	u8 layer_nr;
	u8 layer_phy_id;
	int ret;
	/* find the first unplug devices and set it as main display */
	int main_vp_index = -1;

	if (vop2->global_init) {
		return;
	}

	/* OTP must enable at the first time, otherwise mirror layer register is error */
	if (soc_is_rk3566()) {
		vop2_mask_write(vop2, RK3568_SYS_OTP_WIN_EN, EN_MASK,
				OTP_WIN_EN_SHIFT, 1, false);
	}

	if (cstate->crtc->assign_plane) {/* dts assign plane */
		for (i = 0; i < vop2->data->nr_vps; i++) {
			if (i > (u32)VOP2_VP_MAX - 1U) {
				break;
			}

			mask = cstate->crtc->vps[i].plane_mask;
			vop2->vp_plane_mask[i].plane_mask = mask;
			layer_nr = (u8)hweight32(mask); /* use bitmap to store plane mask */
			vop2->vp_plane_mask[i].attached_layers_nr = layer_nr;
			primary_plane_id = cstate->crtc->vps[i].primary_plane_id;
			if (primary_plane_id >= (u8)ROCKCHIP_VOP2_LAYER_MAX) {
				primary_plane_id = vop2_get_primary_plane(vop2, mask);
			}
			vop2->vp_plane_mask[i].primary_plane_id = primary_plane_id;
			vop2->vp_plane_mask[i].plane_mask = mask;

			/* plane mask[bitmap] convert into layer phy id[enum vop2_layer_phy_id]*/
			for (j = 0; j < layer_nr; j++) {
				if (j > ARRAY_SIZE(vop2->vp_plane_mask[i].attached_layers) - 1U) {
					break;
				}

				vop2->vp_plane_mask[i].attached_layers[j] = (u8)ffs((int)mask) - 1U;
				mask &= ~(u32)BIT(vop2->vp_plane_mask[i].attached_layers[j]);
			}
		}
	} else {/* need soft assign plane mask */
		(void)printf("Assign plane mask automatically\n");
		if (vop2->version == VOP_VERSION_RK3576) {
			for (i = 0; i < vop2->data->nr_vps; i++) {
				if (i > (u32)VOP2_VP_MAX - 1U) {
					break;
				}

				if (cstate->crtc->vps[i].enable) {
					vop2->vp_plane_mask[i].attached_layers_nr = 1;
					vop2->vp_plane_mask[i].primary_plane_id =
						vop2->data->vp_default_primary_plane[i];
					vop2->vp_plane_mask[i].attached_layers[0] =
						vop2->data->vp_default_primary_plane[i];
					vop2->vp_plane_mask[i].plane_mask |=
						(u32)BIT(vop2->data->vp_default_primary_plane[i]);
					active_vp_num++;
				}
			}
			(void)printf("VOP have %d active VP\n", active_vp_num);
		} else {
			for (i = 0; i < vop2->data->nr_vps; i++) {
				if (i > (u32)VOP2_VP_MAX - 1U) {
					break;
				}

				if (cstate->crtc->vps[i].enable) {
					active_vp_num++;
				}
			}
			(void)printf("VOP have %d active VP\n", active_vp_num);

			if (soc_is_rk3566() && active_vp_num > 2U) {
				(void)printf("ERROR: rk3566 only support 2 display output!!\n");
			}
			plane_mask = vop2->data->plane_mask;
			plane_mask += (active_vp_num - 1U) * (u32)VOP2_VP_MAX;
			/*
			 * For rk3528, one display policy for hdmi store in plane_mask[0], and
			 * the other for cvbs store in plane_mask[2].
			 */
			if (vop2->version == VOP_VERSION_RK3528 && active_vp_num == 1U &&
			    cstate->crtc->vps[1].output_type == DRM_MODE_CONNECTOR_TV) {
				plane_mask += 2U * (u32)VOP2_VP_MAX;
			}

			if (vop2->version == VOP_VERSION_RK3528) {
				/*
				 * For rk3528, the plane mask of vp is limited, only esmart2 can
				 * be selected by both vp0 and vp1.
				 */
				j = 0;
			} else {
				for (i = 0; i < vop2->data->nr_vps; i++) {
					if (i > (u32)VOP2_VP_MAX - 1U) {
						break;
					}

					if (!is_hot_plug_devices(cstate->crtc->vps[i].output_type)) {
						/* the first store main display plane mask */
						vop2->vp_plane_mask[i] = plane_mask[0];
						main_vp_index = (int)i;
						break;
					}
				}

				/* if no find unplug devices, use vp0 as main display */
				if (main_vp_index < 0) {
					main_vp_index = 0;
					vop2->vp_plane_mask[0] = plane_mask[0];
				}

				/* plane_mask[0] store main display, so we from plane_mask[1] */
				j = 1;
			}

			/* init other display except main display */
			for (i = 0; i < vop2->data->nr_vps; i++) {
				if (i > (u32)VOP2_VP_MAX - 1U) {
					break;
				}

				/* main display or no connect devices */
				if (i == (u32)main_vp_index || !cstate->crtc->vps[i].enable) {
					continue;
				}
				vop2->vp_plane_mask[i] = plane_mask[j];
				j++;
			}
		}
		/* store plane mask for vop2_fixup_dts */
		for (i = 0; i < vop2->data->nr_vps; i++) {
			if (i > (u32)VOP2_VP_MAX - 1U) {
				break;
			}

			layer_nr = vop2->vp_plane_mask[i].attached_layers_nr;
			for (j = 0; j < layer_nr; j++) {
				if (j > ARRAY_SIZE(vop2->vp_plane_mask[i].attached_layers) - 1U) {
					break;
				}

				layer_phy_id = vop2->vp_plane_mask[i].attached_layers[j];
				vop2->vp_plane_mask[i].plane_mask |= (u32)BIT(layer_phy_id);
			}
		}
	}

	rk3588_vop2_regsbak(vop2);

	vop2_mask_write(vop2, RK3568_OVL_CTRL, EN_MASK,
			OVL_PORT_MUX_REG_DONE_IMD_SHIFT, 1, false);
	vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
			IF_CTRL_REG_DONE_IMD_SHIFT, 1, false);

	for (i = 0; i < vop2->data->nr_vps; i++) {
		if (i > (u32)VOP2_VP_MAX - 1U) {
			break;
		}

		(void)printf("vp%d have layer nr:%d[", i, vop2->vp_plane_mask[i].attached_layers_nr);
		for (j = 0; j < vop2->vp_plane_mask[i].attached_layers_nr; j++) {
			if (j > ARRAY_SIZE(vop2->vp_plane_mask[i].attached_layers) - 1U) {
				break;
			}

			(void)printf("%s ",
			       vop2_plane_id_to_string(vop2->vp_plane_mask[i].attached_layers[j]));
		}
		(void)printf("], primary plane: %s\n",
		       vop2_plane_id_to_string(vop2->vp_plane_mask[i].primary_plane_id));
	}

	if (is_vop3(vop2)) {
		vop3_overlay_init(vop2, state);
	} else {
		vop2_overlay_init(vop2, state);
	}

	if (is_vop3(vop2)) {
		/*
		 * you can rewrite at dts vop node:
		 *
		 * VOP3_ESMART_8K_MODE = 0,
		 * VOP3_ESMART_4K_4K_MODE = 1,
		 * VOP3_ESMART_4K_2K_2K_MODE = 2,
		 * VOP3_ESMART_2K_2K_2K_2K_MODE = 3,
		 *
		 * &vop {
		 * 	esmart_lb_mode = /bits/ 8 <2>;
		 * };
		 */
		ret = ofnode_read_u32(cstate->node, "esmart_lb_mode", &vop2->esmart_lb_mode);
		if (ret < 0) {
			vop2->esmart_lb_mode = vop2->data->esmart_lb_mode;
		}
		if (vop2->version == VOP_VERSION_RK3576) {
			vop2_mask_write(vop2, RK3576_SYS_ESMART_PD_CTRL,
					RK3576_ESMART_LB_MODE_SEL_MASK,
					RK3576_ESMART_LB_MODE_SEL_SHIFT,
					vop3_get_esmart_lb_mode(vop2), true);
		} else {
			vop2_mask_write(vop2, RK3568_SYS_LUT_PORT_SEL,
					ESMART_LB_MODE_SEL_MASK,
					ESMART_LB_MODE_SEL_SHIFT,
					vop3_get_esmart_lb_mode(vop2), false);
		}

		vop3_init_esmart_scale_engine(vop2);

		if (vop2->version == VOP_VERSION_RK3576) {
			vop2_mask_write(vop2, RK3576_SYS_PORT_CTRL, EN_MASK,
					RK3576_DSP_VS_T_SEL_SHIFT, 0, true);
		} else {
			vop2_mask_write(vop2, RK3568_SYS_AXI_LUT_CTRL, EN_MASK,
					DSP_VS_T_SEL_SHIFT, 0, false);
		}

		/*
		 * This is a workaround for RK3528/RK3562/RK3576:
		 *
		 * The aclk pre auto gating function may disable the aclk
		 * in some unexpected cases, which detected by hardware
		 * automatically.
		 *
		 * For example, if the above function is enabled, the post
		 * scale function will be affected, resulting in abnormal
		 * display.
		 */
		if (vop2->version == VOP_VERSION_RK3528 || vop2->version == VOP_VERSION_RK3562 ||
		    vop2->version == VOP_VERSION_RK3576) {
			vop2_mask_write(vop2, RK3568_AUTO_GATING_CTRL, EN_MASK,
					ACLK_PRE_AUTO_GATING_EN_SHIFT, 0, false);
		}
	}

	if (vop2->version == VOP_VERSION_RK3568) {
		vop2_writel(vop2, RK3568_AUTO_GATING_CTRL, 0);
	}

	if (vop2->version == VOP_VERSION_RK3576) {
		vop2->merge_irq = ofnode_read_bool(cstate->node, "rockchip,vop-merge-irq");

		/* Default use rkiommu 1.0 for axi0 */
		vop2_mask_write(vop2, RK3576_SYS_MMU_CTRL, EN_MASK, RKMMU_V2_EN_SHIFT, 0, true);

		/* Init frc2.0 config */
		vop2_writel(vop2, 0xca0, 0xc8);
		vop2_writel(vop2, 0xca4, 0x01000100);
		vop2_writel(vop2, 0xca8, 0x03ff0100);
		vop2_writel(vop2, 0xda0, 0xc8);
		vop2_writel(vop2, 0xda4, 0x01000100);
		vop2_writel(vop2, 0xda8, 0x03ff0100);

		if (vop2->version == VOP_VERSION_RK3576 && vop2->merge_irq == true) {
			vop2_mask_write(vop2, RK3576_SYS_PORT_CTRL, EN_MASK,
					VP_INTR_MERGE_EN_SHIFT, 1, true);
		}

		/* Set reg done every field for interlace */
		vop2_mask_write(vop2, RK3576_SYS_PORT_CTRL, INTERLACE_FRM_REG_DONE_MASK,
				INTERLACE_FRM_REG_DONE_SHIFT, 0, false);
	}

	vop2->global_init = true;
}

static void rockchip_vop2_sharp_init(struct vop2 *vop2, struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	const struct vop2_data *vop2_data = vop2->data;
	const struct vop2_vp_data *vp_data = &vop2_data->vp_data[cstate->crtc_id];
	struct resource sharp_regs;
	u32 *sharp_reg_base;
	int ret;

	if ((vp_data->feature & VOP_FEATURE_POST_SHARP) == 0U) {
		return;
	}

	ret = ofnode_read_resource_byname(cstate->node, "sharp_regs", &sharp_regs);
	if (ret != 0) {
		(void)printf("failed to get sharp regs\n");
		return;
	}
	sharp_reg_base = (u32 *)sharp_regs.start;

	/*
	 * After vop initialization, keep sw_sharp_enable always on.
	 * Only enable/disable sharp submodule to avoid black screen.
	 */
	(void)writel((0x1), (sharp_reg_base));
}

static void rockchip_vop2_acm_init(struct vop2 *vop2, struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	const struct vop2_data *vop2_data = vop2->data;
	const struct vop2_vp_data *vp_data = &vop2_data->vp_data[cstate->crtc_id];
	struct resource acm_regs;
	u32 *acm_reg_base;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	int ret;

	if ((vp_data->feature & VOP_FEATURE_POST_ACM) == 0U) {
		return;
	}

	ret = ofnode_read_resource_byname(cstate->node, "acm_regs", &acm_regs);
	if (ret != 0) {
		(void)printf("failed to get acm regs\n");
		return;
	}
	acm_reg_base = (u32 *)acm_regs.start;

	/*
	 * Black screen is displayed when acm bypass switched
	 * between enable and disable. Therefore, disable acm
	 * bypass by default after system boot.
	 */
	vop2_mask_write(vop2, RK3528_VP0_ACM_CTRL + vp_offset,
			POST_ACM_BYPASS_EN_MASK, POST_ACM_BYPASS_EN_SHIFT, 0, false);

	(void)writel((0), (acm_reg_base + 0));
}

static int rockchip_vop2_of_get_gamma_lut(struct display_state *state,
					  struct device_node *dsp_lut_node)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct resource gamma_res;
	fdt_size_t lut_size;
	u32 *lut_regs;
	u32 *lut;
	u32 r, g, b;
	u32 lut_len;
	u32 i, j;
	u32 length;
	int len;
	int ret;

	(void)of_get_property(dsp_lut_node, "gamma-lut", &len);
	if (len == 0) {
		return -EINVAL;
	}
	length = (u32)len;

	ret = ofnode_read_resource_byname(cstate->node, "gamma_lut", &gamma_res);
	if (ret != 0) {
		(void)printf("failed to get gamma lut res\n");
	}
	lut_regs = (u32 *)gamma_res.start;
	lut_size = gamma_res.end - gamma_res.start + 1U;
	if (lut_regs == (u32 *)FDT_ADDR_T_NONE) {
		(void)printf("failed to get gamma lut register\n");
		return -EINVAL;
	}
	lut_len = (u32)lut_size / 4U;

	cstate->lut_val = (u32 *)calloc(1, lut_size);
	if (!cstate->lut_val) {
		return -ENOMEM;
	}

	length >>= 2U;
	if (length != lut_len) {
		lut = (u32 *)calloc(1, lut_len);
		if (!lut) {
			free(cstate->lut_val);
			return -ENOMEM;
		}

		ret = of_read_u32_array(dsp_lut_node, "gamma-lut", lut, length);
		if (ret != 0) {
			(void)printf("Failed to load gamma-lut for vp%d\n", cstate->crtc_id);
			free(cstate->lut_val);
			free(lut);
			return -EINVAL;
		}

		/*
		 * In order to achieve the same gamma correction effect in different
		 * platforms, the following conversion helps to translate from 8bit
		 * gamma table with 256 parameters to 10bit gamma with 1024 parameters.
		 */
		for (i = 0; i < lut_len; i++) {
			j = i * length / lut_len;
			if (j < length) {
				r = lut[j] / length / length * lut_len / length;
				g = lut[j] / length % length * lut_len / length;
				b = lut[j] % length * lut_len / length;

				cstate->lut_val[i] = r * lut_len * lut_len + g * lut_len + b;
			}
		}
		free(lut);
	} else {
		(void)of_read_u32_array(dsp_lut_node, "gamma-lut", cstate->lut_val, lut_len);
	}

	return 0;
}

static void rockchip_vop2_of_get_dsp_lut(struct vop2 *vop2, struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct device_node *dsp_lut_node;
	int handle;
	int ret;

	handle = ofnode_read_s32_default(np_to_ofnode(cstate->port_node), "dsp-lut", -1);
	if (handle < 0) {
		return;
	}

	dsp_lut_node = of_find_node_by_phandle((u32)handle);
	if (dsp_lut_node == NULL) {
		return;
	}

	ret = rockchip_vop2_of_get_gamma_lut(state, dsp_lut_node);
	if (ret != 0) {
		(void)printf("failed to load vp%d gamma-lut from dts\n", cstate->crtc_id);
	}
}

static int vop2_initial(struct vop2 *vop2, struct display_state *state)
{
	rockchip_vop2_of_get_dsp_lut(vop2, state);

	(void)rockchip_vop2_gamma_lut_init(vop2, state);
	(void)rockchip_vop2_cubic_lut_init(vop2, state);
	rockchip_vop2_sharp_init(vop2, state);
	rockchip_vop2_acm_init(vop2, state);

	return 0;
}

/*
 * VOP2 have multi video ports.
 * video port ------- crtc
 */
static int rockchip_vop2_preinit(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	const struct vop2_data *vop2_data = cstate->crtc->data;
	struct regmap *map;
	char dclk_name[16];
	int ret;

	if (!rockchip_vop2) {
		rockchip_vop2 = calloc(1, sizeof(struct vop2));
		if (!rockchip_vop2) {
			return -ENOMEM;
		}
		(void)memset(rockchip_vop2, 0, sizeof(struct vop2));
		rockchip_vop2->regsbak = malloc(RK3568_MAX_REG);
		rockchip_vop2->reg_len = RK3568_MAX_REG;
#ifdef CONFIG_SPL_BUILD
		rockchip_vop2->regs = (void *)RK3528_VOP_BASE;
#else
		rockchip_vop2->regs = dev_read_addr_ptr(cstate->dev);
		map = syscon_regmap_lookup_by_phandle(cstate->dev, "rockchip,grf");
		rockchip_vop2->grf = regmap_get_range(map, 0);
		if (rockchip_vop2->grf <= 0) {
			(void)printf("%s: Get syscon grf failed (ret=%p)\n", __func__, rockchip_vop2->grf);
		}
#endif
		rockchip_vop2->version = vop2_data->version;
		rockchip_vop2->data = vop2_data;
		if (rockchip_vop2->version == VOP_VERSION_RK3588) {
			map = syscon_regmap_lookup_by_phandle(cstate->dev, "rockchip,vop-grf");
			rockchip_vop2->vop_grf = regmap_get_range(map, 0);
			if (rockchip_vop2->vop_grf <= 0) {
				(void)printf("%s: Get syscon vop_grf failed (ret=%p)\n",
					     __func__, rockchip_vop2->vop_grf);
			}
			map = syscon_regmap_lookup_by_phandle(cstate->dev, "rockchip,vo1-grf");
			rockchip_vop2->vo1_grf = regmap_get_range(map, 0);
			if (rockchip_vop2->vo1_grf <= 0) {
				(void)printf("%s: Get syscon vo1_grf failed (ret=%p)\n",
					     __func__, rockchip_vop2->vo1_grf);
			}
			map = syscon_regmap_lookup_by_phandle(cstate->dev, "rockchip,pmu");
			rockchip_vop2->sys_pmu = regmap_get_range(map, 0);
			if (rockchip_vop2->sys_pmu <= 0) {
				(void)printf("%s: Get syscon sys_pmu failed (ret=%p)\n",
					     __func__, rockchip_vop2->sys_pmu);
			}
		} else if (rockchip_vop2->version == VOP_VERSION_RK3576) {
			map = syscon_regmap_lookup_by_phandle(cstate->dev, "rockchip,ioc-grf");
			rockchip_vop2->ioc_grf = regmap_get_range(map, 0);
			if (rockchip_vop2->ioc_grf <= 0) {
				(void)printf("%s: Get syscon ioc_grf failed (ret=%p)\n",
					     __func__, rockchip_vop2->ioc_grf);
			}
			map = syscon_regmap_lookup_by_phandle(cstate->dev, "rockchip,pmu");
			rockchip_vop2->sys_pmu = regmap_get_range(map, 0);
			if (rockchip_vop2->sys_pmu <= 0) {
				(void)printf("%s: Get syscon sys_pmu failed (ret=%p)\n",
					     __func__, rockchip_vop2->sys_pmu);
			}
		} else {
			/* Do nothing */
		}
	}

	(void)snprintf(dclk_name, sizeof(dclk_name), "dclk_vp%d", cstate->crtc_id);
	ret = reset_get_by_name(cstate->dev, dclk_name, &cstate->dclk_rst);
	if (ret < 0) {
		(void)printf("%s: failed to get dclk reset: %d\n", __func__, ret);
		cstate->dclk_rst.dev = NULL;
	}

	cstate->private = rockchip_vop2;
	cstate->max_output = vop2_data->vp_data[cstate->crtc_id].max_output;
	cstate->feature = vop2_data->vp_data[cstate->crtc_id].feature;

	vop2_global_initial(rockchip_vop2, state);

	return 0;
}

/*
 * calc the dclk on rk3588
 * the available div of dclk is 1, 2, 4
 *
 */
static unsigned long vop2_calc_dclk(unsigned long child_clk, unsigned long max_dclk)
{
	if (child_clk * 4U <= max_dclk) {
		return child_clk * 4U;
	} else if (child_clk * 2U <= max_dclk) {
		return child_clk * 2U;
	} else if (child_clk <= max_dclk) {
		return child_clk;
	} else {
		return 0;
	}
}

/*
 * 4 pixclk/cycle on rk3588
 * RGB/eDP/HDMI: if_pixclk >= dclk_core
 * DP: dp_pixclk = dclk_out <= dclk_core
 * DSI: mipi_pixclk <= dclk_out <= dclk_core
 */
static unsigned long vop2_calc_cru_cfg(struct display_state *state,
				       int *dclk_core_div, int *dclk_out_div,
				       int *if_pixclk_div, int *if_dclk_div)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct vop2 *vop2 = cstate->private;
	unsigned long v_pixclk = (unsigned long)mode->crtc_clock;
	unsigned long dclk_core_rate = v_pixclk >> 2;
	unsigned long dclk_rate = v_pixclk;
	unsigned long dclk_out_rate;
	u64 if_dclk_rate;
	u64 if_pixclk_rate;
	int output_type = conn_state->type;
	int output_mode = conn_state->output_mode;
	u32 K = 1;

	if ((((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) &&
	    output_mode == ROCKCHIP_OUT_MODE_YUV420) {
		(void)printf("Dual channel and YUV420 can't work together\n");
		return 0U;
	}

	if ((((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) ||
	    output_mode == ROCKCHIP_OUT_MODE_YUV420) {
		K = 2;
	}

	if (output_type == DRM_MODE_CONNECTOR_HDMIA) {
		/*
		 * K = 2: dclk_core = if_pixclk_rate > if_dclk_rate
		 * K = 1: dclk_core = hdmie_edp_dclk > if_pixclk_rate
		 */
		if ((((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) ||
		    output_mode == ROCKCHIP_OUT_MODE_YUV420) {
			dclk_rate = dclk_rate >> 1;
			K = 2;
		}
		if (cstate->dsc_enable != 0U) {
			if_pixclk_rate = cstate->dsc_cds_clk_rate << 1;
			if_dclk_rate = cstate->dsc_cds_clk_rate;
		} else {
			if_pixclk_rate = (dclk_core_rate << 1U) / K;
			if_dclk_rate = dclk_core_rate / K;
		}

		if (v_pixclk > VOP2_MAX_DCLK_RATE) {
			dclk_rate = vop2_calc_dclk(dclk_core_rate,
						   vop2->data->vp_data[cstate->crtc_id].max_dclk);
		}

		if (dclk_rate == 0U) {
			(void)printf("DP if_pixclk_rate out of range(max_dclk: %d KHZ, dclk_core: %lld KHZ)\n",
			       vop2->data->vp_data[cstate->crtc_id].max_dclk, if_pixclk_rate);
			return 0U;
		}
		*if_pixclk_div = (int)dclk_rate / (int)if_pixclk_rate;
		*if_dclk_div = (int)dclk_rate / (int)if_dclk_rate;
		*dclk_core_div = (int)dclk_rate / (int)dclk_core_rate;
		(void)printf("dclk:%lu,if_pixclk_div;%d,if_dclk_div:%d\n",
		       dclk_rate, *if_pixclk_div, *if_dclk_div);
	} else if (output_type == DRM_MODE_CONNECTOR_eDP) {
		/* edp_pixclk = edp_dclk > dclk_core */
		if_pixclk_rate = v_pixclk / K;
		dclk_rate = if_pixclk_rate * K;
		*dclk_core_div = (int)dclk_rate / (int)dclk_core_rate;
		*if_pixclk_div = (int)dclk_rate / (int)if_pixclk_rate;
		*if_dclk_div = *if_pixclk_div;
	} else if (output_type == DRM_MODE_CONNECTOR_DisplayPort) {
		dclk_out_rate = v_pixclk >> 2;
		dclk_out_rate = dclk_out_rate / K;

		dclk_rate = vop2_calc_dclk(dclk_out_rate,
					   vop2->data->vp_data[cstate->crtc_id].max_dclk);
		if (dclk_rate == 0U) {
			(void)printf("DP dclk_core out of range(max_dclk: %d KHZ, dclk_core: %ld KHZ)\n",
			       vop2->data->vp_data[cstate->crtc_id].max_dclk, dclk_core_rate);
			return 0U;
		}
		*dclk_out_div = (int)dclk_rate / (int)dclk_out_rate;
		*dclk_core_div = (int)dclk_rate / (int)dclk_core_rate;
	} else if (output_type == DRM_MODE_CONNECTOR_DSI) {
		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) {
			K = 2;
		}
		if (cstate->dsc_enable != 0U) {
			/* dsc output is 96bit, dsi input is 192 bit */
			if_pixclk_rate = cstate->dsc_cds_clk_rate >> 1;
		}
		/* dclk_core = dclk_out * K = if_pixclk * K = v_pixclk / 4 */
		dclk_out_rate = dclk_core_rate / K;
		/* dclk_rate = N * dclk_core_rate N = (1,2,4 ), we get a little factor here */
		dclk_rate = vop2_calc_dclk(dclk_out_rate,
					   vop2->data->vp_data[cstate->crtc_id].max_dclk);
		if (dclk_rate == 0U) {
			(void)printf("MIPI dclk out of range(max_dclk: %d KHZ, dclk_rate: %ld KHZ)\n",
			       vop2->data->vp_data[cstate->crtc_id].max_dclk, dclk_rate);
			return 0U;
		}

		if (cstate->dsc_enable != 0U) {
			dclk_rate /= cstate->dsc_slice_num;
		}

		*dclk_out_div = (int)dclk_rate / (int)dclk_out_rate;
		*dclk_core_div = (int)dclk_rate / (int)dclk_core_rate;
		*if_pixclk_div = 1;       /*mipi pixclk == dclk_out*/
		if (cstate->dsc_enable != 0U) {
			*if_pixclk_div = (int)((long)dclk_out_rate * 1000 / (long)if_pixclk_rate);
		}
	} else if (output_type == DRM_MODE_CONNECTOR_DPI) {
		dclk_rate = v_pixclk;
		*dclk_core_div = (int)dclk_rate / (int)dclk_core_rate;
	} else {
		/* Do nothing */
	}

	*if_pixclk_div = ilog2((u32)(*if_pixclk_div));
	*if_dclk_div = ilog2((u32)(*if_dclk_div));
	*dclk_core_div = ilog2((u32)(*dclk_core_div));
	*dclk_out_div = ilog2((u32)(*dclk_out_div));

	return dclk_rate;
}

static int vop2_calc_dsc_clk(struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct crtc_state *cstate = &state->crtc_state;
	u64 v_pixclk = (u64)mode->crtc_clock * 1000ULL; /* video timing pixclk */
	u8 k = 1;

	if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) {
		k = 2;
	}

	cstate->dsc_txp_clk_rate = v_pixclk;
	(void)do_div(cstate->dsc_txp_clk_rate, (u32)((u32)cstate->dsc_pixel_num * (u32)k));

	cstate->dsc_pxl_clk_rate = v_pixclk;
	(void)do_div(cstate->dsc_pxl_clk_rate, (u32)((u32)cstate->dsc_slice_num * (u32)k));

	/* dsc_cds = crtc_clock / (cds_dat_width / bits_per_pixel)
	 * cds_dat_width = 96;
	 * bits_per_pixel = [8-12];
	 * As cds clk is div from txp clk and only support 1/2/4 div,
	 * so when txp_clk is equal to v_pixclk, we set dsc_cds = crtc_clock / 4,
	 * otherwise dsc_cds = crtc_clock / 8;
	 */
	cstate->dsc_cds_clk_rate = v_pixclk / (cstate->dsc_txp_clk_rate == v_pixclk ? 4U : 8U);

	return 0;
}

static unsigned long rk3588_vop2_if_cfg(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct rockchip_dsc_sink_cap *dsc_sink_cap = &cstate->dsc_sink_cap;
	struct vop2 *vop2 = cstate->private;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	u16 hdisplay = (u16)mode->crtc_hdisplay;
	u32 output_if = (u32)conn_state->output_if;
	int if_pixclk_div = 0;
	int if_dclk_div = 0;
	unsigned long dclk_rate;
	u32 dclk_inv;
	u32 val;
	bool yc_swap;

	dclk_inv = (conn_state->bus_flags & (u32)DRM_BUS_FLAG_PIXDATA_DRIVE_NEGEDGE) != 0U ? 1U : 0U;
	if ((output_if & ((u32)VOP_OUTPUT_IF_HDMI0 | (u32)VOP_OUTPUT_IF_HDMI1)) != 0U) {
		val = (mode->flags & (u32)DRM_MODE_FLAG_NHSYNC) != 0U ? (u32)BIT((u32)HSYNC_POSITIVE) : 0U;
		val |= (mode->flags & (u32)DRM_MODE_FLAG_NVSYNC) != 0U ? (u32)BIT((u32)VSYNC_POSITIVE) : 0U;
	} else {
		val = (mode->flags & (u32)DRM_MODE_FLAG_NHSYNC) != 0U ? 0U : (u32)BIT((u32)HSYNC_POSITIVE);
		val |= (mode->flags & (u32)DRM_MODE_FLAG_NVSYNC) != 0U ? 0U : (u32)BIT((u32)VSYNC_POSITIVE);
	}

	if (cstate->dsc_enable != 0U) {
		u8 k = 1;

		if (vop2->data->nr_dscs == 0U) {
			(void)printf("Unsupported DSC\n");
			return 0;
		}

		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) {
			k = 2;
		}

		cstate->dsc_id = (output_if & ((u32)VOP_OUTPUT_IF_MIPI0 | (u32)VOP_OUTPUT_IF_HDMI0)) != 0U ? 0U : 1U;
		cstate->dsc_slice_num = (u8)(hdisplay / dsc_sink_cap->slice_width) / k;
		cstate->dsc_pixel_num = cstate->dsc_slice_num > 4U ? 4U : cstate->dsc_slice_num;

		(void)vop2_calc_dsc_clk(state);
		(void)printf("Enable DSC%d slice:%dx%d, slice num:%d\n",
		       cstate->dsc_id, dsc_sink_cap->slice_width,
		       dsc_sink_cap->slice_height, cstate->dsc_slice_num);
	}

	dclk_rate = vop2_calc_cru_cfg(state, &cstate->dclk_core_div, &cstate->dclk_out_div, &if_pixclk_div, &if_dclk_div);

	if ((output_if & VOP_OUTPUT_IF_RGB) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, 0x7, RK3588_RGB_EN_SHIFT,
				4, false);
		vop2_grf_writel(vop2, vop2->grf, RK3588_GRF_SOC_CON1, EN_MASK,
				RK3588_GRF_VOP_DCLK_INV_SEL_SHIFT, dclk_inv == 1U ? 0U: 1U);
	}

	if ((output_if & VOP_OUTPUT_IF_BT1120) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, 0x7, RK3588_RGB_EN_SHIFT,
				3, false);
		vop2_grf_writel(vop2, vop2->grf, RK3588_GRF_SOC_CON1, EN_MASK,
				RK3588_GRF_VOP_DCLK_INV_SEL_SHIFT, dclk_inv == 1U ? 0U: 1U);
		yc_swap = is_yc_swap(conn_state->bus_format);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK, RK3588_BT1120_YC_SWAP_SHIFT,
				yc_swap ? 1U : 0U, false);
	}

	if ((output_if & VOP_OUTPUT_IF_BT656) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, 0x7, RK3588_RGB_EN_SHIFT,
				2, false);
		vop2_grf_writel(vop2, vop2->grf, RK3588_GRF_SOC_CON1, EN_MASK,
				RK3588_GRF_VOP_DCLK_INV_SEL_SHIFT, dclk_inv == 1U ? 0U: 1U);
		yc_swap = is_yc_swap(conn_state->bus_format);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK, RK3588_BT656_YC_SWAP_SHIFT,
				yc_swap ? 1U : 0U, false);
	}

	if ((output_if & VOP_OUTPUT_IF_MIPI0) != 0U) {
		if (cstate->crtc_id == 2) {
			val = 0;
		} else {
			val = 1;
		}

		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_MIPI_DS_MODE) != 0U) {
			vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
					RK3588_MIPI_DSI0_MODE_SEL_SHIFT, 1, false);
		}

		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RK3588_MIPI0_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, 1, RK3588_MIPI0_MUX_SHIFT, val, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, MIPI0_PIXCLK_DIV_SHIFT,
				(u32)if_pixclk_div, false);

		if (conn_state->hold_mode) {
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
					EN_MASK, EDPI_TE_EN, !cstate->soft_te ? 1U : 0U, false);
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
					EN_MASK, EDPI_WMS_HOLD_EN, 1, false);
		}
	}

	if ((output_if & VOP_OUTPUT_IF_MIPI1) != 0U) {
		if (cstate->crtc_id == 2) {
			val = 0;
		} else if (cstate->crtc_id == 3) {
			val = 1;
		} else {
			val = 3; /*VP1*/
		}
		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_MIPI_DS_MODE) != 0U) {
			vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
					RK3588_MIPI_DSI1_MODE_SEL_SHIFT, 1, false);
		}

		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RK3588_MIPI1_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK, MIPI1_MUX_SHIFT,
				val, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, MIPI1_PIXCLK_DIV_SHIFT,
				(u32)if_pixclk_div, false);

		if (conn_state->hold_mode) {
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
					EN_MASK, EDPI_TE_EN, !cstate->soft_te ? 1U : 0U, false);
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
					EN_MASK, EDPI_WMS_HOLD_EN, 1, false);
		}
	}

	if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) {
		vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset, EN_MASK,
				MIPI_DUAL_EN_SHIFT, 1, false);
		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DATA_SWAP) != 0U) {
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
					EN_MASK, MIPI_DUAL_SWAP_EN_SHIFT, 1,
					false);
		}
		switch (conn_state->type) {
		case DRM_MODE_CONNECTOR_DisplayPort:
			vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
					RK3588_DP_DUAL_EN_SHIFT, 1, false);
			break;
		case DRM_MODE_CONNECTOR_eDP:
			vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
					RK3588_EDP_DUAL_EN_SHIFT, 1, false);
			break;
		case DRM_MODE_CONNECTOR_HDMIA:
			vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
					RK3588_HDMI_DUAL_EN_SHIFT, 1, false);
			break;
		case DRM_MODE_CONNECTOR_DSI:
			vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
					RK3568_MIPI_DUAL_EN_SHIFT, 1, false);
			break;
		default:
			(void)printf("Unsupported connector type[%d] for dual channel mode\n",
				     conn_state->type);
			break;
		}
	}

	if ((output_if & VOP_OUTPUT_IF_eDP0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RK3588_EDP0_EN_SHIFT,
				1U, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK, RK3588_HDMI_EDP0_MUX_SHIFT,
				(u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, HDMI_EDP0_DCLK_DIV_SHIFT,
				(u32)if_dclk_div, false);

		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, HDMI_EDP0_PIXCLK_DIV_SHIFT,
				(u32)if_pixclk_div, false);

		vop2_grf_writel(vop2, vop2->vop_grf, RK3588_GRF_VOP_CON2, EN_MASK,
				RK3588_GRF_EDP0_ENABLE_SHIFT, 1);
	}

	if ((output_if & VOP_OUTPUT_IF_eDP1) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RK3588_EDP1_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK, RK3588_HDMI_EDP1_MUX_SHIFT,
				(u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, HDMI_EDP1_DCLK_DIV_SHIFT,
				(u32)if_dclk_div, false);

		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, HDMI_EDP1_PIXCLK_DIV_SHIFT,
				(u32)if_pixclk_div, false);

		vop2_grf_writel(vop2, vop2->vop_grf, RK3588_GRF_VOP_CON2, EN_MASK,
				RK3588_GRF_EDP1_ENABLE_SHIFT, 1);
	}

	if ((output_if & VOP_OUTPUT_IF_HDMI0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RK3588_HDMI0_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK, RK3588_HDMI_EDP0_MUX_SHIFT,
				(u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, HDMI_EDP0_DCLK_DIV_SHIFT,
				(u32)if_dclk_div, false);

		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, HDMI_EDP0_PIXCLK_DIV_SHIFT,
				(u32)if_pixclk_div, false);

		vop2_grf_writel(vop2, vop2->vop_grf, RK3588_GRF_VOP_CON2, EN_MASK,
				RK3588_GRF_HDMITX0_ENABLE_SHIFT, 1);
		vop2_grf_writel(vop2, vop2->vo1_grf, RK3588_GRF_VO1_CON0,
				HDMI_SYNC_POL_MASK,
				HDMI0_SYNC_POL_SHIFT, val);
	}

	if ((output_if & VOP_OUTPUT_IF_HDMI1) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RK3588_HDMI1_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK, RK3588_HDMI_EDP1_MUX_SHIFT,
				(u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, HDMI_EDP1_DCLK_DIV_SHIFT,
				(u32)if_dclk_div, false);

		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, 3, HDMI_EDP1_PIXCLK_DIV_SHIFT,
				(u32)if_pixclk_div, false);

		vop2_grf_writel(vop2, vop2->vop_grf, RK3588_GRF_VOP_CON2, EN_MASK,
				RK3588_GRF_HDMITX1_ENABLE_SHIFT, 1);
		vop2_grf_writel(vop2, vop2->vo1_grf, RK3588_GRF_VO1_CON0,
				HDMI_SYNC_POL_MASK,
				HDMI1_SYNC_POL_SHIFT, val);
	}

	if ((output_if & VOP_OUTPUT_IF_DP0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK, RK3588_DP0_MUX_SHIFT,
				(u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, RK3588_IF_PIN_POL_MASK,
				RK3588_DP0_PIN_POL_SHIFT, val, false);
	}

	if ((output_if & VOP_OUTPUT_IF_DP1) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK, RK3588_DP1_MUX_SHIFT,
				(u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, RK3588_IF_PIN_POL_MASK,
				RK3588_DP1_PIN_POL_SHIFT, val, false);
	}

	vop2_mask_write(vop2, RK3588_VP0_CLK_CTRL + vp_offset, 0x3,
			DCLK_CORE_DIV_SHIFT, (u32)cstate->dclk_core_div, false);
	vop2_mask_write(vop2, RK3588_VP0_CLK_CTRL + vp_offset, 0x3,
			DCLK_OUT_DIV_SHIFT, (u32)cstate->dclk_out_div, false);

	return dclk_rate;
}

static unsigned long rk3576_vop2_if_cfg(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct vop2 *vop2 = cstate->private;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	u8 port_pix_rate = vop2->data->vp_data[cstate->crtc_id].pixel_rate;
	u32 output_if = (u32)conn_state->output_if;
	bool yc_swap;
	bool split_mode = ((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U ? true : false;
	u32 pix_half_rate = 0U, post_dclk_out_sel = 0U;
	u32 post_dclk_core_sel, interface_dclk_sel, interface_pix_clk_sel = 0U;
	bool double_pixel = (mode->flags & (u32)DRM_MODE_FLAG_DBLCLK) != 0U ||
			    ((u32)conn_state->output_if & VOP_OUTPUT_IF_BT656) != 0U;
	u32 dclk_in_rate, dclk_core_rate;
	u32 val;
	u32 dclk_inv;

	dclk_inv = (conn_state->bus_flags & (u32)DRM_BUS_FLAG_PIXDATA_DRIVE_NEGEDGE) != 0U ? 1U : 0U;
	if ((output_if & VOP_OUTPUT_IF_MIPI0) != 0U) {
		/*
		 * RK3576 DSI CTRL hsync/vsync polarity is positive and can't update,
		 * so set VOP hsync/vsync polarity as positive by default.
		 */
		val = (u32)BIT((u32)HSYNC_POSITIVE) | (u32)BIT((u32)VSYNC_POSITIVE);
	} else {
		val = ((mode->flags & (u32)DRM_MODE_FLAG_NHSYNC)) != 0U ? 0U : (u32)BIT((u32)HSYNC_POSITIVE);
		val |= ((mode->flags & (u32)DRM_MODE_FLAG_NVSYNC)) != 0U ? 0U : (u32)BIT((u32)VSYNC_POSITIVE);
	}

	if ((u32)cstate->crtc_id > (u32)VOP2_MAX_VP - 1U) {
		return 0U;
	}

	if ((u32)conn_state->output_mode == (u32)ROCKCHIP_OUT_MODE_YUV420 ||
	    (u32)mode->crtc_clock > VOP2_MAX_DCLK_RATE || (cstate->crtc_id == 0 && split_mode)) {
		cstate->crtc->vps[cstate->crtc_id].dclk_div = 2; /* div2 */
	} else {
		cstate->crtc->vps[cstate->crtc_id].dclk_div = 1; /* no div */
	}
	dclk_in_rate = (u32)mode->crtc_clock / cstate->crtc->vps[cstate->crtc_id].dclk_div;

	if (double_pixel) {
		dclk_core_rate = (u32)mode->crtc_clock / 2U;
	} else {
		dclk_core_rate = (u32)mode->crtc_clock / port_pix_rate;
	}
	post_dclk_core_sel = dclk_in_rate > dclk_core_rate ? 1U : 0U; /* 0: no div, 1: div2 */

	if (split_mode || conn_state->output_mode == ROCKCHIP_OUT_MODE_YUV420) {
		pix_half_rate = 1U;
		post_dclk_out_sel = 1U;
	}

	if ((output_if & VOP_OUTPUT_IF_RGB) != 0U) {
		interface_dclk_sel = pix_half_rate == 1U ? 1U : 0U;
		/*
		 * RGB interface_pix_clk_sel will auto config according
		 * to rgb_en/bt1120_en/bt656_en.
		 */
	} else if ((output_if & VOP_OUTPUT_IF_eDP0) != 0U) {
		interface_dclk_sel = pix_half_rate == 1U ? 1U : 0U;
		interface_pix_clk_sel = port_pix_rate == 2U ? 1U : 0U;
	} else {
		interface_dclk_sel = pix_half_rate == 1U ? 1U : 0U;
		interface_pix_clk_sel = port_pix_rate == 1U ? 1U : 0U;
	}

	/* dclk_core */
	vop2_mask_write(vop2, RK3568_VP0_DCLK_SEL + vp_offset, EN_MASK,
			RK3576_DCLK_CORE_SEL_SHIFT, post_dclk_core_sel, false);
	/* dclk_out */
	vop2_mask_write(vop2, RK3568_VP0_DCLK_SEL + vp_offset, EN_MASK,
			RK3576_DCLK_OUT_SEL_SHIFT, post_dclk_out_sel, false);

	if ((output_if & VOP_OUTPUT_IF_RGB) != 0U) {
		/* 0: dclk_core, 1: dclk_out */
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, RK3576_IF_DCLK_SEL_MASK,
				RK3576_IF_DCLK_SEL_SHIFT, interface_dclk_sel, false);

		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_IF_REGDONE_IMD_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_IF_CLK_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_IF_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, RK3576_IF_PORT_SEL_MASK,
				RK3576_IF_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, RK3576_IF_PIN_POL_MASK,
				RK3576_IF_PIN_POL_SHIFT, val, false);
		vop2_grf_writel(vop2, vop2->ioc_grf, RK3576_VCCIO_IOC_MISC_CON8, EN_MASK,
				RK3576_IOC_VOP_DCLK_INV_SEL_SHIFT, dclk_inv);
	}

	if ((output_if & VOP_OUTPUT_IF_BT1120) != 0U) {
		/* 0: dclk_core, 1: dclk_out */
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, RK3576_IF_DCLK_SEL_MASK,
				RK3576_IF_DCLK_SEL_SHIFT, interface_dclk_sel, false);

		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_IF_REGDONE_IMD_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_IF_CLK_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_IF_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_BT1120_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, RK3576_IF_PORT_SEL_MASK,
				RK3576_IF_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
		vop2_grf_writel(vop2, vop2->ioc_grf, RK3576_VCCIO_IOC_MISC_CON8, EN_MASK,
				RK3576_IOC_VOP_DCLK_INV_SEL_SHIFT, dclk_inv == 1U ? 0U: 1U);
		yc_swap = is_yc_swap(conn_state->bus_format);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_BT1120_YC_SWAP_SHIFT, yc_swap ? 1U : 0U, false);
	}

	if ((output_if & VOP_OUTPUT_IF_BT656) != 0U) {
		/* 0: dclk_core, 1: dclk_out */
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, RK3576_IF_DCLK_SEL_MASK,
				RK3576_IF_DCLK_SEL_SHIFT, interface_dclk_sel, false);

		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_IF_REGDONE_IMD_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_IF_CLK_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_IF_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_BT656_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, RK3576_IF_PORT_SEL_MASK,
				RK3576_IF_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
		vop2_grf_writel(vop2, vop2->ioc_grf, RK3576_VCCIO_IOC_MISC_CON8, EN_MASK,
				RK3576_IOC_VOP_DCLK_INV_SEL_SHIFT, dclk_inv == 1U ? 0U: 1U);
		yc_swap = is_yc_swap(conn_state->bus_format);
		vop2_mask_write(vop2, RK3576_RGB_IF_CTRL, EN_MASK,
				RK3576_BT656_YC_SWAP_SHIFT, yc_swap ? 1U : 0U, false);
	}

	if ((output_if & VOP_OUTPUT_IF_MIPI0) != 0U) {
		vop2_mask_write(vop2, RK3576_MIPI0_IF_CTRL, RK3576_IF_DCLK_SEL_MASK,
				RK3576_IF_DCLK_SEL_SHIFT, interface_dclk_sel, false);
		/* 0: div2, 1: div4 */
		vop2_mask_write(vop2, RK3576_MIPI0_IF_CTRL, RK3576_IF_PIX_CLK_SEL_MASK,
				RK3576_IF_PIX_CLK_SEL_SHIFT, interface_pix_clk_sel, false);

		vop2_mask_write(vop2, RK3576_MIPI0_IF_CTRL, EN_MASK,
				RK3576_IF_REGDONE_IMD_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_MIPI0_IF_CTRL, EN_MASK,
				RK3576_IF_CLK_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_MIPI0_IF_CTRL, EN_MASK,
				RK3576_IF_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_MIPI0_IF_CTRL, RK3576_IF_PORT_SEL_MASK,
				RK3576_IF_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
		/*
		 * RK3576 DSI CTRL hsync/vsync polarity is positive and can't update,
		 * so set VOP hsync/vsync polarity as positive by default.
		 */
		if (vop2->version == VOP_VERSION_RK3576) {
			val = (u32)BIT((u32)HSYNC_POSITIVE) | (u32)BIT((u32)VSYNC_POSITIVE);
		}
		vop2_mask_write(vop2, RK3576_MIPI0_IF_CTRL, RK3576_IF_PIN_POL_MASK,
				RK3576_IF_PIN_POL_SHIFT, val, false);

		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_MIPI_DS_MODE) != 0U) {
			vop2_mask_write(vop2, RK3576_MIPI0_IF_CTRL, EN_MASK,
					RK3576_MIPI_CMD_MODE_SHIFT, 1, false);
		}

		if (conn_state->hold_mode) {
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset, EN_MASK,
					EDPI_TE_EN, cstate->soft_te ? 0U : 1U, false);
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset, EN_MASK,
					EDPI_WMS_HOLD_EN, 1, false);
		}
	}

	if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) {
		vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset, EN_MASK,
				MIPI_DUAL_EN_SHIFT, 1, false);
		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DATA_SWAP) != 0U) {
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset, EN_MASK,
					MIPI_DUAL_SWAP_EN_SHIFT, 1, false);
		}
		switch (conn_state->type) {
		case DRM_MODE_CONNECTOR_DisplayPort:
			vop2_mask_write(vop2, RK3576_DP0_IF_CTRL, EN_MASK,
					RK3576_IF_SPLIT_EN_SHIFT, 1, false);
			break;
		case DRM_MODE_CONNECTOR_eDP:
			vop2_mask_write(vop2, RK3576_EDP0_IF_CTRL, EN_MASK,
					RK3576_IF_SPLIT_EN_SHIFT, 1, false);
			break;
		case DRM_MODE_CONNECTOR_HDMIA:
			vop2_mask_write(vop2, RK3576_HDMI0_IF_CTRL, EN_MASK,
					RK3576_IF_SPLIT_EN_SHIFT, 1, false);
			break;
		case DRM_MODE_CONNECTOR_DSI:
			vop2_mask_write(vop2, RK3576_MIPI0_IF_CTRL, EN_MASK,
					RK3576_IF_SPLIT_EN_SHIFT, 1, false);
			break;
		default:
			(void)printf("Unsupported connector type[%d] for dual channel mode\n",
				     conn_state->type);
			break;
		}
	}

	if ((output_if & VOP_OUTPUT_IF_eDP0) != 0U) {
		vop2_mask_write(vop2, RK3576_EDP0_IF_CTRL, RK3576_IF_DCLK_SEL_MASK,
				RK3576_IF_DCLK_SEL_SHIFT, interface_dclk_sel, false);
		/* 0: dclk, 1: port0_dclk */
		vop2_mask_write(vop2, RK3576_EDP0_IF_CTRL, RK3576_IF_PIX_CLK_SEL_MASK,
				RK3576_IF_PIX_CLK_SEL_SHIFT, interface_pix_clk_sel, false);

		vop2_mask_write(vop2, RK3576_EDP0_IF_CTRL, EN_MASK,
				RK3576_IF_REGDONE_IMD_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_EDP0_IF_CTRL, EN_MASK,
				RK3576_IF_CLK_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_EDP0_IF_CTRL, EN_MASK,
				RK3576_IF_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_EDP0_IF_CTRL, RK3576_IF_PORT_SEL_MASK,
				RK3576_IF_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3576_EDP0_IF_CTRL, RK3576_IF_PIN_POL_MASK,
				RK3576_IF_PIN_POL_SHIFT, val, false);
	}

	if ((output_if & VOP_OUTPUT_IF_HDMI0) != 0U) {
		vop2_mask_write(vop2, RK3576_HDMI0_IF_CTRL, RK3576_IF_DCLK_SEL_MASK,
				RK3576_IF_DCLK_SEL_SHIFT, interface_dclk_sel, false);
		/* 0: div2, 1: div4 */
		vop2_mask_write(vop2, RK3576_HDMI0_IF_CTRL, RK3576_IF_PIX_CLK_SEL_MASK,
				RK3576_IF_PIX_CLK_SEL_SHIFT, interface_pix_clk_sel, false);

		vop2_mask_write(vop2, RK3576_HDMI0_IF_CTRL, EN_MASK,
				RK3576_IF_REGDONE_IMD_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_HDMI0_IF_CTRL, EN_MASK,
				RK3576_IF_CLK_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_HDMI0_IF_CTRL, EN_MASK,
				RK3576_IF_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_HDMI0_IF_CTRL, RK3576_IF_PORT_SEL_MASK,
				RK3576_IF_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3576_HDMI0_IF_CTRL, RK3576_IF_PIN_POL_MASK,
				RK3576_IF_PIN_POL_SHIFT, val, false);
	}

	if ((output_if & VOP_OUTPUT_IF_DP0) != 0U) {
		vop2_mask_write(vop2, RK3576_DP0_IF_CTRL, RK3576_IF_DCLK_SEL_MASK,
				RK3576_IF_DCLK_SEL_SHIFT, interface_dclk_sel, false);
		/* 0: no div, 1: div2 */
		vop2_mask_write(vop2, RK3576_DP0_IF_CTRL, RK3576_IF_PIX_CLK_SEL_MASK,
				RK3576_IF_PIX_CLK_SEL_SHIFT, interface_pix_clk_sel, false);

		vop2_mask_write(vop2, RK3576_DP0_IF_CTRL, EN_MASK,
				RK3576_IF_REGDONE_IMD_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_DP0_IF_CTRL, EN_MASK,
				RK3576_IF_CLK_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_DP0_IF_CTRL, RK3576_IF_PORT_SEL_MASK,
				RK3576_IF_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3576_DP0_IF_CTRL, RK3576_IF_PIN_POL_MASK,
				RK3576_IF_PIN_POL_SHIFT, val, false);
	}

	if ((output_if & VOP_OUTPUT_IF_DP1) != 0U) {
		vop2_mask_write(vop2, RK3576_DP1_IF_CTRL, RK3576_IF_DCLK_SEL_MASK,
				RK3576_IF_DCLK_SEL_SHIFT, interface_dclk_sel, false);
		/* 0: no div, 1: div2 */
		vop2_mask_write(vop2, RK3576_DP1_IF_CTRL, RK3576_IF_PIX_CLK_SEL_MASK,
				RK3576_IF_PIX_CLK_SEL_SHIFT, interface_pix_clk_sel, false);

		vop2_mask_write(vop2, RK3576_DP1_IF_CTRL, EN_MASK,
				RK3576_IF_REGDONE_IMD_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_DP1_IF_CTRL, EN_MASK,
				RK3576_IF_CLK_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_DP1_IF_CTRL, RK3576_IF_PORT_SEL_MASK,
				RK3576_IF_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3576_DP1_IF_CTRL, RK3576_IF_PIN_POL_MASK,
				RK3576_IF_PIN_POL_SHIFT, val, false);
	}

	if ((output_if & VOP_OUTPUT_IF_DP2) != 0U) {
		vop2_mask_write(vop2, RK3576_DP2_IF_CTRL, RK3576_IF_DCLK_SEL_MASK,
				RK3576_IF_DCLK_SEL_SHIFT, interface_dclk_sel, false);
		/* 0: no div, 1: div2 */
		vop2_mask_write(vop2, RK3576_DP2_IF_CTRL, RK3576_IF_PIX_CLK_SEL_MASK,
				RK3576_IF_PIX_CLK_SEL_SHIFT, interface_pix_clk_sel, false);

		vop2_mask_write(vop2, RK3576_DP2_IF_CTRL, EN_MASK,
				RK3576_IF_REGDONE_IMD_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_DP2_IF_CTRL, EN_MASK,
				RK3576_IF_CLK_OUT_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3576_DP2_IF_CTRL, RK3576_IF_PORT_SEL_MASK,
				RK3576_IF_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3576_DP2_IF_CTRL, RK3576_IF_PIN_POL_MASK,
				RK3576_IF_PIN_POL_SHIFT, val, false);
	}

	return (u32)mode->crtc_clock;
}

static void rk3568_vop2_setup_dual_channel_if(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct vop2 *vop2 = cstate->private;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);

	if (((u32)conn_state->output_flags &
	     (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_ODD_EVEN_MODE) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
				LVDS_DUAL_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
				LVDS_DUAL_LEFT_RIGHT_EN_SHIFT, 0, false);
		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DATA_SWAP) != 0U) {
			vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
					LVDS_DUAL_SWAP_EN_SHIFT, 1, false);
		}

		return;
	}

	vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset, EN_MASK,
			MIPI_DUAL_EN_SHIFT, 1, false);
	if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DATA_SWAP) != 0U) {
		vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset, EN_MASK,
				MIPI_DUAL_SWAP_EN_SHIFT, 1, false);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_LVDS1) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
				LVDS_DUAL_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_CTRL, EN_MASK,
				LVDS_DUAL_LEFT_RIGHT_EN_SHIFT, 1, false);
	}
}

static unsigned long rk3568_vop2_if_cfg(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct vop2 *vop2 = cstate->private;
	u32 dclk_inv;
	u32 val;

	dclk_inv = (conn_state->bus_flags & (u32)DRM_BUS_FLAG_PIXDATA_DRIVE_NEGEDGE) != 0U ? 1U : 0U;
	val = (mode->flags & (u32)DRM_MODE_FLAG_NHSYNC) != 0U ? 0U : (u32)BIT((u32)HSYNC_POSITIVE);
	val |= (mode->flags & (u32)DRM_MODE_FLAG_NVSYNC) != 0U ? 0U : (u32)BIT((u32)VSYNC_POSITIVE);

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_RGB) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RGB_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				RGB_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, IF_CTRL_RGB_LVDS_PIN_POL_MASK,
				IF_CTRL_RGB_LVDS_PIN_POL_SHIFT, val, false);
		vop2_grf_writel(vop2, vop2->grf, RK3568_GRF_VO_CON1, EN_MASK,
				GRF_RGB_DCLK_INV_SHIFT, dclk_inv);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_BT1120) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RGB_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK,
				BT1120_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				RGB_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_grf_writel(vop2, vop2->grf, RK3568_GRF_VO_CON1, EN_MASK,
				GRF_BT1120_CLK_INV_SHIFT, dclk_inv == 1U ? 0U : 1U);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_BT656) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, BT656_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				RGB_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_grf_writel(vop2, vop2->grf, RK3568_GRF_VO_CON1, EN_MASK,
				GRF_BT656_CLK_INV_SHIFT, dclk_inv == 1U ? 0U : 1U);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_LVDS0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, LVDS0_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				LVDS0_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, IF_CTRL_RGB_LVDS_PIN_POL_MASK,
				IF_CTRL_RGB_LVDS_PIN_POL_SHIFT, val, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
				IF_CTRL_RGB_LVDS_DCLK_POL_SHIFT, dclk_inv, false);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_LVDS1) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, LVDS1_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				LVDS1_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, IF_CTRL_RGB_LVDS_PIN_POL_MASK,
				IF_CTRL_RGB_LVDS_PIN_POL_SHIFT, val, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
				IF_CTRL_RGB_LVDS_DCLK_POL_SHIFT, dclk_inv, false);
	}


	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_MIPI0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, MIPI0_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				MIPI0_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
				IF_CRTL_MIPI_DCLK_POL_SHIT, dclk_inv, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, IF_CTRL_MIPI_PIN_POL_MASK,
				IF_CTRL_MIPI_PIN_POL_SHIFT, val, false);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_MIPI1) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, MIPI1_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				MIPI1_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
				IF_CRTL_MIPI_DCLK_POL_SHIT, dclk_inv, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, IF_CTRL_MIPI_PIN_POL_MASK,
				IF_CTRL_MIPI_PIN_POL_SHIFT, val, false);
	}

	if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U ||
	    ((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_ODD_EVEN_MODE) != 0U) {
		rk3568_vop2_setup_dual_channel_if(state);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_eDP0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, EDP0_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				EDP0_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
				IF_CRTL_EDP_DCLK_POL_SHIT, dclk_inv, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, IF_CTRL_EDP_PIN_POL_MASK,
				IF_CTRL_EDP_PIN_POL_SHIFT, val, false);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_HDMI0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, HDMI0_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				HDMI0_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
				IF_CRTL_HDMI_DCLK_POL_SHIT, 1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL,
				IF_CRTL_HDMI_PIN_POL_MASK,
				IF_CRTL_HDMI_PIN_POL_SHIT, val, false);
	}

	return (u32)mode->crtc_clock;
}

static unsigned long rk3562_vop2_if_cfg(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct vop2 *vop2 = cstate->private;
	u32 dclk_inv;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	u32 val;

	dclk_inv = (conn_state->bus_flags & (u32)DRM_BUS_FLAG_PIXDATA_DRIVE_NEGEDGE) != 0U ? 1U : 0U;
	val = (mode->flags & (u32)DRM_MODE_FLAG_NHSYNC) != 0U ? 0U : (u32)BIT((u32)HSYNC_POSITIVE);
	val |= (mode->flags & (u32)DRM_MODE_FLAG_NVSYNC) != 0U ? 0U : (u32)BIT((u32)VSYNC_POSITIVE);

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_RGB) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RGB_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				RGB_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_grf_writel(vop2, vop2->grf, RK3562_GRF_IOC_VO_IO_CON, EN_MASK,
				GRF_RGB_DCLK_INV_SHIFT, dclk_inv);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, RK3562_IF_PIN_POL_MASK,
				IF_CTRL_RGB_LVDS_PIN_POL_SHIFT, val, false);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_LVDS0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, LVDS0_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				LVDS0_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
				IF_CTRL_RGB_LVDS_DCLK_POL_SHIFT, dclk_inv, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, RK3562_IF_PIN_POL_MASK,
				IF_CTRL_RGB_LVDS_PIN_POL_SHIFT, val, false);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_MIPI0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, MIPI0_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				MIPI0_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
				RK3562_MIPI_DCLK_POL_SHIFT, dclk_inv, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, RK3562_IF_PIN_POL_MASK,
				RK3562_MIPI_PIN_POL_SHIFT, val, false);

		if (conn_state->hold_mode) {
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
					EN_MASK, EDPI_TE_EN, cstate->soft_te ? 0U : 1U, false);
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
					EN_MASK, EDPI_WMS_HOLD_EN, 1U, false);
		}
	}

	return (u32)mode->crtc_clock;
}

static unsigned long rk3528_vop2_if_cfg(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct vop2 *vop2 = cstate->private;
	u32 val;

	val = (mode->flags & (u32)DRM_MODE_FLAG_NHSYNC) != 0U ? 0U : (u32)BIT((u32)HSYNC_POSITIVE);
	val |= (mode->flags & (u32)DRM_MODE_FLAG_NVSYNC) != 0U ? 0U : (u32)BIT((u32)VSYNC_POSITIVE);

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_BT656) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, BT656_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				RGB_MUX_SHIFT, (u32)cstate->crtc_id, false);
	}

	if (((u32)conn_state->output_if & VOP_OUTPUT_IF_HDMI0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, HDMI0_EN_SHIFT,
				1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, IF_MUX_MASK,
				HDMI0_MUX_SHIFT, (u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL, EN_MASK,
				IF_CRTL_HDMI_DCLK_POL_SHIT, 1, false);
		vop2_mask_write(vop2, RK3568_DSP_IF_POL,
				IF_CRTL_HDMI_PIN_POL_MASK,
				IF_CRTL_HDMI_PIN_POL_SHIT, val, false);
	}

	return (u32)mode->crtc_clock;
}

static void vop2_post_color_swap(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct vop2 *vop2 = cstate->private;
	int output_type = conn_state->type;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	u32 data_swap = 0;

	if (is_uv_swap(state) || is_rb_swap(state)) {
		data_swap = DSP_RB_SWAP;
	}

	if ((vop2->version == VOP_VERSION_RK3588 || vop2->version == VOP_VERSION_RK3576)) {
		if ((output_type == DRM_MODE_CONNECTOR_HDMIA ||
		     output_type == DRM_MODE_CONNECTOR_DisplayPort) &&
		    (conn_state->bus_format == MEDIA_BUS_FMT_YUV8_1X24 ||
		     conn_state->bus_format == MEDIA_BUS_FMT_YUV10_1X30)) {
			data_swap |= (u32)DSP_RG_SWAP;
		}
	}

	vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset,
			DATA_SWAP_MASK, DATA_SWAP_SHIFT, data_swap, false);
}

static void vop2_clk_set_parent(struct clk *clk, struct clk *parent)
{
	int ret;

	if (parent->dev) {
		ret = clk_set_parent(clk, parent);
		if (ret < 0) {
			(void)printf("failed to set %s as parent for %s\n",
			      parent->dev->name, clk->dev->name);
		}
	}
}

static ulong vop2_clk_set_rate(struct clk *clk, ulong rate)
{
	ulong ret = 0;

	if (clk->dev) {
		ret = clk_set_rate(clk, rate);
	}

	return ret;
}

static void vop2_calc_dsc_cru_cfg(struct display_state *state,
				  u64 *dsc_txp_clk_div, u64 *dsc_pxl_clk_div,
				  u64 *dsc_cds_clk_div, u64 dclk_rate)
{
	struct crtc_state *cstate = &state->crtc_state;

	*dsc_txp_clk_div = dclk_rate / cstate->dsc_txp_clk_rate;
	*dsc_pxl_clk_div = dclk_rate / cstate->dsc_pxl_clk_rate;
	*dsc_cds_clk_div = dclk_rate / cstate->dsc_cds_clk_rate;

	*dsc_txp_clk_div = ilog2((u32)(*dsc_txp_clk_div));
	*dsc_pxl_clk_div = ilog2((u32)(*dsc_pxl_clk_div));
	*dsc_cds_clk_div = ilog2((u32)(*dsc_cds_clk_div));
}

static void vop2_load_pps(struct display_state *state, struct vop2 *vop2, u8 dsc_id)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct drm_dsc_picture_parameter_set *pps = &cstate->pps;
	struct drm_dsc_picture_parameter_set config_pps;
	const struct vop2_data *vop2_data = vop2->data;
	const struct vop2_dsc_data *dsc_data = &vop2_data->dsc[dsc_id];
	void *pps_tmp = (void *)&config_pps;
	u32 *pps_val = (u32 *)pps_tmp;
	u32 decoder_regs_offset = ((u32)dsc_id * 0x100U);
	int i;

	(void)memcpy(&config_pps, pps, sizeof(config_pps));

	if ((config_pps.pps_3 & 0xfU) > dsc_data->max_linebuf_depth) {
		config_pps.pps_3 &= 0xf0U;
		config_pps.pps_3 |= dsc_data->max_linebuf_depth;
		(void)printf("DSC%d max_linebuf_depth is: %d, current set value is: %d\n",
		       dsc_id, dsc_data->max_linebuf_depth, config_pps.pps_3 & 0xfU);
	}

	for (i = 0; i < DSC_NUM_BUF_RANGES; i++) {
		config_pps.rc_range_parameters[i] =
			(pps->rc_range_parameters[i] >> 3U & 0x1fU) |
			((pps->rc_range_parameters[i] >> 14U) << 5U) |
			((pps->rc_range_parameters[i] >> 0U) << 7U) |
			((pps->rc_range_parameters[i] >> 8U & 0x3fU) << 10U);
	}

	for (i = 0; i < ROCKCHIP_DSC_PPS_SIZE_BYTE / 4; i++) {
		vop2_writel(vop2, RK3588_DSC_8K_PPS0_3 + decoder_regs_offset + (u32)i * 4U, *pps_val);
		pps_val++;
	}
}

static void vop2_dsc_enable(struct display_state *state, struct vop2 *vop2, u8 dsc_id, u64 dclk_rate)
{
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct crtc_state *cstate = &state->crtc_state;
	struct rockchip_dsc_sink_cap *dsc_sink_cap = &cstate->dsc_sink_cap;
	const struct vop2_data *vop2_data = vop2->data;
	const struct vop2_dsc_data *dsc_data = &vop2_data->dsc[dsc_id];
	bool mipi_ds_mode = false;
	u8 dsc_interface_mode;
	u16 hsync_len = (u16)mode->crtc_hsync_end - (u16)mode->crtc_hsync_start;
	u16 hdisplay = (u16)mode->crtc_hdisplay;
	u16 htotal = (u16)mode->crtc_htotal;
	u16 hact_st = (u16)mode->crtc_htotal - (u16)mode->crtc_hsync_start;
	u16 vdisplay = (u16)mode->crtc_vdisplay;
	u16 vtotal = (u16)mode->crtc_vtotal;
	u16 vsync_len = (u16)mode->crtc_vsync_end - (u16)mode->crtc_vsync_start;
	u16 vact_st = (u16)mode->crtc_vtotal - (u16)mode->crtc_vsync_start;
	u16 vact_end = (u16)vact_st + vdisplay;
	u32 ctrl_regs_offset = ((u32)dsc_id * 0x30U);
	u32 decoder_regs_offset = ((u32)dsc_id * 0x100U);
	u64 dsc_txp_clk_div = 0;
	u64 dsc_pxl_clk_div = 0;
	u64 dsc_cds_clk_div = 0;
	u32 val;

	if (vop2->data->nr_dscs == 0U) {
		(void)printf("Unsupported DSC\n");
		return;
	}

	if (cstate->dsc_slice_num > dsc_data->max_slice_num) {
		(void)printf("DSC%d supported max slice is: %d, current is: %d\n",
		       dsc_data->id, dsc_data->max_slice_num, cstate->dsc_slice_num);
	}

	if (dsc_data->pd_id != 0U) {
		if (vop2_power_domain_on(vop2, dsc_data->pd_id) != 0) {
			(void)printf("open dsc%d pd fail\n", dsc_id);
		}
	}

	vop2_mask_write(vop2, RK3588_DSC_8K_INIT_DLY + ctrl_regs_offset, EN_MASK,
			SCAN_TIMING_PARA_IMD_EN_SHIFT, 1, false);
	vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, DSC_PORT_SEL_MASK,
			DSC_PORT_SEL_SHIFT, (u32)cstate->crtc_id, false);
	if (((u32)conn_state->output_if & ((u32)VOP_OUTPUT_IF_HDMI0 | (u32)VOP_OUTPUT_IF_HDMI1)) != 0U) {
		dsc_interface_mode = (u8)VOP_DSC_IF_HDMI;
	} else {
		mipi_ds_mode = ((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_MIPI_DS_MODE) != 0U;
		if (mipi_ds_mode) {
			dsc_interface_mode = (u8)VOP_DSC_IF_MIPI_DS_MODE;
		} else {
			dsc_interface_mode = (u8)VOP_DSC_IF_MIPI_VIDEO_MODE;
		}
	}

	if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) {
		vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, DSC_MAN_MODE_MASK,
				DSC_MAN_MODE_SHIFT, 0, false);
	} else {
		vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, DSC_MAN_MODE_MASK,
				DSC_MAN_MODE_SHIFT, 1, false);
	}

	vop2_calc_dsc_cru_cfg(state, &dsc_txp_clk_div, &dsc_pxl_clk_div, &dsc_cds_clk_div, dclk_rate);

	vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, DSC_INTERFACE_MODE_MASK,
			DSC_INTERFACE_MODE_SHIFT, dsc_interface_mode, false);
	vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, DSC_PIXEL_NUM_MASK,
			DSC_PIXEL_NUM_SHIFT, ((u32)cstate->dsc_pixel_num >> 1U), false);
	vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, DSC_TXP_CLK_DIV_MASK,
			DSC_TXP_CLK_DIV_SHIFT, (u32)dsc_txp_clk_div, false);
	vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, DSC_PXL_CLK_DIV_MASK,
			DSC_PXL_CLK_DIV_SHIFT, (u32)dsc_pxl_clk_div, false);
	vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, DSC_CDS_CLK_DIV_MASK,
			DSC_CDS_CLK_DIV_SHIFT, (u32)dsc_cds_clk_div, false);
	vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, EN_MASK,
			DSC_SCAN_EN_SHIFT, mipi_ds_mode ? 0U : 1U, false);
	vop2_mask_write(vop2, RK3588_DSC_8K_SYS_CTRL + ctrl_regs_offset, DSC_CDS_CLK_DIV_MASK,
			DSC_HALT_EN_SHIFT, mipi_ds_mode ? 1U : 0U, false);

	if (!mipi_ds_mode) {
		u16 dsc_hsync, dsc_htotal, dsc_hact_st, dsc_hact_end;
		u16 target_bpp = dsc_sink_cap->target_bits_per_pixel_x16;
		u64 dsc_cds_rate = cstate->dsc_cds_clk_rate;
		u32 v_pixclk_mhz = (u32)mode->crtc_clock / 1000U; /* video timing pixclk */
		u32 dly_num, dsc_cds_rate_mhz, value;
		u8 k = 1;

		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) {
			k = 2;
		}

		if (target_bpp >> 4 < dsc_data->min_bits_per_pixel) {
			(void)printf("Unsupported bpp less than: %d\n", dsc_data->min_bits_per_pixel);
		}

		/*
		 * dly_num = delay_line_num * T(one-line) / T (dsc_cds)
		 * T (one-line) = 1/v_pixclk_mhz * htotal = htotal/v_pixclk_mhz
		 * T (dsc_cds) = 1 / dsc_cds_rate_mhz
		 *
		 * HDMI:
		 * delay_line_num: according the pps initial_xmit_delay to adjust vop dsc delay
		 *                 delay_line_num = 4 - BPP / 8
		 *                                = (64 - target_bpp / 8) / 16
		 * dly_num = htotal * dsc_cds_rate_mhz / v_pixclk_mhz * (64 - target_bpp / 8) / 16;
		 *
		 * MIPI DSI[4320 and 9216 is buffer size for DSC]:
		 * DSC0:delay_line_num = 4320 * 8 / slince_num / chunk_size;
		 *	delay_line_num = delay_line_num > 5 ? 5 : delay_line_num;
		 * DSC1:delay_line_num = 9216 * 2 / slince_num / chunk_size;
		 *	delay_line_num = delay_line_num > 5 ? 5 : delay_line_num;
		 * dly_num = htotal * dsc_cds_rate_mhz / v_pixclk_mhz * delay_line_num
		 */
		(void)do_div(dsc_cds_rate, 1000000U); /* hz to Mhz */
		dsc_cds_rate_mhz = (u32)dsc_cds_rate;
		dsc_hsync = hsync_len / 2U;
		if (dsc_interface_mode == (u8)VOP_DSC_IF_HDMI) {
			dly_num = (u32)htotal * dsc_cds_rate_mhz / v_pixclk_mhz * (u32)(64U - (u32)target_bpp / 8U) / 16U;
		} else {
			u32 dsc_buf_size  = dsc_id == 0U ? 4320U * 8U : 9216U * 2U;
			u32 delay_line_num = dsc_buf_size / cstate->dsc_slice_num /
					     be16_to_cpu(cstate->pps.chunk_size);

			delay_line_num = delay_line_num > 5U ? 5U : delay_line_num;
			dly_num = htotal * dsc_cds_rate_mhz / v_pixclk_mhz * delay_line_num;

			/* The dsc mipi video mode dsc_hsync minimum size is 8 pixels */
			if (dsc_hsync < 8U) {
				dsc_hsync = 8U;
			}
		}
		vop2_mask_write(vop2, RK3588_DSC_8K_INIT_DLY + ctrl_regs_offset, DSC_INIT_DLY_MODE_MASK,
				DSC_INIT_DLY_MODE_SHIFT, 0U, false);
		vop2_mask_write(vop2, RK3588_DSC_8K_INIT_DLY + ctrl_regs_offset, DSC_INIT_DLY_NUM_MASK,
				DSC_INIT_DLY_NUM_SHIFT, dly_num, false);

		/*
		 * htotal / dclk_core = dsc_htotal /cds_clk
		 *
		 * dclk_core = DCLK / (1 << dclk_core->div_val)
		 * cds_clk = txp_clk / (1 << dsc_cds_clk->div_val)
		 * txp_clk = DCLK / (1 << dsc_txp_clk->div_val)
		 *
		 * dsc_htotal = htotal * (1 << dclk_core->div_val) /
		 *              ((1 << dsc_txp_clk->div_val) * (1 << dsc_cds_clk->div_val))
		 */
		dsc_htotal = (u16)htotal * (u16)((u16)1U << (u16)cstate->dclk_core_div) /
			     ((u16)((u16)1U << (u16)dsc_txp_clk_div) * (u16)((u16)1U << (u16)dsc_cds_clk_div));
		value = (u32)(((u32)dsc_htotal << 16U) | (u32)dsc_hsync);
		vop2_mask_write(vop2, RK3588_DSC_8K_HTOTAL_HS_END + ctrl_regs_offset, DSC_HTOTAL_PW_MASK,
				DSC_HTOTAL_PW_SHIFT, value, false);

		dsc_hact_st = hact_st / 2U;
		dsc_hact_end = (hdisplay / k * target_bpp >> 4U) / 2U + dsc_hact_st;
		value = (u32)((u32)dsc_hact_end << 16U | (u32)dsc_hact_st);
		vop2_mask_write(vop2, RK3588_DSC_8K_HACT_ST_END + ctrl_regs_offset, DSC_HACT_ST_END_MASK,
				DSC_HACT_ST_END_SHIFT, value, false);

		vop2_mask_write(vop2, RK3588_DSC_8K_VTOTAL_VS_END + ctrl_regs_offset, DSC_VTOTAL_PW_MASK,
				DSC_VTOTAL_PW_SHIFT, (u32)vtotal << 16 | (u32)vsync_len, false);
		vop2_mask_write(vop2, RK3588_DSC_8K_VACT_ST_END + ctrl_regs_offset, DSC_VACT_ST_END_MASK,
				DSC_VACT_ST_END_SHIFT, (u32)vact_end << 16 | (u32)vact_st, false);
	}

	vop2_mask_write(vop2, RK3588_DSC_8K_RST + ctrl_regs_offset, RST_DEASSERT_MASK,
			RST_DEASSERT_SHIFT, 1, false);
	udelay(10);

	val = (u32)((u32)DSC_CTRL0_DEF_CON |
		     (u32)((u32)ilog2(cstate->dsc_slice_num) << (u32)DSC_NSLC_SHIFT) |
		     (u32)((u32)(dsc_sink_cap->version_minor == 2U ? 1U : 0U) << (u32)DSC_IFEP_SHIFT));
	vop2_writel(vop2, RK3588_DSC_8K_CTRL0 + decoder_regs_offset, val);

	vop2_load_pps(state, vop2, dsc_id);

	val |= (u32)((u32)1U << DSC_PPS_UPD_SHIFT);
	vop2_writel(vop2, RK3588_DSC_8K_CTRL0 + decoder_regs_offset, val);

	(void)printf("DSC%d: txp:%lld div:%lld, pxl:%lld div:%lld, dsc:%lld div:%lld\n",
	       dsc_id,
	       cstate->dsc_txp_clk_rate, dsc_txp_clk_div,
	       cstate->dsc_pxl_clk_rate, dsc_pxl_clk_div,
	       cstate->dsc_cds_clk_rate, dsc_cds_clk_div);
}

static bool is_extend_pll(struct display_state *state, struct udevice **clk_dev)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	struct udevice *vp_dev, *dev;
	struct ofnode_phandle_args args;
	char vp_name[10];
	bool phypll0_found, phypll1_found;
	int ret;

	if (vop2->version != VOP_VERSION_RK3588 && vop2->version != VOP_VERSION_RK3576) {
		return false;
	}

	(void)sprintf(vp_name, "port@%d", cstate->crtc_id);
	if (uclass_find_device_by_name(UCLASS_VIDEO_CRTC, vp_name, &vp_dev) != 0) {
		(void)printf("warn: can't get vp device\n");
		return false;
	}

	ret = dev_read_phandle_with_args(vp_dev, "assigned-clock-parents", "#clock-cells", 0,
					 0, &args);
	if (ret != 0) {
		(void)printf("assigned-clock-parents's node not define\n");
		return false;
	}

	if (uclass_find_device_by_ofnode(UCLASS_CLK, args.node, &dev) != 0) {
		(void)printf("warn: can't get clk device\n");
		return false;
	}

	phypll0_found = strncmp(dev->name, "hdmiphypll_clk0", 15) == 0;
	phypll1_found = strncmp(dev->name, "hdmiphypll_clk1", 15) == 0;
	if (phypll0_found || phypll1_found) {
		(void)printf("%s: clk dev :%s: vp port:%s\n", __func__, dev->name, vp_dev->name);
		if (clk_dev) {
			*clk_dev = dev;
		}
		return true;
	}

	return false;
}

static void vop3_mcu_mode_setup(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);

	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, EN_MASK,
			MCU_TYPE_SHIFT, 1, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, EN_MASK,
			MCU_HOLD_MODE_SHIFT, 1, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_PIX_TOTAL_MASK,
			MCU_PIX_TOTAL_SHIFT, (u32)cstate->mcu_timing.mcu_pix_total, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_CS_PST_MASK,
			MCU_CS_PST_SHIFT, (u32)cstate->mcu_timing.mcu_cs_pst, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_CS_PEND_MASK,
			MCU_CS_PEND_SHIFT, (u32)cstate->mcu_timing.mcu_cs_pend, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_RW_PST_MASK,
			MCU_RW_PST_SHIFT, (u32)cstate->mcu_timing.mcu_rw_pst, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_RW_PEND_MASK,
			MCU_RW_PEND_SHIFT, (u32)cstate->mcu_timing.mcu_rw_pend, false);
}

static void vop3_mcu_bypass_mode_setup(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);

	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, EN_MASK,
			MCU_TYPE_SHIFT, 1, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, EN_MASK,
			MCU_HOLD_MODE_SHIFT, 1, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_PIX_TOTAL_MASK,
			MCU_PIX_TOTAL_SHIFT, 53, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_CS_PST_MASK,
			MCU_CS_PST_SHIFT, 6, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_CS_PEND_MASK,
			MCU_CS_PEND_SHIFT, 48, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_RW_PST_MASK,
			MCU_RW_PST_SHIFT, 12, false);
	vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, MCU_RW_PEND_MASK,
			MCU_RW_PEND_SHIFT, 30, false);
}

static int rockchip_vop2_send_mcu_cmd(struct display_state *state, u32 type, u32 value)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct vop2 *vop2 = cstate->private;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);

	/*
	 * 1.set mcu bypass mode timing.
	 * 2.set dclk rate to 150M.
	 */
	if (type == (u32)MCU_SETBYPASS && value != 0U) {
		vop3_mcu_bypass_mode_setup(state);
		(void)vop2_clk_set_rate(&cstate->dclk, 150000000);
	}

	switch (type) {
	case (u32)MCU_WRCMD:
		vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, EN_MASK,
				MCU_RS_SHIFT, 0, false);
		vop2_mask_write(vop2, RK3562_VP0_MCU_RW_BYPASS_PORT + vp_offset,
				MCU_WRITE_DATA_BYPASS_MASK, MCU_WRITE_DATA_BYPASS_SHIFT,
				value, false);
		vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, EN_MASK,
				MCU_RS_SHIFT, 1, false);
		break;
	case (u32)MCU_WRDATA:
		vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, EN_MASK,
				MCU_RS_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3562_VP0_MCU_RW_BYPASS_PORT + vp_offset,
				MCU_WRITE_DATA_BYPASS_MASK, MCU_WRITE_DATA_BYPASS_SHIFT,
				value, false);
		break;
	case (u32)MCU_SETBYPASS:
		vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, EN_MASK,
				MCU_BYPASS_SHIFT, value != 0U ? 1U : 0U, false);
		break;
	default:
		(void)printf("Unsupported MCU cmd type\n");
		break;
	}

	/*
	 * 1.restore mcu data mode timing.
	 * 2.restore dclk rate to crtc_clock.
	 */
	if (type == (u32)MCU_SETBYPASS && value == 0U) {
		vop3_mcu_mode_setup(state);
		(void)vop2_clk_set_rate(&cstate->dclk, (u32)mode->crtc_clock * 1000UL);
	}

	return 0;
}

static void vop2_dither_setup(struct vop2 *vop2, int bus_format, int crtc_id)
{
	const struct vop2_data *vop2_data = vop2->data;
	const struct vop2_vp_data *vp_data = &vop2_data->vp_data[crtc_id];
	u32 vp_offset = (u32)crtc_id * 0x100U;
	bool pre_dither_down_en;

	switch (bus_format) {
	case MEDIA_BUS_FMT_RGB565_1X16:
	case MEDIA_BUS_FMT_RGB565_2X8_LE:
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				PRE_DITHER_DOWN_EN_SHIFT, 1U, false);
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				DITHER_DOWN_MODE_SHIFT, (u32)RGB888_TO_RGB565, false);
		pre_dither_down_en = true;
		break;
	case MEDIA_BUS_FMT_RGB666_1X18:
	case MEDIA_BUS_FMT_RGB666_1X24_CPADHI:
	case MEDIA_BUS_FMT_RGB666_1X7X3_SPWG:
	case MEDIA_BUS_FMT_RGB666_3X6:
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				PRE_DITHER_DOWN_EN_SHIFT, 1U, false);
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				DITHER_DOWN_MODE_SHIFT, (u32)RGB888_TO_RGB666, false);
		pre_dither_down_en = true;
		break;
	case MEDIA_BUS_FMT_YUYV8_1X16:
	case MEDIA_BUS_FMT_YUV8_1X24:
	case MEDIA_BUS_FMT_UYYVYY8_0_5X24:
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				PRE_DITHER_DOWN_EN_SHIFT, 0U, false);
		pre_dither_down_en = true;
		break;
	case MEDIA_BUS_FMT_YUYV10_1X20:
	case MEDIA_BUS_FMT_YUV10_1X30:
	case MEDIA_BUS_FMT_UYYVYY10_0_5X30:
	case MEDIA_BUS_FMT_RGB101010_1X30:
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				PRE_DITHER_DOWN_EN_SHIFT, 0U, false);
		pre_dither_down_en = false;
		break;
	case MEDIA_BUS_FMT_RGB888_3X8:
	case MEDIA_BUS_FMT_RGB888_DUMMY_4X8:
	case MEDIA_BUS_FMT_RGB888_1X24:
	case MEDIA_BUS_FMT_RGB888_1X7X4_SPWG:
	case MEDIA_BUS_FMT_RGB888_1X7X4_JEIDA:
	default:
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				PRE_DITHER_DOWN_EN_SHIFT, 0U, false);
		pre_dither_down_en = true;
		break;
	}

	if (is_yuv_output(bus_format) && (vp_data->feature & (u32)VOP_FEATURE_POST_FRC_V2) == 0U) {
		pre_dither_down_en = false;
	}

	if ((vp_data->feature & (u32)VOP_FEATURE_POST_FRC_V2) != 0U && pre_dither_down_en) {
		if (vop2->version == VOP_VERSION_RK3576) {
			vop2_writel(vop2, RK3576_VP0_POST_DITHER_FRC_0 + vp_offset, 0x00000000);
			vop2_writel(vop2, RK3576_VP0_POST_DITHER_FRC_1 + vp_offset, 0x01000100);
			vop2_writel(vop2, RK3576_VP0_POST_DITHER_FRC_2 + vp_offset, 0x04030100);
		}

		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				PRE_DITHER_DOWN_EN_SHIFT, 0U, false);
		/* enable frc2.0 do 10->8 */
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				DITHER_DOWN_EN_SHIFT, 1U, false);
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, DITHER_DOWN_SEL_MASK,
				DITHER_DOWN_SEL_SHIFT, (u32)DITHER_DOWN_FRC, false);
	} else {
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				PRE_DITHER_DOWN_EN_SHIFT, pre_dither_down_en ? 1U : 0U, false);
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, DITHER_DOWN_SEL_MASK,
				DITHER_DOWN_SEL_SHIFT, (u32)DITHER_DOWN_ALLEGRO, false);
	}
}

static int rockchip_vop2_init(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct rockchip_vp *vp;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct vop2 *vop2 = cstate->private;
	u16 hsync_len = (u16)mode->crtc_hsync_end - (u16)mode->crtc_hsync_start;
	u16 hdisplay = (u16)mode->crtc_hdisplay;
	u16 htotal = (u16)mode->crtc_htotal;
	u16 hact_st = (u16)mode->crtc_htotal - (u16)mode->crtc_hsync_start;
	u16 hact_end = hact_st + hdisplay;
	u16 vdisplay = (u16)mode->crtc_vdisplay;
	u16 vtotal = (u16)mode->crtc_vtotal;
	u16 vsync_len = (u16)mode->crtc_vsync_end - (u16)mode->crtc_vsync_start;
	u16 vact_st = (u16)mode->crtc_vtotal - (u16)mode->crtc_vsync_start;
	u16 vact_end = vact_st + vdisplay;
	bool yuv_overlay;
	u32 vp_offset;
	u32 line_flag_offset;
	u32 val, act_end;
	u32 cfg_done;
	u32 dclk_div_factor = 1;
	u8 vp_dclk_div = 1;
	char output_type_name[30] = {0};
#ifndef CONFIG_SPL_BUILD
	char dclk_name[9];
#endif
	struct clk hdmi0_phy_pll;
	struct clk hdmi1_phy_pll;
	struct clk hdmi_phy_pll;
	struct udevice *disp_dev;
	unsigned long dclk_rate = 0;
	unsigned long actual_rate;
	int ret;

	if (cstate->crtc_id < 0 || cstate->crtc_id > VOP2_MAX_VP - 1) {
		return -EINVAL;
	}
	vp = &cstate->crtc->vps[cstate->crtc_id];
	vp_offset = ((u32)cstate->crtc_id * 0x100U);
	line_flag_offset = ((u32)cstate->crtc_id * 4U);
	cfg_done = (u32)CFG_DONE_EN | (u32)BIT((u32)cstate->crtc_id) | (u32)(BIT((u32)cstate->crtc_id) << 16U);

	(void)printf("VOP update mode to: %dx%d%s%d, type:%s for VP%d\n",
	       mode->crtc_hdisplay, mode->vdisplay,
	       (mode->flags & (u32)DRM_MODE_FLAG_INTERLACE) != 0U ? "i" : "p",
	       mode->vrefresh,
	       rockchip_get_output_if_name((u32)conn_state->output_if, output_type_name),
	       cstate->crtc_id);

	if (mode->hdisplay > VOP2_MAX_VP_OUTPUT_WIDTH) {
		cstate->splice_mode = true;
		cstate->splice_crtc_id = vop2->data->vp_data[cstate->crtc_id].splice_vp_id;
		if (cstate->splice_crtc_id == 0U) {
			(void)printf("%s: Splice mode is unsupported by vp%d\n",
			       __func__, cstate->crtc_id);
			return -EINVAL;
		}

		vop2_mask_write(vop2, RK3568_SYS_LUT_PORT_SEL, EN_MASK,
				PORT_MERGE_EN_SHIFT, 1, false);
	}

	vop2_mask_write(vop2, RK3588_SYS_VAR_FREQ_CTRL, EN_MASK,
			(u32)RK3588_VP0_LINE_FLAG_OR_EN_SHIFT + (u32)cstate->crtc_id, 1, false);
	vop2_mask_write(vop2, RK3588_SYS_VAR_FREQ_CTRL, EN_MASK,
			(u32)RK3588_VP0_ALMOST_FULL_OR_EN_SHIFT + (u32)cstate->crtc_id, 1, false);

	if (vop2->data->vp_data[cstate->crtc_id].urgency) {
		u8 urgen_thl = vop2->data->vp_data[cstate->crtc_id].urgency->urgen_thl;
		u8 urgen_thh = vop2->data->vp_data[cstate->crtc_id].urgency->urgen_thh;

		vop2_mask_write(vop2, RK3576_SYS_AXI_HURRY_CTRL0_IMD, EN_MASK,
				(u32)AXI0_PORT_URGENCY_EN_SHIFT + (u32)cstate->crtc_id, 1, false);
		vop2_mask_write(vop2, RK3576_SYS_AXI_HURRY_CTRL1_IMD, EN_MASK,
				(u32)AXI1_PORT_URGENCY_EN_SHIFT + (u32)cstate->crtc_id, 1, false);
		vop2_mask_write(vop2, RK3568_VP0_COLOR_BAR_CTRL + vp_offset, EN_MASK,
				POST_URGENCY_EN_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3568_VP0_COLOR_BAR_CTRL + vp_offset, POST_URGENCY_THL_MASK,
				POST_URGENCY_THL_SHIFT, urgen_thl, false);
		vop2_mask_write(vop2, RK3568_VP0_COLOR_BAR_CTRL + vp_offset, POST_URGENCY_THH_MASK,
				POST_URGENCY_THH_SHIFT, urgen_thh, false);
	}

	(void)vop2_initial(vop2, state);
	if (vop2->version == VOP_VERSION_RK3588) {
		dclk_rate = rk3588_vop2_if_cfg(state);
	} else if (vop2->version == VOP_VERSION_RK3576) {
		dclk_rate = rk3576_vop2_if_cfg(state);
	} else if (vop2->version == VOP_VERSION_RK3568) {
		dclk_rate = rk3568_vop2_if_cfg(state);
	} else if (vop2->version == VOP_VERSION_RK3562) {
		dclk_rate = rk3562_vop2_if_cfg(state);
	} else if (vop2->version == VOP_VERSION_RK3528) {
		dclk_rate = rk3528_vop2_if_cfg(state);
	} else {
		/* Do nothing */
	}

	if ((conn_state->output_mode == ROCKCHIP_OUT_MODE_AAAA &&
	     (cstate->feature & (u32)VOP_FEATURE_OUTPUT_10BIT) == 0U) ||
	    ((u32)conn_state->output_if & (u32)VOP_OUTPUT_IF_BT656) != 0U) {
		conn_state->output_mode = ROCKCHIP_OUT_MODE_P888;
	}

	vop2_post_color_swap(state);

	vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, OUT_MODE_MASK,
			OUT_MODE_SHIFT, (u32)conn_state->output_mode, false);

	vop2_dither_setup(vop2, conn_state->bus_format, cstate->crtc_id);
	if (cstate->splice_mode) {
		vop2_dither_setup(vop2, conn_state->bus_format, (int)cstate->splice_crtc_id);
	}

	yuv_overlay = is_yuv_output(conn_state->bus_format);
	vop2_mask_write(vop2, RK3568_OVL_CTRL, EN_MASK, (u32)cstate->crtc_id,
			yuv_overlay ? 1U : 0U, false);

	cstate->yuv_overlay = yuv_overlay;

	vop2_writel(vop2, RK3568_VP0_DSP_HTOTAL_HS_END + vp_offset,
		    (u32)((u32)htotal << 16U) | (u32)hsync_len);
	val = (u32)hact_st << 16U;
	val |= (u32)hact_end;
	vop2_writel(vop2, RK3568_VP0_DSP_HACT_ST_END + vp_offset, val);
	val = (u32)vact_st << 16U;
	val |= (u32)vact_end;
	vop2_writel(vop2, RK3568_VP0_DSP_VACT_ST_END + vp_offset, val);
	if ((mode->flags & (u32)DRM_MODE_FLAG_INTERLACE) != 0U) {
		u16 vact_st_f1 = vtotal + vact_st + 1U;
		u16 vact_end_f1 = vact_st_f1 + vdisplay;

		val = (u32)vact_st_f1 << 16U | (u32)vact_end_f1;
		vop2_writel(vop2, RK3568_VP0_DSP_VACT_ST_END_F1 + vp_offset,
			    val);

		val = (u32)vtotal << 16U | ((u32)vtotal + (u32)vsync_len);
		vop2_writel(vop2, RK3568_VP0_DSP_VS_ST_END_F1 + vp_offset, val);
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				INTERLACE_EN_SHIFT, 1U, false);
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				DSP_FILED_POL, 1U, false);
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				P2I_EN_SHIFT, 1U, false);
		vtotal += vtotal + 1U;
		act_end = vact_end_f1;
	} else {
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				INTERLACE_EN_SHIFT, 0U, false);
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				P2I_EN_SHIFT, 0U, false);
		act_end = vact_end;
	}
	vop2_writel(vop2, RK3568_VP0_DSP_VTOTAL_VS_END + vp_offset,
		    ((u32)vtotal << 16U) | (u32)vsync_len);

	if (vop2->version == VOP_VERSION_RK3528 ||
	    vop2->version == VOP_VERSION_RK3562 ||
	    vop2->version == VOP_VERSION_RK3568) {
		if ((mode->flags & (u32)DRM_MODE_FLAG_DBLCLK) != 0U ||
		    ((u32)conn_state->output_if & (u32)VOP_OUTPUT_IF_BT656) != 0U) {
			vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
					CORE_DCLK_DIV_EN_SHIFT, 1U, false);
		} else {
			vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
					CORE_DCLK_DIV_EN_SHIFT, 0U, false);
		}

		if (conn_state->output_mode == ROCKCHIP_OUT_MODE_YUV420) {
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
					DCLK_DIV2_MASK, DCLK_DIV2_SHIFT, 0x3U, false);
		} else {
			vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
					DCLK_DIV2_MASK, DCLK_DIV2_SHIFT, 0U, false);
		}
	}

	vop2_mask_write(vop2, RK3568_OVL_CTRL, OVL_MODE_SEL_MASK,
			(u32)cstate->crtc_id, yuv_overlay ? 1U : 0U, false);

	if (yuv_overlay) {
		val = 0x20010200U;
	} else {
		val = 0U;
	}
	vop2_writel(vop2, RK3568_VP0_DSP_BG + vp_offset, val);
	if (cstate->splice_mode) {
		vop2_mask_write(vop2, RK3568_OVL_CTRL, OVL_MODE_SEL_MASK,
				(u32)cstate->splice_crtc_id,
				yuv_overlay ? 1U : 0U, false);
		vop2_writel(vop2, (u32)RK3568_VP0_DSP_BG + (u32)cstate->splice_crtc_id * 0x100U, val);
	}

	vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
			POST_DSP_OUT_R2Y_SHIFT, yuv_overlay ? 1U : 0U, false);

	if (vp->xmirror_en) {
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				DSP_X_MIR_EN_SHIFT, 1U, false);
	}

	vop2_tv_config_update(state, vop2);
	vop2_post_config(state, vop2);
	if ((cstate->feature & ((u32)VOP_FEATURE_POST_ACM | (u32)VOP_FEATURE_POST_CSC)) != 0U) {
		vop3_post_config(state, vop2);
	}

	if (cstate->dsc_enable != 0U) {
		if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) {
			vop2_dsc_enable(state, vop2, 0, dclk_rate * 1000ULL);
			vop2_dsc_enable(state, vop2, 1, dclk_rate * 1000ULL);
		} else {
			vop2_dsc_enable(state, vop2, cstate->dsc_id, dclk_rate * 1000ULL);
		}
	}

#ifndef CONFIG_SPL_BUILD
	(void)snprintf(dclk_name, sizeof(dclk_name), "dclk_vp%d", cstate->crtc_id);
	ret = clk_get_by_name(cstate->dev, dclk_name, &cstate->dclk);
	if (ret != 0) {
		(void)printf("%s: Failed to get dclk ret=%d\n", __func__, ret);
		return ret;
	}
#endif

	ret = uclass_get_device_by_name(UCLASS_VIDEO, "display-subsystem", &disp_dev);
	if (ret == 0) {
		ret = clk_get_by_name(disp_dev, "hdmi0_phy_pll", &hdmi0_phy_pll);
		if (ret != 0) {
			(void)printf("%s: hdmi0_phy_pll may not define\n", __func__);
		}
		ret = clk_get_by_name(disp_dev, "hdmi1_phy_pll", &hdmi1_phy_pll);
		if (ret != 0) {
			(void)printf("%s: hdmi1_phy_pll may not define\n", __func__);
		}
	} else {
		hdmi0_phy_pll.dev = NULL;
		hdmi1_phy_pll.dev = NULL;
		(void)printf("%s: Faile to find display-subsystem node\n", __func__);
	}

	if (vop2->version == VOP_VERSION_RK3528) {
		struct ofnode_phandle_args args;

		ret = dev_read_phandle_with_args(cstate->dev, "assigned-clock-parents",
						 "#clock-cells", 0, 0, &args);
		if (ret == 0) {
			ret = uclass_find_device_by_ofnode(UCLASS_CLK, args.node, &hdmi0_phy_pll.dev);
			if (ret != 0) {
				(void)printf("warn: can't get clk device\n");
				return ret;
			}
		} else {
			(void)printf("assigned-clock-parents's node not define\n");
		}
	}

	if (vop2->version == VOP_VERSION_RK3576) {
		if (cstate->crtc_id >= 0 && cstate->crtc_id < VOP2_MAX_VP) {
			vp_dclk_div = cstate->crtc->vps[cstate->crtc_id].dclk_div;
		}
	}

	if ((u32)mode->crtc_clock < VOP2_MAX_DCLK_RATE) {
		if (((u32)conn_state->output_if & (u32)VOP_OUTPUT_IF_HDMI0) != 0U) {
			vop2_clk_set_parent(&cstate->dclk, &hdmi0_phy_pll);
		} else if (((u32)conn_state->output_if & (u32)VOP_OUTPUT_IF_HDMI1) != 0U) {
			vop2_clk_set_parent(&cstate->dclk, &hdmi1_phy_pll);
		} else {
			/* Do nothing */
		}

		/*
		 * uboot clk driver won't set dclk parent's rate when use
		 * hdmi phypll as dclk source.
		 * So set dclk rate is meaningless. Set hdmi phypll rate
		 * directly.
		 */
		if (((u32)conn_state->output_if & (u32)VOP_OUTPUT_IF_HDMI0) != 0U && hdmi0_phy_pll.dev != NULL) {
			actual_rate = vop2_clk_set_rate(&hdmi0_phy_pll, dclk_rate / vp_dclk_div * 1000U);
		} else if (((u32)conn_state->output_if & (u32)VOP_OUTPUT_IF_HDMI1) != 0U && hdmi1_phy_pll.dev != NULL) {
			actual_rate = vop2_clk_set_rate(&hdmi1_phy_pll, dclk_rate / vp_dclk_div * 1000U);
		} else {
			if (is_extend_pll(state, &hdmi_phy_pll.dev)) {
				actual_rate = vop2_clk_set_rate(&hdmi_phy_pll,
								dclk_rate / vp_dclk_div * 1000U);
			} else {
#ifndef CONFIG_SPL_BUILD
				actual_rate = vop2_clk_set_rate(&cstate->dclk,
								dclk_rate / vp_dclk_div * 1000U);
#else
				if (vop2->version == VOP_VERSION_RK3528) {
					void *cru_base = (void *)RK3528_CRU_BASE;

					/* dclk src switch to hdmiphy pll */
					(void)writel((BIT(0) << 16) | BIT(0), cru_base + 0x450);
					rockchip_phy_set_pll(conn_state->connector->phy, dclk_rate * 1000U);
					actual_rate = dclk_rate * 1000U;
				}
#endif
			}
		}
	} else {
		if (is_extend_pll(state, &hdmi_phy_pll.dev)) {
			actual_rate = vop2_clk_set_rate(&hdmi_phy_pll, dclk_rate / vp_dclk_div * 1000U);
		} else {
			actual_rate = vop2_clk_set_rate(&cstate->dclk, dclk_rate / vp_dclk_div * 1000U);
		}
	}

	if (IS_ERR_VALUE(actual_rate) != 0) {
		(void)printf("%s: Failed to set vp%d dclk[%ld KHZ] ret=%ld\n",
		       __func__, cstate->crtc_id, dclk_rate, actual_rate);
		return (int)actual_rate;
	} else {
		if (cstate->mcu_timing.mcu_pix_total != 0) {
			mode->crtc_clock = (int)roundup((actual_rate), (1000UL)) / 1000;
		} else {
			if (dclk_rate != 0U) {
				dclk_div_factor = (u32)mode->crtc_clock / (u32)dclk_rate;
			}
			mode->crtc_clock = (int)roundup((actual_rate), (1000UL)) * (int)dclk_div_factor / 1000;
		}
		(void)printf("VP%d set crtc_clock to %dKHz\n", cstate->crtc_id, mode->crtc_clock);
	}

	vop2_mask_write(vop2, RK3568_SYS_CTRL_LINE_FLAG0 + line_flag_offset, LINE_FLAG_NUM_MASK,
			RK3568_DSP_LINE_FLAG_NUM0_SHIFT, act_end, false);
	vop2_mask_write(vop2, RK3568_SYS_CTRL_LINE_FLAG0 + line_flag_offset, LINE_FLAG_NUM_MASK,
			RK3568_DSP_LINE_FLAG_NUM1_SHIFT, act_end, false);

	if (cstate->mcu_timing.mcu_pix_total != 0) {
		vop2_writel(vop2, RK3568_REG_CFG_DONE, cfg_done);
		vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
				STANDBY_EN_SHIFT, 0, false);
		vop3_mcu_mode_setup(state);
	}

	return 0;
}

static void vop2_setup_scale(struct vop2 *vop2, struct vop2_win_data *win,
			     uint32_t src_w, uint32_t src_h, uint32_t dst_w,
			     uint32_t dst_h)
{
	enum scale_mode yrgb_hor_scl_mode, yrgb_ver_scl_mode;
	enum vop2_scale_down_mode filter_scl_down_mode;
	uint16_t hscl_filter_mode, vscl_filter_mode;
	uint8_t xgt2 = 0, xgt4 = 0;
	uint8_t ygt2 = 0, ygt4 = 0;
	uint32_t xfac, yfac;
	u32 win_offset = win->reg_offset;
	bool xgt_en = false;
	bool xavg_en = false;

	if (is_vop3(vop2)) {
		if (vop2->version == VOP_VERSION_RK3576 && win->type == CLUSTER_LAYER) {
			if (src_w >= (8U * dst_w)) {
				xgt4 = 1U;
				src_w >>= 2U;
			} else if (src_w >= (4U * dst_w)) {
				xgt2 = 1U;
				src_w >>= 1U;
			} else {
				/* Do nothing */
			}
		} else {
			if (src_w >= (4U * dst_w)) {
				xgt4 = 1U;
				src_w >>= 2U;
			} else if (src_w >= (2U * dst_w)) {
				xgt2 = 1U;
				src_w >>= 1U;
			} else {
				/* Do nothing */
			}
		}
	}

	/**
	 * The rk3528 is processed as 2 pixel/cycle,
	 * so ygt2/ygt4 needs to be triggered in advance to improve performance
	 * when src_w is bigger than 1920.
	 * dst_h / src_h is at [1, 0.65)     ygt2=0; ygt4=0;
	 * dst_h / src_h is at [0.65, 0.35)  ygt2=1; ygt4=0;
	 * dst_h / src_h is at [0.35, 0)     ygt2=0; ygt4=1;
	 */
	if (vop2->version == VOP_VERSION_RK3528 && src_w > 1920U) {
		if (src_h >= (100U * dst_h / 35U)) {
			ygt4 = 1U;
			src_h >>= 2U;
		} else if (src_h >= (100U * dst_h / 65U)) {
			ygt2 = 1U;
			src_h >>= 1U;
		} else {
			/* Do nothing */
		}
	} else {
		if (win->vsd_filter_mode == (u8)VOP2_SCALE_DOWN_ZME) {
			if (src_h >= (8U * dst_h)) {
				ygt4 = 1U;
				src_h >>= 2U;
			} else if (src_h >= (4U * dst_h)) {
				ygt2 = 1U;
				src_h >>= 1U;
			} else {
				/* Do nothing */
			}
		} else {
			if (src_h >= (4U * dst_h)) {
				ygt4 = 1U;
				src_h >>= 2U;
			} else if (src_h >= (2U * dst_h)) {
				ygt2 = 1U;
				src_h >>= 1U;
			} else {
				/* Do nothing */
			}
		}
	}

	yrgb_hor_scl_mode = scl_get_scl_mode(src_w, dst_w);
	yrgb_ver_scl_mode = scl_get_scl_mode(src_h, dst_h);

	if (yrgb_hor_scl_mode == SCALE_UP) {
		hscl_filter_mode = win->hsu_filter_mode;
	} else {
		hscl_filter_mode = win->hsd_filter_mode;
	}

	if (yrgb_ver_scl_mode == SCALE_UP) {
		vscl_filter_mode = win->vsu_filter_mode;
	} else {
		vscl_filter_mode = win->vsd_filter_mode;
	}

	/*
	 * RK3568 VOP Esmart/Smart dsp_w should be even pixel
	 * at scale down mode
	 */
	if ((yrgb_hor_scl_mode == SCALE_DOWN) && ((dst_w & 0x1U) != 0U) && !is_vop3(vop2)) {
		(void)printf("win dst_w[%d] should align as 2 pixel\n", dst_w);
		dst_w += 1U;
	}

	if (is_vop3(vop2)) {
		xfac = vop3_scale_factor(yrgb_hor_scl_mode, src_w, dst_w, true);
		yfac = vop3_scale_factor(yrgb_ver_scl_mode, src_h, dst_h, false);

		if (win->hsd_pre_filter_mode == (u8)VOP3_PRE_SCALE_DOWN_AVG) {
			xavg_en = xgt2 != 0U || xgt4 != 0U;
		} else {
			xgt_en = xgt2 != 0U || xgt4 != 0U;
		}

		if (vop2->version == VOP_VERSION_RK3576) {
			bool zme_dering_en = false;

			if ((yrgb_hor_scl_mode == SCALE_UP &&
			     hscl_filter_mode == (u16)VOP2_SCALE_UP_ZME) ||
			    (yrgb_ver_scl_mode == SCALE_UP &&
			     vscl_filter_mode == (u16)VOP2_SCALE_UP_ZME)) {
				zme_dering_en = true;
			}

			/* Recommended configuration from the algorithm */
			vop2_writel(vop2, RK3576_CLUSTER0_WIN0_ZME_DERING_PARA + win_offset,
				    0x04100d10);
			vop2_mask_write(vop2, RK3576_CLUSTER0_WIN0_ZME_CTRL + win_offset,
					EN_MASK, WIN0_ZME_DERING_EN_SHIFT, zme_dering_en ? 1U : 0U, false);
		}
	} else {
		if (yrgb_hor_scl_mode == SCALE_DOWN && hscl_filter_mode == (u16)VOP2_SCALE_DOWN_BIL) {
			filter_scl_down_mode = VOP2_SCALE_DOWN_BIL;
		} else {
			filter_scl_down_mode = VOP2_SCALE_DOWN_NRST_NBOR;
		}
		xfac = vop2_scale_factor(yrgb_hor_scl_mode, filter_scl_down_mode, src_w, dst_w);
		if (yrgb_ver_scl_mode == SCALE_DOWN && vscl_filter_mode == (u16)VOP2_SCALE_DOWN_BIL) {
			filter_scl_down_mode = VOP2_SCALE_DOWN_BIL;
		} else {
			filter_scl_down_mode = VOP2_SCALE_DOWN_NRST_NBOR;
		}
		yfac = vop2_scale_factor(yrgb_ver_scl_mode, filter_scl_down_mode, src_h, dst_h);
	}

	if (win->type == CLUSTER_LAYER) {
		vop2_writel(vop2, RK3568_CLUSTER0_WIN0_SCL_FACTOR_YRGB + win_offset,
			    yfac << 16 | xfac);

		if (is_vop3(vop2)) {
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					EN_MASK, CLUSTER_XGT_EN_SHIFT, xgt_en ? 1U : 0U, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					EN_MASK, CLUSTER_XAVG_EN_SHIFT, xavg_en ? 1U : 0U, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					XGT_MODE_MASK, CLUSTER_XGT_MODE_SHIFT, xgt2 == 1U ? 0U : 1U, false);

			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					YRGB_XSCL_MODE_MASK, RK3528_CLUSTER_YRGB_XSCL_MODE_SHIFT,
					(u32)yrgb_hor_scl_mode, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					YRGB_YSCL_MODE_MASK, RK3528_CLUSTER_YRGB_YSCL_MODE_SHIFT,
					(u32)yrgb_ver_scl_mode, false);
		} else {
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					YRGB_XSCL_MODE_MASK, RK3568_CLUSTER_YRGB_XSCL_MODE_SHIFT,
					(u32)yrgb_hor_scl_mode, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					YRGB_YSCL_MODE_MASK, RK3568_CLUSTER_YRGB_YSCL_MODE_SHIFT,
					(u32)yrgb_ver_scl_mode, false);
		}

		if (!is_vop3(vop2) || win->vsd_pre_filter_mode == (u8)VOP3_PRE_SCALE_DOWN_GT) {
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					YRGB_GT2_MASK, CLUSTER_YRGB_GT2_SHIFT, ygt2, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					YRGB_GT4_MASK, CLUSTER_YRGB_GT4_SHIFT, ygt4, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					AVG2_MASK, CLUSTER_AVG2_SHIFT, 0U, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					AVG4_MASK, CLUSTER_AVG4_SHIFT, 0U, false);
		} else {
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					YRGB_GT2_MASK, CLUSTER_YRGB_GT2_SHIFT, 0U, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					YRGB_GT4_MASK, CLUSTER_YRGB_GT4_SHIFT, 0U, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					AVG2_MASK, CLUSTER_AVG2_SHIFT, ygt2, false);
			vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL1 + win_offset,
					AVG4_MASK, CLUSTER_AVG4_SHIFT, ygt4, false);
		}
	} else {
		vop2_writel(vop2, RK3568_ESMART0_REGION0_SCL_FACTOR_YRGB + win_offset,
			    yfac << 16 | xfac);

		if (is_vop3(vop2)) {
			vop2_mask_write(vop2, RK3568_ESMART0_REGION0_CTRL + win_offset,
					EN_MASK, ESMART_XGT_EN_SHIFT, xgt_en ? 1U : 0U, false);
			vop2_mask_write(vop2, RK3568_ESMART0_REGION0_CTRL + win_offset,
					EN_MASK, ESMART_XAVG_EN_SHIFT, xavg_en ? 1U : 0U, false);
			vop2_mask_write(vop2, RK3568_ESMART0_REGION0_CTRL + win_offset,
					XGT_MODE_MASK, ESMART_XGT_MODE_SHIFT, xgt2 == 1U ? 0U : 1U, false);
		}

		vop2_mask_write(vop2, RK3568_ESMART0_REGION0_CTRL + win_offset,
				YRGB_GT2_MASK, YRGB_GT2_SHIFT, ygt2, false);
		vop2_mask_write(vop2, RK3568_ESMART0_REGION0_CTRL + win_offset,
				YRGB_GT4_MASK, YRGB_GT4_SHIFT, ygt4, false);

		vop2_mask_write(vop2, RK3568_ESMART0_REGION0_SCL_CTRL + win_offset,
				YRGB_XSCL_MODE_MASK, YRGB_XSCL_MODE_SHIFT, (u32)yrgb_hor_scl_mode, false);
		vop2_mask_write(vop2, RK3568_ESMART0_REGION0_SCL_CTRL + win_offset,
				YRGB_YSCL_MODE_MASK, YRGB_YSCL_MODE_SHIFT, (u32)yrgb_ver_scl_mode, false);

		vop2_mask_write(vop2, RK3568_ESMART0_REGION0_SCL_CTRL + win_offset,
				YRGB_XSCL_FILTER_MODE_MASK, YRGB_XSCL_FILTER_MODE_SHIFT,
				(u32)hscl_filter_mode, false);
		vop2_mask_write(vop2, RK3568_ESMART0_REGION0_SCL_CTRL + win_offset,
				YRGB_YSCL_FILTER_MODE_MASK, YRGB_YSCL_FILTER_MODE_SHIFT,
				(u32)vscl_filter_mode, false);
	}
}

static void vop2_axi_config(struct vop2 *vop2, struct vop2_win_data *win)
{
	u32 win_offset = win->reg_offset;

	if (win->type == CLUSTER_LAYER) {
		vop2_mask_write(vop2, RK3568_CLUSTER0_CTRL + win_offset, CLUSTER_AXI_ID_MASK,
				CLUSTER_AXI_ID_SHIFT, win->axi_id, false);
		vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL2 + win_offset, CLUSTER_AXI_YRGB_ID_MASK,
				CLUSTER_AXI_YRGB_ID_SHIFT, win->axi_yrgb_id, false);
		vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL2 + win_offset, CLUSTER_AXI_UV_ID_MASK,
				CLUSTER_AXI_UV_ID_SHIFT, win->axi_uv_id, false);
	} else {
		vop2_mask_write(vop2, RK3568_ESMART0_AXI_CTRL + win_offset, ESMART_AXI_ID_MASK,
				ESMART_AXI_ID_SHIFT, win->axi_id, false);
		vop2_mask_write(vop2, RK3568_ESMART0_CTRL1 + win_offset, ESMART_AXI_YRGB_ID_MASK,
				ESMART_AXI_YRGB_ID_SHIFT, win->axi_yrgb_id, false);
		vop2_mask_write(vop2, RK3568_ESMART0_CTRL1 + win_offset, ESMART_AXI_UV_ID_MASK,
				ESMART_AXI_UV_ID_SHIFT, win->axi_uv_id, false);
	}
}

static bool vop2_win_dither_up(enum data_format format)
{
	bool ret;

	switch (format) {
	case ROCKCHIP_FMT_RGB565:
		ret = true;
		break;
	default:
		ret = false;
		break;
	}

	return ret;
}

static bool vop2_is_mirror_win(struct vop2_win_data *win)
{
	return soc_is_rk3566() && ((win->feature & (u32)WIN_FEATURE_MIRROR) != 0U);
}

static int vop2_set_cluster_win(struct display_state *state, struct vop2_win_data *win)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct vop2 *vop2 = cstate->private;
	int src_w = cstate->src_rect.w;
	int src_h = cstate->src_rect.h;
	int crtc_x = cstate->crtc_rect.x;
	int crtc_y = cstate->crtc_rect.y;
	int crtc_w = cstate->crtc_rect.w;
	int crtc_h = cstate->crtc_rect.h;
	int xvir = cstate->xvir;
	enum vop_csc_format csc_mode;
	u32 y_mirror;
	u32 act_info, dsp_info, dsp_st, dsp_stx, dsp_sty;
	/* offset of the right window in splice mode */
	u32 splice_pixel_offset;
	u32 splice_yrgb_offset = 0;
	u32 win_offset = win->reg_offset;
	u32 cfg_done = (u32)CFG_DONE_EN | (u32)BIT((u32)cstate->crtc_id) | ((u32)BIT((u32)cstate->crtc_id) << 16U);
	bool dither_up;

	if (win->splice_mode_right) {
		src_w = cstate->right_src_rect.w;
		src_h = cstate->right_src_rect.h;
		crtc_x = cstate->right_crtc_rect.x;
		crtc_y = cstate->right_crtc_rect.y;
		crtc_w = cstate->right_crtc_rect.w;
		crtc_h = cstate->right_crtc_rect.h;
		splice_pixel_offset = (u32)cstate->right_src_rect.x - (u32)cstate->src_rect.x;
		splice_yrgb_offset = splice_pixel_offset * (state->logo.bpp >> 3);
		cfg_done = (u32)CFG_DONE_EN | (u32)BIT((u32)cstate->splice_crtc_id) | (u32)(BIT((u32)cstate->splice_crtc_id) << 16U);
	}

	act_info = ((u32)src_h - 1U) << 16U;
	act_info |= (u32)(((u32)src_w - 1U) & 0xffffU);

	dsp_info = ((u32)crtc_h - 1U) << 16U;
	dsp_info |= (u32)(((u32)crtc_w - 1U) & 0xffffU);

	dsp_stx = (u32)crtc_x;
	dsp_sty = (u32)crtc_y;
	dsp_st = (u32)(dsp_sty << 16U) | (u32)(dsp_stx & 0xffffU);

	if ((mode->flags & (u32)DRM_MODE_FLAG_YMIRROR) != 0U) {
		y_mirror = 1U;
	} else {
		y_mirror = 0U;
	}

	vop2_setup_scale(vop2, win, (u32)src_w, (u32)src_h, (u32)crtc_w, (u32)crtc_h);

	if (vop2->version != VOP_VERSION_RK3568) {
		vop2_axi_config(vop2, win);
	}

	if (y_mirror != 0U) {
		(void)printf("WARN: y mirror is unsupported by cluster window\n");
	}

	if (is_vop3(vop2)) {
		vop2_mask_write(vop2, RK3576_CLUSTER0_PORT_SEL + win_offset,
				CLUSTER_PORT_SEL_SHIFT, CLUSTER_PORT_SEL_SHIFT,
				(u32)cstate->crtc_id, false);
	}

	/* rk3588 should set half_blocK_en to 1 in line and tile mode */
	if (vop2->version == VOP_VERSION_RK3588) {
		vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_AFBCD_CTRL + win_offset,
				EN_MASK, CLUSTER_AFBCD_HALF_BLOCK_SHIFT, 1U, false);
	}

	vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL0 + win_offset,
			WIN_FORMAT_MASK, WIN_FORMAT_SHIFT, (u32)cstate->format,
			false);
	vop2_writel(vop2, RK3568_CLUSTER0_WIN0_VIR + win_offset, (u32)xvir);
	vop2_writel(vop2, RK3568_CLUSTER0_WIN0_YRGB_MST + win_offset,
		    cstate->dma_addr + splice_yrgb_offset);

	vop2_writel(vop2, RK3568_CLUSTER0_WIN0_ACT_INFO + win_offset, act_info);
	vop2_writel(vop2, RK3568_CLUSTER0_WIN0_DSP_INFO + win_offset, dsp_info);
	vop2_writel(vop2, RK3568_CLUSTER0_WIN0_DSP_ST + win_offset, dsp_st);

	vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL0 + win_offset, EN_MASK, WIN_EN_SHIFT, 1U, false);

	csc_mode = vop2_convert_csc_mode(conn_state->color_encoding, conn_state->color_range,
					 CSC_10BIT_DEPTH);
	vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL0 + win_offset, EN_MASK,
			CLUSTER_RGB2YUV_EN_SHIFT,
			is_yuv_output(conn_state->bus_format) ? 1U : 0U, false);
	vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL0 + win_offset, CSC_MODE_MASK,
			CLUSTER_CSC_MODE_SHIFT, (u32)csc_mode, false);

	dither_up = vop2_win_dither_up(cstate->format);
	vop2_mask_write(vop2, RK3568_CLUSTER0_WIN0_CTRL0 + win_offset, EN_MASK,
			CLUSTER_DITHER_UP_EN_SHIFT, dither_up ? 1U : 0U, false);

	vop2_mask_write(vop2, RK3568_CLUSTER0_CTRL + win_offset, EN_MASK, CLUSTER_EN_SHIFT, 1U, false);

	vop2_writel(vop2, RK3568_REG_CFG_DONE, cfg_done);

	return 0;
}

static int vop2_set_smart_win(struct display_state *state, struct vop2_win_data *win)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct vop2 *vop2 = cstate->private;
	int src_w = cstate->src_rect.w;
	int src_h = cstate->src_rect.h;
	int crtc_x = cstate->crtc_rect.x;
	int crtc_y = cstate->crtc_rect.y;
	int crtc_w = cstate->crtc_rect.w;
	int crtc_h = cstate->crtc_rect.h;
	int xvir = cstate->xvir;
	enum vop_csc_format csc_mode;
	u32 y_mirror;
	u32 act_info, dsp_info, dsp_st, dsp_stx, dsp_sty;
	/* offset of the right window in splice mode */
	u32 splice_pixel_offset;
	u32 splice_yrgb_offset = 0;
	u32 win_offset = win->reg_offset;
	u32 cfg_done = (u32)CFG_DONE_EN | (u32)BIT((u32)cstate->crtc_id) | (u32)(BIT((u32)cstate->crtc_id) << 16U);
	u32 val;
	bool dither_up;

	if (vop2_is_mirror_win(win)) {
		struct vop2_win_data *source_win = vop2_find_win_by_phys_id(vop2, win->source_win_id);

		if (!source_win) {
			(void)printf("invalid source win id %d\n", win->source_win_id);
			return -ENODEV;
		}

		val = vop2_readl(vop2, RK3568_ESMART0_REGION0_CTRL + source_win->reg_offset);
		if ((val & (u32)BIT(WIN_EN_SHIFT)) == 0U) {
			(void)printf("WARN: the source win should be enabled before mirror win\n");
			return -EAGAIN;
		}
	}

	if (win->splice_mode_right) {
		src_w = cstate->right_src_rect.w;
		src_h = cstate->right_src_rect.h;
		crtc_x = cstate->right_crtc_rect.x;
		crtc_y = cstate->right_crtc_rect.y;
		crtc_w = cstate->right_crtc_rect.w;
		crtc_h = cstate->right_crtc_rect.h;
		splice_pixel_offset = (u32)cstate->right_src_rect.x - (u32)cstate->src_rect.x;
		splice_yrgb_offset = (u32)splice_pixel_offset * (u32)(state->logo.bpp >> 3U);
		cfg_done = (u32)CFG_DONE_EN | (u32)BIT(cstate->splice_crtc_id) | (u32)(BIT(cstate->splice_crtc_id) << 16U);
	}

	/*
	 * This is workaround solution for IC design:
	 * esmart can't support scale down when actual_w % 16 == 1.
	 */
	if (src_w > crtc_w && ((u32)src_w & 0xfU) == 1U) {
		(void)printf("WARN: vp%d unsupported act_w[%d] mode 16 = 1 when scale down\n", cstate->crtc_id, src_w);
		src_w -= 1;
	}

	act_info = ((u32)src_h - 1U) << 16U;
	act_info |= (u32)((u32)src_w - 1U) & 0xffffU;

	dsp_info = ((u32)crtc_h - 1U) << 16U;
	dsp_info |= (u32)((u32)crtc_w - 1U) & 0xffffU;

	dsp_stx = (u32)crtc_x;
	dsp_sty = (u32)crtc_y;
	dsp_st = dsp_sty << 16U | (dsp_stx & 0xffffU);

	if ((mode->flags & (u32)DRM_MODE_FLAG_YMIRROR) != 0U) {
		y_mirror = 1U;
	} else {
		y_mirror = 0U;
	}

	if (is_vop3(vop2)) {
		vop2_mask_write(vop2, RK3568_ESMART0_CTRL0 + win_offset,
				ESMART_LB_SELECT_MASK, ESMART_LB_SELECT_SHIFT,
				win->scale_engine_num, false);
		vop2_mask_write(vop2, RK3576_ESMART0_PORT_SEL + win_offset,
				ESMART_PORT_SEL_MASK, ESMART_PORT_SEL_SHIFT,
				(u32)cstate->crtc_id, false);
		vop2_mask_write(vop2, RK3576_ESMART0_DLY_NUM + win_offset,
				ESMART_DLY_NUM_MASK, ESMART_DLY_NUM_SHIFT,
				0, false);

		/* Merge esmart1/3 from vp1 post to vp0 */
		if (vop2->version == VOP_VERSION_RK3576 && cstate->crtc_id == 0 &&
		    (win->phys_id == (u8)ROCKCHIP_VOP2_ESMART1 ||
		     win->phys_id == (u8)ROCKCHIP_VOP2_ESMART3)) {
			vop2_mask_write(vop2, RK3576_ESMART0_PORT_SEL + win_offset,
					ESMART_PORT_SEL_MASK, ESMART_PORT_SEL_SHIFT,
					1, false);
		}
	}

	vop2_setup_scale(vop2, win, (u32)src_w, (u32)src_h, (u32)crtc_w, (u32)crtc_h);

	if (vop2->version != VOP_VERSION_RK3568) {
		vop2_axi_config(vop2, win);
	}

	if (y_mirror != 0U) {
		cstate->dma_addr += ((u32)src_h - 1U) * (u32)xvir * 4U;
	}
	vop2_mask_write(vop2, RK3568_ESMART0_CTRL1 + win_offset, EN_MASK,
			YMIRROR_EN_SHIFT, y_mirror, false);

	vop2_mask_write(vop2, RK3568_ESMART0_REGION0_CTRL + win_offset,
			WIN_FORMAT_MASK, WIN_FORMAT_SHIFT, (u32)cstate->format,
			false);

	if (vop2->version == VOP_VERSION_RK3576) {
		vop2_writel(vop2, RK3576_ESMART0_ALPHA_MAP + win_offset, 0x8000ff00U);
	}

	vop2_writel(vop2, RK3568_ESMART0_REGION0_VIR + win_offset, (u32)xvir);
	vop2_writel(vop2, RK3568_ESMART0_REGION0_YRGB_MST + win_offset,
		    cstate->dma_addr + splice_yrgb_offset);

	vop2_writel(vop2, RK3568_ESMART0_REGION0_ACT_INFO + win_offset,
		    act_info);
	vop2_writel(vop2, RK3568_ESMART0_REGION0_DSP_INFO + win_offset,
		    dsp_info);
	vop2_writel(vop2, RK3568_ESMART0_REGION0_DSP_ST + win_offset, dsp_st);

	vop2_mask_write(vop2, RK3568_ESMART0_REGION0_CTRL + win_offset, EN_MASK,
			WIN_EN_SHIFT, 1U, false);

	csc_mode = vop2_convert_csc_mode(conn_state->color_encoding, conn_state->color_range,
					 CSC_10BIT_DEPTH);
	vop2_mask_write(vop2, RK3568_ESMART0_CTRL0 + win_offset, EN_MASK,
			RGB2YUV_EN_SHIFT,
			is_yuv_output(conn_state->bus_format) ? 1U : 0U, false);
	vop2_mask_write(vop2, RK3568_ESMART0_CTRL0 + win_offset, CSC_MODE_MASK,
			CSC_MODE_SHIFT, (u32)csc_mode, false);

	dither_up = vop2_win_dither_up(cstate->format);
	vop2_mask_write(vop2, RK3568_ESMART0_REGION0_CTRL + win_offset, EN_MASK,
			REGION0_DITHER_UP_EN_SHIFT, dither_up ? 1U : 0U, false);

	vop2_writel(vop2, RK3568_REG_CFG_DONE, cfg_done);

	return 0;
}

static void vop2_calc_display_rect_for_splice(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct connector_state *conn_state = &state->conn_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct display_rect *src_rect = &cstate->src_rect;
	struct display_rect *dst_rect = &cstate->crtc_rect;
	struct display_rect left_src, left_dst, right_src, right_dst;
	u16 half_hdisplay = (u16)mode->crtc_hdisplay >> 1U;
	int left_src_w, left_dst_w, right_dst_w;

	left_dst_w = min_t(u16, (half_hdisplay), ((u16)dst_rect->x + (u16)dst_rect->w)) - dst_rect->x;
	if (left_dst_w < 0) {
		left_dst_w = 0;
	}
	right_dst_w = dst_rect->w - left_dst_w;

	if (right_dst_w == 0) {
		left_src_w = src_rect->w;
	} else {
		left_src_w = src_rect->x + src_rect->w - src_rect->w / 2;
	}

	left_src.x = src_rect->x;
	left_src.w = left_src_w;
	left_dst.x = dst_rect->x;
	left_dst.w = left_dst_w;
	right_src.x = left_src.x + left_src.w;
	right_src.w = src_rect->x + src_rect->w - left_src.x - left_src.w;
	right_dst.x = dst_rect->x + left_dst_w - (int)half_hdisplay;
	right_dst.w = right_dst_w;

	left_src.y = src_rect->y;
	left_src.h = src_rect->h;
	left_dst.y = dst_rect->y;
	left_dst.h = dst_rect->h;
	right_src.y = src_rect->y;
	right_src.h = src_rect->h;
	right_dst.y = dst_rect->y;
	right_dst.h = dst_rect->h;

	(void)memcpy(&cstate->src_rect, &left_src, sizeof(struct display_rect));
	(void)memcpy(&cstate->crtc_rect, &left_dst, sizeof(struct display_rect));
	(void)memcpy(&cstate->right_src_rect, &right_src, sizeof(struct display_rect));
	(void)memcpy(&cstate->right_crtc_rect, &right_dst, sizeof(struct display_rect));
}

static int rockchip_vop2_set_plane(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	struct vop2_win_data *win_data;
	struct vop2_win_data *splice_win_data;
	u8 primary_plane_id;
	int ret;

	if (cstate->crtc_id < 0 || cstate->crtc_id > (int)VOP2_VP_MAX - 1) {
		return -EINVAL;
	}
	primary_plane_id = vop2->vp_plane_mask[cstate->crtc_id].primary_plane_id;

	if (cstate->crtc_rect.w > cstate->max_output.width) {
		(void)printf("ERROR: output w[%d] exceeded max width[%d]\n",
		       cstate->crtc_rect.w, cstate->max_output.width);
		return -EINVAL;
	}

	win_data = vop2_find_win_by_phys_id(vop2, primary_plane_id);
	if (!win_data) {
		(void)printf("invalid win id %d\n", primary_plane_id);
		return -ENODEV;
	}

	/* ignore some plane register according vop3 esmart lb mode */
	if (vop3_ignore_plane(vop2, win_data)) {
		return -EACCES;
	}

	if (vop2->version == VOP_VERSION_RK3588 || vop2->version == VOP_VERSION_RK3576) {
		if (vop2_power_domain_on(vop2, win_data->pd_id) != 0) {
			(void)printf("open vp%d plane pd fail\n", cstate->crtc_id);
		}
	}

	if (cstate->splice_mode) {
		if (win_data->splice_win_id != 0U) {
			splice_win_data = vop2_find_win_by_phys_id(vop2, win_data->splice_win_id);
			if (splice_win_data == NULL) {
				return -EINVAL;
			}
			splice_win_data->splice_mode_right = true;

			if (vop2_power_domain_on(vop2, splice_win_data->pd_id) != 0) {
				(void)printf("splice mode: open vp%d plane pd fail\n", cstate->splice_crtc_id);
			}

			vop2_calc_display_rect_for_splice(state);
			if (win_data->type == CLUSTER_LAYER) {
				(void)vop2_set_cluster_win(state, splice_win_data);
			} else {
				(void)vop2_set_smart_win(state, splice_win_data);
			}
		} else {
			(void)printf("ERROR: splice mode is unsupported by plane %s\n",
			       vop2_plane_id_to_string(primary_plane_id));
			return -EINVAL;
		}
	}

	if (win_data->type == CLUSTER_LAYER) {
		ret = vop2_set_cluster_win(state, win_data);
	} else {
		ret = vop2_set_smart_win(state, win_data);
	}
	if (ret != 0) {
		return ret;
	}

	(void)printf("VOP VP%d enable %s[%dx%d->%dx%d@%dx%d] fmt[%d] addr[0x%x]\n",
		cstate->crtc_id, vop2_plane_id_to_string(primary_plane_id),
		cstate->src_rect.w, cstate->src_rect.h, cstate->crtc_rect.w, cstate->crtc_rect.h,
		cstate->crtc_rect.x, cstate->crtc_rect.y, cstate->format,
		cstate->dma_addr);

	return 0;
}

static int rockchip_vop2_prepare(struct display_state *state)
{
	return 0;
}

static void vop2_dsc_cfg_done(struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	u8 dsc_id = cstate->dsc_id;
	u32 ctrl_regs_offset = ((u32)dsc_id * 0x30U);

	if (((u32)conn_state->output_flags & (u32)ROCKCHIP_OUTPUT_DUAL_CHANNEL_LEFT_RIGHT_MODE) != 0U) {
		vop2_mask_write(vop2, RK3588_DSC_8K_CFG_DONE, EN_MASK,
				DSC_CFG_DONE_SHIFT, 1, false);
		vop2_mask_write(vop2, RK3588_DSC_8K_CFG_DONE + 0x30U, EN_MASK,
				DSC_CFG_DONE_SHIFT, 1, false);
	} else {
		vop2_mask_write(vop2, RK3588_DSC_8K_CFG_DONE + ctrl_regs_offset, EN_MASK,
				DSC_CFG_DONE_SHIFT, 1, false);
	}
}

static int rockchip_vop2_enable(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	u32 cfg_done = (u32)CFG_DONE_EN | (u32)BIT((u32)cstate->crtc_id) | (u32)(BIT((u32)cstate->crtc_id) << 16U);

	vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
			STANDBY_EN_SHIFT, 0, false);

	if (cstate->splice_mode) {
		cfg_done |= (u32)BIT((u32)cstate->splice_crtc_id) | (u32)(BIT((u32)cstate->splice_crtc_id) << 16U);
	}

	vop2_writel(vop2, RK3568_REG_CFG_DONE, cfg_done);

	if (cstate->dsc_enable != 0U) {
		vop2_dsc_cfg_done(state);
	}

	if (cstate->mcu_timing.mcu_pix_total != 0) {
		vop2_mask_write(vop2, RK3562_VP0_MCU_CTRL + vp_offset, EN_MASK,
				MCU_HOLD_MODE_SHIFT, 0, false);
	}

	return 0;
}

static int rk3588_vop2_post_enable(struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	u32 output_if = (u32)conn_state->output_if;
	u32 cfg_done = (u32)CFG_DONE_EN | (u32)BIT((u32)cstate->crtc_id) | (u32)(BIT((u32)cstate->crtc_id) << 16U);
	int ret, val;

	if ((output_if & (u32)VOP_OUTPUT_IF_DP0) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RK3588_DP0_EN_SHIFT,
				1, false);
	}

	if ((output_if & (u32)VOP_OUTPUT_IF_DP1) != 0U) {
		vop2_mask_write(vop2, RK3568_DSP_IF_EN, EN_MASK, RK3588_DP1_EN_SHIFT,
				1, false);
	}

	if ((output_if & ((u32)VOP_OUTPUT_IF_DP0 | (u32)VOP_OUTPUT_IF_DP1)) != 0U) {
		vop2_writel(vop2, RK3568_REG_CFG_DONE, cfg_done);
		ret = (int)readl_poll_timeout((vop2->regs + RK3568_REG_CFG_DONE), (val),
					      ((u32)((u32)val & (u32)BIT((u32)cstate->crtc_id)) != 0U), (50UL * 1000UL));
		if (ret != 0) {
			(void)printf("%s wait cfg done timeout\n", __func__);
		}

		if (cstate->dclk_rst.dev) {
			(void)reset_assert(&cstate->dclk_rst);
			udelay(20);
			(void)reset_deassert(&cstate->dclk_rst);
		}
	}

	return 0;
}

static int rk3576_vop2_post_enable(struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	u32 output_if = (u32)conn_state->output_if;
	u32 cfg_done = (u32)CFG_DONE_EN | (u32)BIT((u32)cstate->crtc_id) | (u32)(BIT((u32)cstate->crtc_id) << 16U);
	int ret, val;

	if ((output_if & (u32)VOP_OUTPUT_IF_DP0) != 0U) {
		vop2_mask_write(vop2, RK3576_DP0_IF_CTRL, EN_MASK,
				RK3576_IF_OUT_EN_SHIFT, 1, false);
	}

	if ((output_if & (u32)VOP_OUTPUT_IF_DP1) != 0U) {
		vop2_mask_write(vop2, RK3576_DP1_IF_CTRL, EN_MASK,
				RK3576_IF_OUT_EN_SHIFT, 1, false);
	}

	if ((output_if & (u32)VOP_OUTPUT_IF_DP2) != 0U) {
		vop2_mask_write(vop2, RK3576_DP2_IF_CTRL, EN_MASK,
				RK3576_IF_OUT_EN_SHIFT, 1, false);
	}

	if ((output_if & ((u32)VOP_OUTPUT_IF_DP0 | (u32)VOP_OUTPUT_IF_DP1 | (u32)VOP_OUTPUT_IF_DP2)) != 0U) {
		vop2_writel(vop2, RK3568_REG_CFG_DONE, cfg_done);
		ret = (int)readl_poll_timeout((vop2->regs + RK3568_REG_CFG_DONE), (val),
					      ((u32)((u32)val & (u32)BIT((u32)cstate->crtc_id)) != 0U), (50UL * 1000UL));
		if (ret != 0) {
			(void)printf("%s wait cfg done timeout\n", __func__);
		}

		if (cstate->dclk_rst.dev) {
			(void)reset_assert(&cstate->dclk_rst);
			udelay(20);
			(void)reset_deassert(&cstate->dclk_rst);
		}
	}

	return 0;
}

static int rockchip_vop2_post_enable(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;

	if (vop2->version == VOP_VERSION_RK3588) {
		(void)rk3588_vop2_post_enable(state);
	} else if (vop2->version == VOP_VERSION_RK3576) {
		(void)rk3576_vop2_post_enable(state);
	} else {
		/* Do nothing */
	}

	return 0;
}

static int rockchip_vop2_disable(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	u32 cfg_done = (u32)CFG_DONE_EN | (u32)BIT((u32)cstate->crtc_id) | (u32)(BIT((u32)cstate->crtc_id) << 16U);

	vop2_mask_write(vop2, RK3568_VP0_DSP_CTRL + vp_offset, EN_MASK,
			STANDBY_EN_SHIFT, 1, false);

	if (cstate->splice_mode) {
		cfg_done |= (u32)BIT((u32)cstate->splice_crtc_id) | (u32)(BIT((u32)cstate->splice_crtc_id) << 16U);
	}

	vop2_writel(vop2, RK3568_REG_CFG_DONE, cfg_done);

	return 0;
}

static int rockchip_vop2_get_cursor_plane(struct display_state *state, u32 plane_mask, int cursor_plane)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	int i;
	int correct_cursor_plane = -1;
	int plane_type = -1;

	if (cursor_plane < 0) {
		return -1;
	}

	if (((u32)plane_mask & (u32)((u32)1U << (u32)cursor_plane)) != 0U) {
		return cursor_plane;
	}

	/* Get current cursor plane type */
	for (i = 0; i < (int)vop2->data->nr_layers; i++) {
		if ((int)vop2->data->plane_table[i].plane_id == cursor_plane) {
			plane_type = (int)vop2->data->plane_table[i].plane_type;
			break;
		}
	}

	/* Get the other same plane type plane id */
	for (i = 0; i < (int)vop2->data->nr_layers; i++) {
		if ((int)vop2->data->plane_table[i].plane_type == plane_type &&
		    (int)vop2->data->plane_table[i].plane_id != cursor_plane) {
			correct_cursor_plane = (int)vop2->data->plane_table[i].plane_id;
			break;
		}
	}

	/* To check whether the new correct_cursor_plane is attach to current vp */
	if (correct_cursor_plane < 0 || (plane_mask & (u32)((u32)1U << (u32)correct_cursor_plane)) == 0U) {
		(void)printf("error: faild to find correct plane as cursor plane\n");
		return -1;
	}

	(void)printf("vp%d adjust cursor plane from %d to %d\n",
	       cstate->crtc_id, cursor_plane, correct_cursor_plane);

	return correct_cursor_plane;
}

static int rockchip_vop2_fixup_dts(struct display_state *state, void *blob)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	ofnode vp_node;
	struct device_node *port_parent_node = cstate->ports_node;
	static bool vop_fix_dts;
	const char *path;
	u32 plane_mask;
	int vp_id = 0;
	int cursor_plane_id;

	if (vop_fix_dts || vop2->version == VOP_VERSION_RK3528) {
		return 0;
	}

	ofnode_for_each_subnode((vp_node), (np_to_ofnode(port_parent_node))) {
		if (vp_id > (int)VOP2_VP_MAX - 1) {
			break;
		}

		path = vp_node.np->full_name;
		plane_mask = vop2->vp_plane_mask[vp_id].plane_mask;

		if (cstate->crtc->assign_plane) {
			continue;
		}
		cursor_plane_id = rockchip_vop2_get_cursor_plane(state, plane_mask,
								 cstate->crtc->vps[vp_id].cursor_plane);
		(void)printf("vp%d, plane_mask:0x%x, primary-id:%d, curser-id:%d\n",
		       vp_id, plane_mask,
		       vop2->vp_plane_mask[vp_id].primary_plane_id,
		       cursor_plane_id);

		do_fixup_by_path_u32(blob, path, "rockchip,plane-mask",
				     plane_mask, 1);
		do_fixup_by_path_u32(blob, path, "rockchip,primary-plane",
				     vop2->vp_plane_mask[vp_id].primary_plane_id, 1);
		if (cursor_plane_id >= 0) {
			do_fixup_by_path_u32(blob, path, "cursor-win-id",
					     (u32)cursor_plane_id, 1);
		}
		vp_id++;
	}

	vop_fix_dts = true;

	return 0;
}

static int rockchip_vop2_check(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct rockchip_crtc *crtc = cstate->crtc;

	if (crtc->splice_mode && (u32)cstate->crtc_id == crtc->splice_crtc_id) {
		(void)printf("WARN: VP%d is busy in splice mode\n", cstate->crtc_id);
		return -ENOTSUPP;
	}

	if (cstate->splice_mode) {
		crtc->splice_mode = true;
		crtc->splice_crtc_id = cstate->splice_crtc_id;
	}

	return 0;
}

static int rockchip_vop2_mode_valid(struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	struct crtc_state *cstate = &state->crtc_state;
	struct drm_display_mode *mode = &conn_state->mode;
	struct videomode vm;

	drm_display_mode_to_videomode(mode, &vm);

	if (vm.hactive < 32U || vm.vactive < 32U ||
	    (vm.hfront_porch * vm.hsync_len * vm.hback_porch *
	     vm.vfront_porch * vm.vsync_len * vm.vback_porch == 0U)) {
		(void)printf("ERROR: VP%d: unsupported display timing\n", cstate->crtc_id);
		return -EINVAL;
	}

	return 0;
}

static int rockchip_vop2_mode_fixup(struct display_state *state)
{
	struct connector_state *conn_state = &state->conn_state;
	struct rockchip_connector *conn = conn_state->connector;
	struct drm_display_mode *mode = &conn_state->mode;
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;

	if (conn_state->secondary) {
		if (!(conn->dual_channel_mode &&
		      conn_state->secondary->type == DRM_MODE_CONNECTOR_eDP) &&
		    conn_state->secondary->type != DRM_MODE_CONNECTOR_LVDS) {
			drm_mode_convert_to_split_mode(mode);
		}
	}

	drm_mode_set_crtcinfo(mode, (int)((u32)CRTC_INTERLACE_HALVE_V | (u32)CRTC_STEREO_DOUBLE));

	/*
	 * For RK3568 and RK3588, the hactive of video timing must
	 * be 4-pixel aligned.
	 */
	if (vop2->version == VOP_VERSION_RK3568 || vop2->version == VOP_VERSION_RK3588) {
		if (mode->crtc_hdisplay % 4 != 0) {
			int old_hdisplay = mode->crtc_hdisplay;
			int align = 4 - (mode->crtc_hdisplay % 4);

			mode->crtc_hdisplay += align;
			mode->crtc_hsync_start += align;
			mode->crtc_hsync_end += align;
			mode->crtc_htotal += align;

			(void)printf("WARN: hactive need to be aligned with 4-pixel, %d -> %d\n",
			       old_hdisplay, mode->hdisplay);
		}
	}

	/*
	 * For RK3576 YUV420 output, hden signal introduce one cycle delay,
	 * so we need to adjust hfp and hbp to compatible with this design.
	 */
	if (vop2->version == VOP_VERSION_RK3576 &&
	    conn_state->output_mode == ROCKCHIP_OUT_MODE_YUV420) {
		mode->crtc_hsync_start += 2;
		mode->crtc_hsync_end += 2;
	}

	if ((mode->flags & (u32)DRM_MODE_FLAG_DBLCLK) != 0U || ((u32)conn_state->output_if & (u32)VOP_OUTPUT_IF_BT656) != 0U) {
		mode->crtc_clock *= 2;
	}

	/*
	 * For RK3528, the path of CVBS output is like:
	 * VOP BT656 ENCODER -> CVBS BT656 DECODER -> CVBS ENCODER -> CVBS VDAC
	 * The vop2 dclk should be four times crtc_clock for CVBS sampling
	 * clock needs.
	 */
	if (vop2->version == VOP_VERSION_RK3528 && ((u32)conn_state->output_if & (u32)VOP_OUTPUT_IF_BT656) != 0U) {
		mode->crtc_clock *= 4;
	}

	mode->crtc_clock *= (int)rockchip_drm_get_cycles_per_pixel((u32)conn_state->bus_format);
	if (cstate->mcu_timing.mcu_pix_total != 0) {
		mode->crtc_clock *= cstate->mcu_timing.mcu_pix_total + 1;
	}

	return 0;
}

#define FRAC_16_16(mult, div)	(u32)(((u32)(mult) << 16U) / (u32)(div))

static int rockchip_vop2_plane_check(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	struct display_rect *src = &cstate->src_rect;
	struct display_rect *dst = &cstate->crtc_rect;
	struct vop2_win_data *win_data;
	u32 min_scale, max_scale;
	int hscale, vscale;
	u8 primary_plane_id;

	if (cstate->crtc_id < 0 || cstate->crtc_id > VOP2_MAX_VP - 1) {
		return -EINVAL;
	}
	primary_plane_id = vop2->vp_plane_mask[cstate->crtc_id].primary_plane_id;

	win_data = vop2_find_win_by_phys_id(vop2, primary_plane_id);
	if (win_data == NULL) {
		(void)printf("ERROR: invalid win id %d\n", primary_plane_id);
		return -ENODEV;
	}

	min_scale = FRAC_16_16(1U, win_data->max_downscale_factor);
	max_scale = FRAC_16_16(win_data->max_upscale_factor, 1U);

	hscale = display_rect_calc_hscale(src, dst, (int)min_scale, (int)max_scale);
	vscale = display_rect_calc_vscale(src, dst, (int)min_scale, (int)max_scale);
	if (hscale < 0 || vscale < 0) {
		(void)printf("ERROR: VP%d %s: scale factor is out of range\n", cstate->crtc_id, win_data->name);
		return -ERANGE;
	}

	return 0;
}

static int rockchip_vop2_apply_soft_te(struct display_state *state)
{
	__maybe_unused struct connector_state *conn_state = &state->conn_state;
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	u32 vp_offset = ((u32)cstate->crtc_id * 0x100U);
	int val = 0;
	int ret;

	ret = (int)readl_poll_timeout((vop2->regs + RK3568_VP0_MIPI_CTRL + vp_offset), (val),
				      (((u32)((u32)val >> (u32)EDPI_WMS_FS) & 0x1U) != 0U), (50UL * 1000UL));
	if (ret == 0) {
#ifndef CONFIG_SPL_BUILD
		ret = (int)readx_poll_timeout((dm_gpio_get_value), (conn_state->te_gpio), (val),
					      (val == 0), (50UL * 1000UL));
		if (ret == 0) {
			ret = (int)readx_poll_timeout((dm_gpio_get_value), (conn_state->te_gpio), (val),
						      (val != 0), (50UL * 1000UL));
			if (ret == 0) {
				vop2_mask_write(vop2, RK3568_VP0_MIPI_CTRL + vp_offset,
						EN_MASK, EDPI_WMS_FS, 1, false);
			} else {
				(void)printf("ERROR: vp%d wait for active TE signal timeout\n",
				       cstate->crtc_id);
				return ret;
			}
		} else {
			(void)printf("ERROR: vp%d TE signal maybe always high\n", cstate->crtc_id);
			return ret;
		}
#endif
	} else {
		(void)printf("ERROR: vp%d wait vop2 frame start timeout in hold mode\n", cstate->crtc_id);
		return ret;
	}

	return 0;
}

static int rockchip_vop2_regs_dump(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	const struct vop2_data *vop2_data = vop2->data;
	const struct vop2_dump_regs *regs = vop2_data->dump_regs;
	u32 len;
	u32 n, i, j;
	u32 base;

	if (!cstate->crtc->active) {
		return -EINVAL;
	}

	n = vop2_data->dump_regs_size;
	for (i = 0; i < n; i++) {
		base = regs[i].offset;
		len = 128;
		if (regs[i].size != 0U) {
			len = min(((u32)(len)), ((u32)(regs[i].size >> 2U)));
		}
		(void)printf("\n%s:\n", regs[i].name);
		for (j = 0; j < len; j += 4U) {
			(void)printf("%08lx:  %08x %08x %08x %08x\n",
			       (uintptr_t)vop2->regs + (uintptr_t)base + (uintptr_t)j * (uintptr_t)4U,
			       vop2_readl(vop2, base + (4U * j)),
			       vop2_readl(vop2, base + (4U * (j + 1U))),
			       vop2_readl(vop2, base + (4U * (j + 2U))),
			       vop2_readl(vop2, base + (4U * (j + 3U))));
		}
	}

	return 0;
}

static int rockchip_vop2_active_regs_dump(struct display_state *state)
{
	struct crtc_state *cstate = &state->crtc_state;
	struct vop2 *vop2 = cstate->private;
	const struct vop2_data *vop2_data = vop2->data;
	const struct vop2_dump_regs *regs = vop2_data->dump_regs;
	u32 len;
	u32 n, i, j;
	u32 base;
	bool enable_state;

	if (!cstate->crtc->active) {
		return -EINVAL;
	}

	n = vop2_data->dump_regs_size;
	for (i = 0; i < n; i++) {
		if (regs[i].state_mask != 0U) {
			enable_state = ((vop2_readl(vop2, regs[i].state_base) >> regs[i].state_shift) & regs[i].state_mask) != 0U;
			if (enable_state != regs[i].enable_state) {
				continue;
			}
		}

		base = regs[i].offset;
		len = 128;
		if (regs[i].size != 0U) {
			len = min(((u32)(len)), ((u32)(regs[i].size >> 2U)));
		}
		(void)printf("\n%s:\n", regs[i].name);
		for (j = 0; j < len; j += 4U) {
			(void)printf("%08lx:  %08x %08x %08x %08x\n",
			       (uintptr_t)vop2->regs + (uintptr_t)base + (uintptr_t)j * (uintptr_t)4U,
			       vop2_readl(vop2, base + (4U * j)),
			       vop2_readl(vop2, base + (4U * (j + 1U))),
			       vop2_readl(vop2, base + (4U * (j + 2U))),
			       vop2_readl(vop2, base + (4U * (j + 3U))));
		}
	}

	return 0;
}

static struct vop2_dump_regs rk3528_dump_regs[] = {
	{ RK3568_REG_CFG_DONE, "SYS", 0, 0, 0, false },
	{ RK3528_OVL_SYS, "OVL_SYS", 0, 0, 0, false },
	{ RK3528_OVL_PORT0_CTRL, "OVL_VP0", RK3568_VP0_DSP_CTRL, 0x1, 31, false },
	{ RK3528_OVL_PORT1_CTRL, "OVL_VP1", RK3568_VP1_DSP_CTRL, 0x1, 31, false },
	{ RK3568_VP0_DSP_CTRL, "VP0", RK3568_VP0_DSP_CTRL, 0x1, 31, false },
	{ RK3568_VP1_DSP_CTRL, "VP1", RK3568_VP1_DSP_CTRL, 0x1, 31, false },
	{ RK3568_CLUSTER0_WIN0_CTRL0, "Cluster0", RK3568_CLUSTER0_WIN0_CTRL0, 0x1, 0, true },
	{ RK3568_ESMART0_CTRL0, "Esmart0", RK3568_ESMART0_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_ESMART1_CTRL0, "Esmart1", RK3568_ESMART1_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_SMART0_CTRL0, "Esmart2", RK3568_SMART0_CTRL0, 0x1, 0, true },
	{ RK3568_SMART1_CTRL0, "Esmart3", RK3568_SMART1_CTRL0, 0x1, 0, true },
	{ RK3528_HDR_LUT_CTRL, "HDR", 0, 0, 0, false },
	{ RK3528_ACM_CTRL, "ACM", RK3528_ACM_CTRL, 0x1, 0, true },
};

static u8 rk3528_vp_primary_plane_order[ROCKCHIP_VOP2_LAYER_MAX] = {
	(u8)ROCKCHIP_VOP2_ESMART0,
	(u8)ROCKCHIP_VOP2_ESMART1,
	(u8)ROCKCHIP_VOP2_ESMART2,
	(u8)ROCKCHIP_VOP2_ESMART3,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
};

static struct vop2_plane_table rk3528_plane_table[ROCKCHIP_VOP2_LAYER_MAX] = {
	{ROCKCHIP_VOP2_CLUSTER0, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_ESMART0, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART1, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART2, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART3, ESMART_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
};

static struct vop2_vp_plane_mask rk3528_vp_plane_mask[VOP2_VP_MAX][VOP2_VP_MAX] = {
	{ /* one display policy for hdmi */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART0,
			.attached_layers_nr = 4,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER0,
				  (u8)ROCKCHIP_VOP2_ESMART0,  (u8)ROCKCHIP_VOP2_ESMART1, (u8)ROCKCHIP_VOP2_ESMART2,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},
		{/* second display */},
		{/* third  display */},
		{/* fourth display */},
	},

	{ /* two display policy */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART0,
			.attached_layers_nr = 3,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER0, (u8)ROCKCHIP_VOP2_ESMART0, (u8)ROCKCHIP_VOP2_ESMART1,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},

		{/* second display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART3,
			.attached_layers_nr = 2,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_ESMART2, (u8)ROCKCHIP_VOP2_ESMART3,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},
		{/* third  display */},
		{/* fourth display */},
	},

	{ /* one display policy for cvbs */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART3,
			.attached_layers_nr = 2,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_ESMART2, (u8)ROCKCHIP_VOP2_ESMART3,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},
		{/* second display */},
		{/* third  display */},
		{/* fourth display */},
	},

	{/* reserved */},
};

static struct vop2_win_data rk3528_win_data[5] = {
	{
		.name = "Esmart0",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART0,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 8,
		.layer_sel_win_id = { 1, 0xff, 0xff, 0xff },
		.reg_offset = 0,
		.axi_id = 0,
		.axi_yrgb_id = 0x06,
		.axi_uv_id = 0x07,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,	/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_GT,	/* gt only */
		.max_upscale_factor = 8,
		.max_downscale_factor = 8,
	},

	{
		.name = "Esmart1",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART1,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 10,
		.layer_sel_win_id = { 2, 0xff, 0xff, 0xff },
		.reg_offset = 0x200,
		.axi_id = 0,
		.axi_yrgb_id = 0x08,
		.axi_uv_id = 0x09,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,	/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_GT,	/* gt only */
		.max_upscale_factor = 8,
		.max_downscale_factor = 8,
	},

	{
		.name = "Esmart2",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART2,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 12,
		.layer_sel_win_id = { 3, 0, 0xff, 0xff },
		.reg_offset = 0x400,
		.axi_id = 0,
		.axi_yrgb_id = 0x0a,
		.axi_uv_id = 0x0b,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,	/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_GT,	/* gt only */
		.max_upscale_factor = 8,
		.max_downscale_factor = 8,
	},

	{
		.name = "Esmart3",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART3,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 14,
		.layer_sel_win_id = { 0xff, 1, 0xff, 0xff },
		.reg_offset = 0x600,
		.axi_id = 0,
		.axi_yrgb_id = 0x0c,
		.axi_uv_id = 0x0d,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,	/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_GT,	/* gt only */
		.max_upscale_factor = 8,
		.max_downscale_factor = 8,
	},

	{
		.name = "Cluster0",
		.phys_id = (u8)ROCKCHIP_VOP2_CLUSTER0,
		.type = CLUSTER_LAYER,
		.win_sel_port_offset = 0,
		.layer_sel_win_id = { 0, 0xff, 0xff, 0xff },
		.reg_offset = 0,
		.axi_id = 0,
		.axi_yrgb_id = 0x02,
		.axi_uv_id = 0x03,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,	/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,	/* gt or avg */
		.max_upscale_factor = 8,
		.max_downscale_factor = 8,
	},
};

static struct vop2_vp_data rk3528_vp_data[2] = {
	{
		.feature = (u32)VOP_FEATURE_ALPHA_SCALE | (u32)VOP_FEATURE_OVERSCAN | (u32)VOP_FEATURE_POST_ACM |
			   (u32)VOP_FEATURE_POST_CSC,
		.max_output = {4096, 4096},
		.layer_mix_dly = 6,
		.hdr_mix_dly = 2,
		.win_dly = 8,
	},
	{
		.feature = (u32)VOP_FEATURE_ALPHA_SCALE | (u32)VOP_FEATURE_OVERSCAN,
		.max_output = {1920, 1080},
		.layer_mix_dly = 2,
		.hdr_mix_dly = 0,
		.win_dly = 8,
	},
};

const struct vop2_data rk3528_vop = {
	.version = VOP_VERSION_RK3528,
	.nr_vps = 2,
	.vp_data = rk3528_vp_data,
	.win_data = rk3528_win_data,
	.plane_mask = rk3528_vp_plane_mask[0],
	.plane_table = rk3528_plane_table,
	.vp_primary_plane_order = rk3528_vp_primary_plane_order,
	.nr_layers = 5,
	.nr_mixers = 3,
	.nr_gammas = 2,
	.esmart_lb_mode = VOP3_ESMART_4K_2K_2K_MODE,
	.dump_regs = rk3528_dump_regs,
	.dump_regs_size = (u32)ARRAY_SIZE(rk3528_dump_regs),
};

static struct vop2_dump_regs rk3562_dump_regs[] = {
	{ RK3568_REG_CFG_DONE, "SYS", 0, 0, 0, false },
	{ RK3528_OVL_SYS, "OVL_SYS", 0, 0, 0, false },
	{ RK3528_OVL_PORT0_CTRL, "OVL_VP0", RK3568_VP0_DSP_CTRL, 0x1, 31, false },
	{ RK3528_OVL_PORT1_CTRL, "OVL_VP1", RK3568_VP1_DSP_CTRL, 0x1, 31, false },
	{ RK3568_VP0_DSP_CTRL, "VP0", RK3568_VP0_DSP_CTRL, 0x1, 31, false },
	{ RK3568_VP1_DSP_CTRL, "VP1", RK3568_VP1_DSP_CTRL, 0x1, 31, false },
	{ RK3568_ESMART0_CTRL0, "Esmart0", RK3568_ESMART0_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_ESMART1_CTRL0, "Esmart1", RK3568_ESMART1_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_SMART0_CTRL0, "Esmart2", RK3568_SMART0_CTRL0, 0x1, 0, true },
	{ RK3568_SMART1_CTRL0, "Esmart3", RK3568_SMART1_CTRL0, 0x1, 0, true },
};

static u8 rk3562_vp_primary_plane_order[ROCKCHIP_VOP2_LAYER_MAX] = {
	(u8)ROCKCHIP_VOP2_ESMART0,
	(u8)ROCKCHIP_VOP2_ESMART1,
	(u8)ROCKCHIP_VOP2_ESMART2,
	(u8)ROCKCHIP_VOP2_ESMART3,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
};

static struct vop2_plane_table rk3562_plane_table[ROCKCHIP_VOP2_LAYER_MAX] = {
	{ROCKCHIP_VOP2_ESMART0, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART1, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART2, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART3, ESMART_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
};

static struct vop2_vp_plane_mask rk3562_vp_plane_mask[VOP2_VP_MAX][VOP2_VP_MAX] = {
	{ /* one display policy for hdmi */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART0,
			.attached_layers_nr = 4,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_ESMART0,  (u8)ROCKCHIP_VOP2_ESMART1,
				  (u8)ROCKCHIP_VOP2_ESMART2,  (u8)ROCKCHIP_VOP2_ESMART3,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},
		{/* second display */},
		{/* third  display */},
		{/* fourth display */},
	},

	{ /* two display policy */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART0,
			.attached_layers_nr = 2,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_ESMART0, (u8)ROCKCHIP_VOP2_ESMART1,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},

		{/* second display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART2,
			.attached_layers_nr = 2,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_ESMART2, (u8)ROCKCHIP_VOP2_ESMART3,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},
		{/* third  display */},
		{/* fourth display */},
	},

	{/* reserved */},

	{/* reserved */},
};

static struct vop2_win_data rk3562_win_data[4] = {
	{
		.name = "Esmart0",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART0,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 8,
		.layer_sel_win_id = { 0, 0, 0xff, 0xff },
		.reg_offset = 0,
		.axi_id = 0,
		.axi_yrgb_id = 0x02,
		.axi_uv_id = 0x03,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8,
		.max_downscale_factor = 8,
	},

	{
		.name = "Esmart1",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART1,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 10,
		.layer_sel_win_id = { 1, 1, 0xff, 0xff },
		.reg_offset = 0x200,
		.axi_id = 0,
		.axi_yrgb_id = 0x04,
		.axi_uv_id = 0x05,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8,
		.max_downscale_factor = 8,
	},

	{
		.name = "Esmart2",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART2,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 12,
		.layer_sel_win_id = { 2, 2, 0xff, 0xff },
		.reg_offset = 0x400,
		.axi_id = 0,
		.axi_yrgb_id = 0x06,
		.axi_uv_id = 0x07,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8,
		.max_downscale_factor = 8,
	},

	{
		.name = "Esmart3",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART3,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 14,
		.layer_sel_win_id = { 3, 3, 0xff, 0xff },
		.reg_offset = 0x600,
		.axi_id = 0,
		.axi_yrgb_id = 0x08,
		.axi_uv_id = 0x0d,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8,
		.max_downscale_factor = 8,
	},
};

static struct vop2_vp_data rk3562_vp_data[2] = {
	{
		.feature = (u32)VOP_FEATURE_ALPHA_SCALE | (u32)VOP_FEATURE_OVERSCAN,
		.max_output = {2048, 4096},
		.win_dly = 8,
		.layer_mix_dly = 8,
	},
	{
		.feature = (u32)VOP_FEATURE_ALPHA_SCALE | (u32)VOP_FEATURE_OVERSCAN,
		.max_output = {2048, 1080},
		.win_dly = 8,
		.layer_mix_dly = 8,
	},
};

const struct vop2_data rk3562_vop = {
	.version = VOP_VERSION_RK3562,
	.nr_vps = 2,
	.vp_data = rk3562_vp_data,
	.win_data = rk3562_win_data,
	.plane_mask = rk3562_vp_plane_mask[0],
	.plane_table = rk3562_plane_table,
	.vp_primary_plane_order = rk3562_vp_primary_plane_order,
	.nr_layers = 4,
	.nr_mixers = 3,
	.nr_gammas = 2,
	.esmart_lb_mode = VOP3_ESMART_2K_2K_2K_2K_MODE,
	.dump_regs = rk3562_dump_regs,
	.dump_regs_size = (u32)ARRAY_SIZE(rk3562_dump_regs),
};

static struct vop2_dump_regs rk3568_dump_regs[] = {
	{ RK3568_REG_CFG_DONE, "SYS", 0, 0, 0, false },
	{ RK3568_OVL_CTRL, "OVL", 0, 0, 0, false },
	{ RK3568_VP0_DSP_CTRL, "VP0", RK3568_VP1_DSP_CTRL, 0x1, 31, false },
	{ RK3568_VP1_DSP_CTRL, "VP1", RK3568_VP1_DSP_CTRL, 0x1, 31, false },
	{ RK3568_VP2_DSP_CTRL, "VP2", RK3568_VP2_DSP_CTRL, 0x1, 31, false },
	{ RK3568_CLUSTER0_WIN0_CTRL0, "Cluster0", RK3568_CLUSTER0_WIN0_CTRL0, 0x1, 0, true },
	{ RK3568_CLUSTER1_WIN0_CTRL0, "Cluster1", RK3568_CLUSTER1_WIN0_CTRL0, 0x1, 0, true },
	{ RK3568_ESMART0_CTRL0, "Esmart0", RK3568_ESMART0_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_ESMART1_CTRL0, "Esmart1", RK3568_ESMART1_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_SMART0_CTRL0, "Smart0", RK3568_SMART0_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_SMART1_CTRL0, "Smart1", RK3568_SMART1_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_HDR_LUT_CTRL, "HDR", 0, 0, 0, false },
};

static u8 rk3568_vp_primary_plane_order[ROCKCHIP_VOP2_LAYER_MAX] = {
	(u8)ROCKCHIP_VOP2_SMART0,
	(u8)ROCKCHIP_VOP2_SMART1,
	(u8)ROCKCHIP_VOP2_ESMART0,
	(u8)ROCKCHIP_VOP2_ESMART1,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
};

static struct vop2_plane_table rk356x_plane_table[ROCKCHIP_VOP2_LAYER_MAX] = {
	{ROCKCHIP_VOP2_CLUSTER0, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_CLUSTER1, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_ESMART0, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART1, ESMART_LAYER},
	{ROCKCHIP_VOP2_SMART0, SMART_LAYER},
	{ROCKCHIP_VOP2_SMART0, SMART_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
};

static struct vop2_vp_plane_mask rk356x_vp_plane_mask[VOP2_VP_MAX][VOP2_VP_MAX] = {
	{ /* one display policy */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_SMART0,
			.attached_layers_nr = 6,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER0, (u8)ROCKCHIP_VOP2_ESMART0, (u8)ROCKCHIP_VOP2_SMART0,
				  (u8)ROCKCHIP_VOP2_CLUSTER1, (u8)ROCKCHIP_VOP2_ESMART1, (u8)ROCKCHIP_VOP2_SMART1,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},
		{/* second display */},
		{/* third  display */},
		{/* fourth display */},
	},

	{ /* two display policy */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_SMART0,
			.attached_layers_nr = 3,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER0, (u8)ROCKCHIP_VOP2_ESMART0, (u8)ROCKCHIP_VOP2_SMART0,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},

		{/* second display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_SMART1,
			.attached_layers_nr = 3,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER1, (u8)ROCKCHIP_VOP2_ESMART1, (u8)ROCKCHIP_VOP2_SMART1,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},
		{/* third  display */},
		{/* fourth display */},
	},

	{ /* three display policy */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_SMART0,
			.attached_layers_nr = 3,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER0, (u8)ROCKCHIP_VOP2_ESMART0, (u8)ROCKCHIP_VOP2_SMART0,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},

		{/* second display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_SMART1,
			.attached_layers_nr = 2,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER1, (u8)ROCKCHIP_VOP2_SMART1,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},

		{/* third  display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART1,
			.attached_layers_nr = 1,
			.attached_layers = { (u8)ROCKCHIP_VOP2_ESMART1,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
				},
		},

		{/* fourth display */},
	},

	{/* reserved for four display policy */},
};

static struct vop2_win_data rk3568_win_data[6] = {
	{
		.name = "Cluster0",
		.phys_id = (u8)ROCKCHIP_VOP2_CLUSTER0,
		.type = CLUSTER_LAYER,
		.win_sel_port_offset = 0,
		.layer_sel_win_id = { 0, 0, 0, 0xff },
		.reg_offset = 0,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 4,
		.max_downscale_factor = 4,
	},

	{
		.name = "Cluster1",
		.phys_id = (u8)ROCKCHIP_VOP2_CLUSTER1,
		.type = CLUSTER_LAYER,
		.win_sel_port_offset = 1,
		.layer_sel_win_id = { 1, 1, 1, 0xff },
		.reg_offset = 0x200,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 4U,
		.max_downscale_factor = 4U,
		.source_win_id = (u8)ROCKCHIP_VOP2_CLUSTER0,
		.feature = (u32)WIN_FEATURE_MIRROR,
	},

	{
		.name = "Esmart0",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART0,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 4,
		.layer_sel_win_id = { 2, 2, 2, 0xff },
		.reg_offset = 0,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
	},

	{
		.name = "Esmart1",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART1,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 5,
		.layer_sel_win_id = { 6, 6, 6, 0xff },
		.reg_offset = 0x200,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
		.source_win_id = (u8)ROCKCHIP_VOP2_ESMART0,
		.feature = (u32)WIN_FEATURE_MIRROR,
	},

	{
		.name = "Smart0",
		.phys_id = (u8)ROCKCHIP_VOP2_SMART0,
		.type = SMART_LAYER,
		.win_sel_port_offset = 6,
		.layer_sel_win_id = { 3, 3, 3, 0xff },
		.reg_offset = 0x400,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
	},

	{
		.name = "Smart1",
		.phys_id = (u8)ROCKCHIP_VOP2_SMART1,
		.type = SMART_LAYER,
		.win_sel_port_offset = 7,
		.layer_sel_win_id = { 7, 7, 7, 0xff },
		.reg_offset = 0x600,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
		.source_win_id = (u8)ROCKCHIP_VOP2_SMART0,
		.feature = (u32)WIN_FEATURE_MIRROR,
	},
};

static struct vop2_vp_data rk3568_vp_data[3] = {
	{
		.feature = (u32)VOP_FEATURE_OUTPUT_10BIT,
		.pre_scan_max_dly = 42,
		.max_output = {4096, 2304},
	},
	{
		.feature = 0,
		.pre_scan_max_dly = 40,
		.max_output = {2048, 1536},
	},
	{
		.feature = 0,
		.pre_scan_max_dly = 40,
		.max_output = {1920, 1080},
	},
};

const struct vop2_data rk3568_vop = {
	.version = VOP_VERSION_RK3568,
	.nr_vps = 3,
	.vp_data = rk3568_vp_data,
	.win_data = rk3568_win_data,
	.plane_mask = rk356x_vp_plane_mask[0],
	.plane_table = rk356x_plane_table,
	.vp_primary_plane_order = rk3568_vp_primary_plane_order,
	.nr_layers = 6,
	.nr_mixers = 5,
	.nr_gammas = 1,
	.dump_regs = rk3568_dump_regs,
	.dump_regs_size = (u32)ARRAY_SIZE(rk3568_dump_regs),
};

static u8 rk3576_vp_default_primary_plane[VOP2_VP_MAX] = {
	(u8)ROCKCHIP_VOP2_ESMART0,
	(u8)ROCKCHIP_VOP2_ESMART1,
	(u8)ROCKCHIP_VOP2_ESMART2,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
};

static struct vop2_plane_table rk3576_plane_table[ROCKCHIP_VOP2_LAYER_MAX] = {
	{ROCKCHIP_VOP2_ESMART0, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART1, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART2, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART3, ESMART_LAYER},
	{ROCKCHIP_VOP2_CLUSTER0, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_CLUSTER1, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
};

static struct vop2_dump_regs rk3576_dump_regs[] = {
	{ RK3568_REG_CFG_DONE, "SYS", 0, 0, 0, false, 0x200 },
	{ RK3528_OVL_SYS, "OVL_SYS", 0, 0, 0, false, 0x50 },
	{ RK3528_OVL_PORT0_CTRL, "OVL_VP0", RK3568_VP0_DSP_CTRL, 0x1, 31, false, 0x80 },
	{ RK3528_OVL_PORT1_CTRL, "OVL_VP1", RK3568_VP1_DSP_CTRL, 0x1, 31, false, 0x80 },
	{ RK3576_OVL_PORT2_CTRL, "OVL_VP2", RK3568_VP2_DSP_CTRL, 0x1, 31, false, 0x80 },
	{ RK3568_VP0_DSP_CTRL, "VP0", RK3568_VP0_DSP_CTRL, 0x1, 31, false, 0x100 },
	{ RK3568_VP1_DSP_CTRL, "VP1", RK3568_VP0_DSP_CTRL, 0x1, 31, false, 0x100 },
	{ RK3568_VP2_DSP_CTRL, "VP2", RK3568_VP0_DSP_CTRL, 0x1, 31, false, 0x100 },
	{ RK3568_CLUSTER0_WIN0_CTRL0, "Cluster0", RK3568_CLUSTER0_WIN0_CTRL0, 0x1, 0, true, 0x200 },
	{ RK3568_CLUSTER1_WIN0_CTRL0, "Cluster1", RK3568_CLUSTER1_WIN0_CTRL0, 0x1, 0, true, 0x200 },
	{ RK3568_ESMART0_CTRL0, "Esmart0", RK3568_ESMART0_REGION0_CTRL, 0x1, 0, true, 0x100 },
	{ RK3568_ESMART1_CTRL0, "Esmart1", RK3568_ESMART1_REGION0_CTRL, 0x1, 0, true, 0x100 },
	{ RK3568_SMART0_CTRL0, "Esmart2", RK3568_SMART0_CTRL0, 0x1, 0, true, 0x100 },
	{ RK3568_SMART1_CTRL0, "Esmart3", RK3568_SMART1_CTRL0, 0x1, 0, true, 0x100 },
	{ RK3528_HDR_LUT_CTRL, "HDR", 0, 0, 0, false, 0x100 },
};

/*
 * RK3576 VOP with 2 Cluster win and 4 Esmart win.
 * Every Esmart win support 4 multi-region.
 * VP0 can use Cluster0/1 and Esmart0/2
 * VP1 can use Cluster0/1 and Esmart1/3
 * VP2 can use Esmart0/1/2/3
 *
 * Scale filter mode:
 *
 * * Cluster:
 * * Support prescale down:
 * * H/V: gt2/avg2 or gt4/avg4
 * * After prescale down:
 *      * nearest-neighbor/bilinear/multi-phase filter for scale up
 *      * nearest-neighbor/bilinear/multi-phase filter for scale down
 *
 * * Esmart:
 * * Support prescale down:
 * * H: gt2/avg2 or gt4/avg4
 * * V: gt2 or gt4
 * * After prescale down:
 *      * nearest-neighbor/bilinear/bicubic for scale up
 *      * nearest-neighbor/bilinear for scale down
 */
static struct vop2_win_data rk3576_win_data[6] = {
	{
		.name = "Esmart0",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART0,
		.type = ESMART_LAYER,
		.layer_sel_win_id = { 2, 0xff, 0, 0xff },
		.reg_offset = 0x0,
		.supported_rotations = (u32)DRM_MODE_REFLECT_Y,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_GT,/* gt only */
		.pd_id = (u16)VOP2_PD_ESMART,
		.axi_id = 0,
		.axi_yrgb_id = 0x0a,
		.axi_uv_id = 0x0b,
		.possible_crtcs = 0x5,/* vp0 or vp2 */
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
		.feature = (u32)WIN_FEATURE_MULTI_AREA | (u32)WIN_FEATURE_Y2R_13BIT_DEPTH,
	},
	{
		.name = "Esmart1",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART1,
		.type = ESMART_LAYER,
		.layer_sel_win_id = { 0xff, 2, 1, 0xff },
		.reg_offset = 0x200,
		.supported_rotations = (u32)DRM_MODE_REFLECT_Y,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_GT,/* gt only */
		.pd_id = (u16)VOP2_PD_ESMART,
		.axi_id = 0,
		.axi_yrgb_id = 0x0c,
		.axi_uv_id = 0x0d,
		.possible_crtcs = 0x6,/* vp1 or vp2 */
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
		.feature = (u32)WIN_FEATURE_MULTI_AREA,
	},

	{
		.name = "Esmart2",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART2,
		.type = ESMART_LAYER,
		.layer_sel_win_id = { 3, 0xff, 2, 0xff },
		.reg_offset = 0x400,
		.supported_rotations = (u32)DRM_MODE_REFLECT_Y,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_GT,/* gt only */
		.pd_id = (u16)VOP2_PD_ESMART,
		.axi_id = 1,
		.axi_yrgb_id = 0x0a,
		.axi_uv_id = 0x0b,
		.possible_crtcs = 0x5,/* vp0 or vp2 */
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
		.feature = (u32)WIN_FEATURE_MULTI_AREA,
	},

	{
		.name = "Esmart3",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART3,
		.type = ESMART_LAYER,
		.layer_sel_win_id = { 0xff, 3, 3, 0xff },
		.reg_offset = 0x600,
		.supported_rotations = (u32)DRM_MODE_REFLECT_Y,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_GT,/* gt only */
		.pd_id = (u16)VOP2_PD_ESMART,
		.axi_id = 1,
		.axi_yrgb_id = 0x0c,
		.axi_uv_id = 0x0d,
		.possible_crtcs = 0x6,/* vp1 or vp2 */
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
		.feature = (u32)WIN_FEATURE_MULTI_AREA,
	},

	{
		.name = "Cluster0",
		.phys_id = (u8)ROCKCHIP_VOP2_CLUSTER0,
		.type = CLUSTER_LAYER,
		.layer_sel_win_id = { 0, 0, 0xff, 0xff },
		.reg_offset = 0x0,
		.supported_rotations = (u32)DRM_MODE_REFLECT_X | (u32)DRM_MODE_REFLECT_Y,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,/* gt or avg */
		.pd_id = (u16)VOP2_PD_CLUSTER,
		.axi_yrgb_id = 0x02,
		.axi_uv_id = 0x03,
		.possible_crtcs = 0x3,/* vp0 or vp1 */
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
		.feature = (u32)WIN_FEATURE_AFBDC | (u32)WIN_FEATURE_CLUSTER_MAIN |
			   (u32)WIN_FEATURE_Y2R_13BIT_DEPTH | (u32)WIN_FEATURE_DCI,
	},

	{
		.name = "Cluster1",
		.phys_id = (u8)ROCKCHIP_VOP2_CLUSTER1,
		.type = CLUSTER_LAYER,
		.layer_sel_win_id = { 1, 1, 0xff, 0xff },
		.reg_offset = 0x200,
		.supported_rotations = (u32)DRM_MODE_REFLECT_X | (u32)DRM_MODE_REFLECT_Y,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.hsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,/* gt or avg */
		.vsd_pre_filter_mode = (u8)VOP3_PRE_SCALE_DOWN_AVG,/* gt or avg */
		.pd_id = (u16)VOP2_PD_CLUSTER,
		.axi_yrgb_id = 0x06,
		.axi_uv_id = 0x07,
		.possible_crtcs = 0x3,/* vp0 or vp1 */
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
		.feature = (u32)WIN_FEATURE_AFBDC | (u32)WIN_FEATURE_CLUSTER_MAIN |
			   (u32)WIN_FEATURE_Y2R_13BIT_DEPTH,
	},
};

/*
 * RK3576 VP0 has 8 lines post linebuffer, when full post line buffer is less 4,
 * the urgency signal will be set to 1, when full post line buffer is over 6, the
 * urgency signal will be set to 0.
 */
static struct vop_urgency rk3576_vp0_urgency = {
	.urgen_thl = 4,
	.urgen_thh = 6,
};

static struct vop2_vp_data rk3576_vp_data[3] = {
	{
		.feature = (u32)VOP_FEATURE_ALPHA_SCALE | (u32)VOP_FEATURE_OVERSCAN | (u32)VOP_FEATURE_VIVID_HDR |
			   (u32)VOP_FEATURE_POST_ACM | (u32)VOP_FEATURE_POST_CSC | (u32)VOP_FEATURE_OUTPUT_10BIT |
			   (u32)VOP_FEATURE_POST_FRC_V2 | (u32)VOP_FEATURE_POST_SHARP,
		.max_output = { 4096, 4096 },
		.hdrvivid_dly = 21,
		.sdr2hdr_dly = 21,
		.layer_mix_dly = 8,
		.hdr_mix_dly = 2,
		.win_dly = 10,
		.pixel_rate = 2,
		.urgency = &rk3576_vp0_urgency,
	},
	{
		.feature = (u32)VOP_FEATURE_ALPHA_SCALE | (u32)VOP_FEATURE_OVERSCAN |
			   (u32)VOP_FEATURE_OUTPUT_10BIT | (u32)VOP_FEATURE_POST_FRC_V2,
		.max_output = { 2560, 2560 },
		.hdrvivid_dly = 0,
		.sdr2hdr_dly = 0,
		.layer_mix_dly = 6,
		.hdr_mix_dly = 0,
		.win_dly = 10,
		.pixel_rate = 1,
	},
	{
		.feature = (u32)VOP_FEATURE_ALPHA_SCALE | (u32)VOP_FEATURE_OVERSCAN,
		.max_output = { 1920, 1920 },
		.hdrvivid_dly = 0,
		.sdr2hdr_dly = 0,
		.layer_mix_dly = 6,
		.hdr_mix_dly = 0,
		.win_dly = 10,
		.pixel_rate = 1,
	},
};

static struct vop2_power_domain_data rk3576_vop_pd_data[] = {
	{
		.id = (u16)VOP2_PD_CLUSTER,
		.module_id_mask = (u32)BIT((u8)ROCKCHIP_VOP2_CLUSTER0) | (u32)BIT((u8)ROCKCHIP_VOP2_CLUSTER1),
	},
	{
		.id = (u16)VOP2_PD_ESMART,
		.module_id_mask = (u32)BIT((u8)ROCKCHIP_VOP2_ESMART0) | (u32)BIT((u8)ROCKCHIP_VOP2_ESMART1) |
				  (u32)BIT((u8)ROCKCHIP_VOP2_ESMART2) | (u32)BIT((u8)ROCKCHIP_VOP2_ESMART3),
	},
};

static const struct vop2_esmart_lb_map rk3576_esmart_lb_mode_map[] = {
	{VOP3_ESMART_4K_4K_4K_MODE, 2},
	{VOP3_ESMART_4K_4K_2K_2K_MODE, 3}
};

const struct vop2_data rk3576_vop = {
	.version = VOP_VERSION_RK3576,
	.nr_vps = 3,
	.nr_mixers = 4,
	.nr_layers = 6,
	.nr_gammas = 3,
	.esmart_lb_mode = VOP3_ESMART_4K_4K_2K_2K_MODE,
	.esmart_lb_mode_num = (u8)ARRAY_SIZE(rk3576_esmart_lb_mode_map),
	.esmart_lb_mode_map = rk3576_esmart_lb_mode_map,
	.vp_data = rk3576_vp_data,
	.win_data = rk3576_win_data,
	.plane_table = rk3576_plane_table,
	.pd = rk3576_vop_pd_data,
	.vp_default_primary_plane = rk3576_vp_default_primary_plane,
	.nr_pd = (u8)ARRAY_SIZE(rk3576_vop_pd_data),
	.dump_regs = rk3576_dump_regs,
	.dump_regs_size = (u32)ARRAY_SIZE(rk3576_dump_regs),
};

static u8 rk3588_vp_primary_plane_order[ROCKCHIP_VOP2_LAYER_MAX] = {
	(u8)ROCKCHIP_VOP2_ESMART0,
	(u8)ROCKCHIP_VOP2_ESMART1,
	(u8)ROCKCHIP_VOP2_ESMART2,
	(u8)ROCKCHIP_VOP2_ESMART3,
	(u8)ROCKCHIP_VOP2_CLUSTER0,
	(u8)ROCKCHIP_VOP2_CLUSTER1,
	(u8)ROCKCHIP_VOP2_CLUSTER2,
	(u8)ROCKCHIP_VOP2_CLUSTER3,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
	(u8)ROCKCHIP_VOP2_LAYER_MAX,
};

static struct vop2_plane_table rk3588_plane_table[ROCKCHIP_VOP2_LAYER_MAX] = {
	{ROCKCHIP_VOP2_CLUSTER0, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_CLUSTER1, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_CLUSTER2, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_CLUSTER3, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_ESMART0, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART1, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART2, ESMART_LAYER},
	{ROCKCHIP_VOP2_ESMART3, ESMART_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
	{ROCKCHIP_VOP2_LAYER_MAX, CLUSTER_LAYER},
};

static struct vop2_dump_regs rk3588_dump_regs[] = {
	{ RK3568_REG_CFG_DONE, "SYS", 0, 0, 0, false },
	{ RK3568_OVL_CTRL, "OVL", 0, 0, 0, false },
	{ RK3568_VP0_DSP_CTRL, "VP0", RK3568_VP0_DSP_CTRL, 0x1, 31, false },
	{ RK3568_VP1_DSP_CTRL, "VP1", RK3568_VP1_DSP_CTRL, 0x1, 31, false },
	{ RK3568_VP2_DSP_CTRL, "VP2", RK3568_VP2_DSP_CTRL, 0x1, 31, false },
	{ RK3588_VP3_DSP_CTRL, "VP3", RK3588_VP3_DSP_CTRL, 0x1, 31, false },
	{ RK3568_CLUSTER0_WIN0_CTRL0, "Cluster0", RK3568_CLUSTER0_WIN0_CTRL0, 0x1, 0, true },
	{ RK3568_CLUSTER1_WIN0_CTRL0, "Cluster1", RK3568_CLUSTER1_WIN0_CTRL0, 0x1, 0, true },
	{ RK3588_CLUSTER2_WIN0_CTRL0, "Cluster2", RK3588_CLUSTER2_WIN0_CTRL0, 0x1, 0, true },
	{ RK3588_CLUSTER3_WIN0_CTRL0, "Cluster3", RK3588_CLUSTER3_WIN0_CTRL0, 0x1, 0, true },
	{ RK3568_ESMART0_CTRL0, "Esmart0", RK3568_ESMART0_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_ESMART1_CTRL0, "Esmart1", RK3568_ESMART1_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_SMART0_CTRL0, "Esmart2", RK3568_SMART0_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_SMART1_CTRL0, "Esmart3", RK3568_SMART1_REGION0_CTRL, 0x1, 0, true },
	{ RK3568_HDR_LUT_CTRL, "HDR", 0, 0, 0, false },
};

static struct vop2_vp_plane_mask rk3588_vp_plane_mask[VOP2_VP_MAX][VOP2_VP_MAX] = {
	{ /* one display policy */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART0,
			.attached_layers_nr = 8,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER0, (u8)ROCKCHIP_VOP2_ESMART0, (u8)ROCKCHIP_VOP2_ESMART2,
				  (u8)ROCKCHIP_VOP2_CLUSTER1, (u8)ROCKCHIP_VOP2_ESMART1, (u8)ROCKCHIP_VOP2_ESMART3,
				  (u8)ROCKCHIP_VOP2_CLUSTER2, (u8)ROCKCHIP_VOP2_CLUSTER3
			},
		},
		{/* second display */},
		{/* third  display */},
		{/* fourth display */},
	},

	{ /* two display policy */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART0,
			.attached_layers_nr = 4,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER0, (u8)ROCKCHIP_VOP2_ESMART0,
				  (u8)ROCKCHIP_VOP2_CLUSTER1, (u8)ROCKCHIP_VOP2_ESMART1,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
			},
		},

		{/* second display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART2,
			.attached_layers_nr = 4,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER2, (u8)ROCKCHIP_VOP2_ESMART2,
				  (u8)ROCKCHIP_VOP2_CLUSTER3, (u8)ROCKCHIP_VOP2_ESMART3,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
			},
		},
		{/* third  display */},
		{/* fourth display */},
	},

	{ /* three display policy */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART0,
			.attached_layers_nr = 3,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER0, (u8)ROCKCHIP_VOP2_CLUSTER1, (u8)ROCKCHIP_VOP2_ESMART0,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
			},
		},

		{/* second display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART1,
			.attached_layers_nr = 3,
			.attached_layers = {
				  (u8)ROCKCHIP_VOP2_CLUSTER2, (u8)ROCKCHIP_VOP2_CLUSTER3, (u8)ROCKCHIP_VOP2_ESMART1,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
			},
		},

		{/* third  display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART2,
			.attached_layers_nr = 2,
			.attached_layers = { (u8)ROCKCHIP_VOP2_ESMART2, (u8)ROCKCHIP_VOP2_ESMART3,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
			},
		},

		{/* fourth display */},
	},

	{ /* four display policy */
		{/* main display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART0,
			.attached_layers_nr = 2,
			.attached_layers = { (u8)ROCKCHIP_VOP2_CLUSTER0, (u8)ROCKCHIP_VOP2_ESMART0,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
			},
		},

		{/* second display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART1,
			.attached_layers_nr = 2,
			.attached_layers = { (u8)ROCKCHIP_VOP2_CLUSTER1, (u8)ROCKCHIP_VOP2_ESMART1,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
			},
		},

		{/* third  display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART2,
			.attached_layers_nr = 2,
			.attached_layers = { (u8)ROCKCHIP_VOP2_CLUSTER2, (u8)ROCKCHIP_VOP2_ESMART2,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
			},
		},

		{/* fourth display */
			.primary_plane_id = (u8)ROCKCHIP_VOP2_ESMART3,
			.attached_layers_nr = 2,
			.attached_layers = { (u8)ROCKCHIP_VOP2_CLUSTER3, (u8)ROCKCHIP_VOP2_ESMART3,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX,
				  (u8)ROCKCHIP_VOP2_LAYER_MAX
			},
		},
	},

};

static struct vop2_win_data rk3588_win_data[8] = {
	{
		.name = "Cluster0",
		.phys_id = (u8)ROCKCHIP_VOP2_CLUSTER0,
		.splice_win_id = (u8)ROCKCHIP_VOP2_CLUSTER1,
		.type = CLUSTER_LAYER,
		.win_sel_port_offset = 0,
		.layer_sel_win_id = { 0, 0, 0, 0 },
		.reg_offset = 0,
		.axi_id = 0,
		.axi_yrgb_id = 2,
		.axi_uv_id = 3,
		.pd_id = (u16)VOP2_PD_CLUSTER0,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 4U,
		.max_downscale_factor = 4U,
	},

	{
		.name = "Cluster1",
		.phys_id = (u8)ROCKCHIP_VOP2_CLUSTER1,
		.type = CLUSTER_LAYER,
		.win_sel_port_offset = 1,
		.layer_sel_win_id = { 1, 1, 1, 1 },
		.reg_offset = 0x200,
		.axi_id = 0,
		.axi_yrgb_id = 6,
		.axi_uv_id = 7,
		.pd_id = (u16)VOP2_PD_CLUSTER1,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 4U,
		.max_downscale_factor = 4U,
	},

	{
		.name = "Cluster2",
		.phys_id = (u8)ROCKCHIP_VOP2_CLUSTER2,
		.splice_win_id = (u8)ROCKCHIP_VOP2_CLUSTER3,
		.type = CLUSTER_LAYER,
		.win_sel_port_offset = 2,
		.layer_sel_win_id = { 4, 4, 4, 4 },
		.reg_offset = 0x400,
		.axi_id = 1,
		.axi_yrgb_id = 2,
		.axi_uv_id = 3,
		.pd_id = (u16)VOP2_PD_CLUSTER2,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 4U,
		.max_downscale_factor = 4U,
	},

	{
		.name = "Cluster3",
		.phys_id = (u8)ROCKCHIP_VOP2_CLUSTER3,
		.type = CLUSTER_LAYER,
		.win_sel_port_offset = 3,
		.layer_sel_win_id = { 5, 5, 5, 5 },
		.reg_offset = 0x600,
		.axi_id = 1,
		.axi_yrgb_id = 6,
		.axi_uv_id = 7,
		.pd_id = (u16)VOP2_PD_CLUSTER3,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 4U,
		.max_downscale_factor = 4U,
	},

	{
		.name = "Esmart0",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART0,
		.splice_win_id = (u8)ROCKCHIP_VOP2_ESMART1,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 4,
		.layer_sel_win_id = { 2, 2, 2, 2 },
		.reg_offset = 0,
		.axi_id = 0,
		.axi_yrgb_id = 0x0a,
		.axi_uv_id = 0x0b,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
	},

	{
		.name = "Esmart1",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART1,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 5,
		.layer_sel_win_id = { 3, 3, 3, 3 },
		.reg_offset = 0x200,
		.axi_id = 0,
		.axi_yrgb_id = 0x0c,
		.axi_uv_id = 0x0d,
		.pd_id = (u16)VOP2_PD_ESMART,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
	},

	{
		.name = "Esmart2",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART2,
		.splice_win_id = (u8)ROCKCHIP_VOP2_ESMART3,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 6,
		.layer_sel_win_id = { 6, 6, 6, 6 },
		.reg_offset = 0x400,
		.axi_id = 1,
		.axi_yrgb_id = 0x0a,
		.axi_uv_id = 0x0b,
		.pd_id = (u16)VOP2_PD_ESMART,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
	},

	{
		.name = "Esmart3",
		.phys_id = (u8)ROCKCHIP_VOP2_ESMART3,
		.type = ESMART_LAYER,
		.win_sel_port_offset = 7,
		.layer_sel_win_id = { 7, 7, 7, 7 },
		.reg_offset = 0x600,
		.axi_id = 1,
		.axi_yrgb_id = 0x0c,
		.axi_uv_id = 0x0d,
		.pd_id = (u16)VOP2_PD_ESMART,
		.hsu_filter_mode = (u8)VOP2_SCALE_UP_BIC,
		.hsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.vsu_filter_mode = (u8)VOP2_SCALE_UP_BIL,
		.vsd_filter_mode = (u8)VOP2_SCALE_DOWN_BIL,
		.max_upscale_factor = 8U,
		.max_downscale_factor = 8U,
	},
};

static struct dsc_error_info dsc_ecw[] = {
	{0x00000000, "no error detected by DSC encoder"},
	{0x0030ffff, "bits per component error"},
	{0x0040ffff, "multiple mode error"},
	{0x0050ffff, "line buffer depth error"},
	{0x0060ffff, "minor version error"},
	{0x0070ffff, "picture height error"},
	{0x0080ffff, "picture width error"},
	{0x0090ffff, "number of slices error"},
	{0x00c0ffff, "slice height Error "},
	{0x00d0ffff, "slice width error"},
	{0x00e0ffff, "second line BPG offset error"},
	{0x00f0ffff, "non second line BPG offset error"},
	{0x0100ffff, "PPS ID error"},
	{0x0110ffff, "bits per pixel (BPP) Error"},
	{0x0120ffff, "buffer flow error"},  /* dsc_buffer_flow */

	{0x01510001, "slice 0 RC buffer model overflow error"},
	{0x01510002, "slice 1 RC buffer model overflow error"},
	{0x01510004, "slice 2 RC buffer model overflow error"},
	{0x01510008, "slice 3 RC buffer model overflow error"},
	{0x01510010, "slice 4 RC buffer model overflow error"},
	{0x01510020, "slice 5 RC buffer model overflow error"},
	{0x01510040, "slice 6 RC buffer model overflow error"},
	{0x01510080, "slice 7 RC buffer model overflow error"},

	{0x01610001, "slice 0 RC buffer model underflow error"},
	{0x01610002, "slice 1 RC buffer model underflow error"},
	{0x01610004, "slice 2 RC buffer model underflow error"},
	{0x01610008, "slice 3 RC buffer model underflow error"},
	{0x01610010, "slice 4 RC buffer model underflow error"},
	{0x01610020, "slice 5 RC buffer model underflow error"},
	{0x01610040, "slice 6 RC buffer model underflow error"},
	{0x01610080, "slice 7 RC buffer model underflow error"},

	{0xffffffffU, "unsuccessfUl RESET cycle status"},
	{0x00a0ffff, "ICH full error precision settings error"},
	{0x0020ffff, "native mode"},
};

static struct dsc_error_info dsc_buffer_flow[] = {
	{0x00000000, "rate buffer status"},
	{0x00000001, "line buffer status"},
	{0x00000002, "decoder model status"},
	{0x00000003, "pixel buffer status"},
	{0x00000004, "balance fifo buffer status"},
	{0x00000005, "syntax element fifo status"},
};

static struct vop2_dsc_data rk3588_dsc_data[] = {
	{
		.id = ROCKCHIP_VOP2_DSC_8K,
		.pd_id = (u16)VOP2_PD_DSC_8K,
		.max_slice_num = 8,
		.max_linebuf_depth = 11,
		.min_bits_per_pixel = 8,
		.dsc_txp_clk_src_name = "dsc_8k_txp_clk_src",
		.dsc_txp_clk_name = "dsc_8k_txp_clk",
		.dsc_pxl_clk_name = "dsc_8k_pxl_clk",
		.dsc_cds_clk_name = "dsc_8k_cds_clk",
	},

	{
		.id = ROCKCHIP_VOP2_DSC_4K,
		.pd_id = (u16)VOP2_PD_DSC_4K,
		.max_slice_num = 2,
		.max_linebuf_depth = 11,
		.min_bits_per_pixel = 8,
		.dsc_txp_clk_src_name = "dsc_4k_txp_clk_src",
		.dsc_txp_clk_name = "dsc_4k_txp_clk",
		.dsc_pxl_clk_name = "dsc_4k_pxl_clk",
		.dsc_cds_clk_name = "dsc_4k_cds_clk",
	},
};

static struct vop2_vp_data rk3588_vp_data[4] = {
	{
		.splice_vp_id = 1,
		.feature = (u32)VOP_FEATURE_OUTPUT_10BIT,
		.pre_scan_max_dly = 54,
		.max_dclk = 600000,
		.max_output = {7680, 4320},
	},
	{
		.feature = (u32)VOP_FEATURE_OUTPUT_10BIT,
		.pre_scan_max_dly = 54,
		.max_dclk = 600000,
		.max_output = {4096, 2304},
	},
	{
		.feature = (u32)VOP_FEATURE_OUTPUT_10BIT,
		.pre_scan_max_dly = 52,
		.max_dclk = 600000,
		.max_output = {4096, 2304},
	},
	{
		.feature = 0,
		.pre_scan_max_dly = 52,
		.max_dclk = 200000,
		.max_output = {1920, 1080},
	},
};

static struct vop2_power_domain_data rk3588_vop_pd_data[] = {
	{
	  .id = (u16)VOP2_PD_CLUSTER0,
	  .module_id_mask = (u32)BIT((u8)ROCKCHIP_VOP2_CLUSTER0),
	},
	{
	  .id = (u16)VOP2_PD_CLUSTER1,
	  .module_id_mask = (u32)BIT((u8)ROCKCHIP_VOP2_CLUSTER1),
	  .parent_id = (u16)VOP2_PD_CLUSTER0,
	},
	{
	  .id = (u16)VOP2_PD_CLUSTER2,
	  .module_id_mask = (u32)BIT((u8)ROCKCHIP_VOP2_CLUSTER2),
	  .parent_id = (u16)VOP2_PD_CLUSTER0,
	},
	{
	  .id = (u16)VOP2_PD_CLUSTER3,
	  .module_id_mask = (u32)BIT((u8)ROCKCHIP_VOP2_CLUSTER3),
	  .parent_id = (u16)VOP2_PD_CLUSTER0,
	},
	{
	  .id = (u16)VOP2_PD_ESMART,
	  .module_id_mask = (u32)BIT((u8)ROCKCHIP_VOP2_ESMART1) |
			    (u32)BIT((u8)ROCKCHIP_VOP2_ESMART2) |
			    (u32)BIT((u8)ROCKCHIP_VOP2_ESMART3),
	},
	{
	  .id = (u16)VOP2_PD_DSC_8K,
	  .module_id_mask = (u32)BIT(ROCKCHIP_VOP2_DSC_8K),
	},
	{
	  .id = (u16)VOP2_PD_DSC_4K,
	  .module_id_mask = (u32)BIT(ROCKCHIP_VOP2_DSC_4K),
	},
};

const struct vop2_data rk3588_vop = {
	.version = VOP_VERSION_RK3588,
	.nr_vps = 4,
	.vp_data = rk3588_vp_data,
	.win_data = rk3588_win_data,
	.plane_mask = rk3588_vp_plane_mask[0],
	.plane_table = rk3588_plane_table,
	.pd = rk3588_vop_pd_data,
	.dsc = rk3588_dsc_data,
	.dsc_error_ecw = dsc_ecw,
	.dsc_error_buffer_flow = dsc_buffer_flow,
	.vp_primary_plane_order = rk3588_vp_primary_plane_order,
	.nr_layers = 8,
	.nr_mixers = 7,
	.nr_gammas = 4,
	.nr_pd = (u8)ARRAY_SIZE(rk3588_vop_pd_data),
	.nr_dscs = 2,
	.nr_dsc_ecw = (u8)ARRAY_SIZE(dsc_ecw),
	.nr_dsc_buffer_flow = (u8)ARRAY_SIZE(dsc_buffer_flow),
	.dump_regs = rk3588_dump_regs,
	.dump_regs_size = (u32)ARRAY_SIZE(rk3588_dump_regs),
};

const struct rockchip_crtc_funcs rockchip_vop2_funcs = {
	.preinit = rockchip_vop2_preinit,
	.prepare = rockchip_vop2_prepare,
	.init = rockchip_vop2_init,
	.set_plane = rockchip_vop2_set_plane,
	.enable = rockchip_vop2_enable,
	.post_enable = rockchip_vop2_post_enable,
	.disable = rockchip_vop2_disable,
	.fixup_dts = rockchip_vop2_fixup_dts,
	.send_mcu_cmd = rockchip_vop2_send_mcu_cmd,
	.check = rockchip_vop2_check,
	.mode_valid = rockchip_vop2_mode_valid,
	.mode_fixup = rockchip_vop2_mode_fixup,
	.plane_check = rockchip_vop2_plane_check,
	.regs_dump = rockchip_vop2_regs_dump,
	.active_regs_dump = rockchip_vop2_active_regs_dump,
	.apply_soft_te = rockchip_vop2_apply_soft_te,
};

// PRQA S 5129 --
// PRQA S 5125 --
// PRQA S 5124 --
// PRQA S 5118 --
// PRQA S 3101 --
